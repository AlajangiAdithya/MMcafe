diff --git a/index.html b/index.html
index 0500404..7b9ed08 100644
--- a/index.html
+++ b/index.html
@@ -11,6 +11,8 @@
     <!-- Speed up the first connection to the services we always hit -->
     <link rel="preconnect" href="https://lh3.googleusercontent.com" crossorigin />
     <link rel="dns-prefetch" href="https://lh3.googleusercontent.com" />
+    <link rel="preconnect" href="https://fonts.googleapis.com" />
+    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
     <link rel="dns-prefetch" href="https://checkout.razorpay.com" />
     <link rel="dns-prefetch" href="https://api.razorpay.com" />
     <link rel="dns-prefetch" href="https://iframe.mediadelivery.net" />
diff --git a/src/App.css b/src/App.css
index a2e36db..2e41444 100644
--- a/src/App.css
+++ b/src/App.css
@@ -1,59 +1,133 @@
 /* ===== FONTS ===== */
-@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700;800;900&family=Yanone+Kaffeesatz:wght@300;400;500;600;700&display=swap');
+@import url('https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,400;0,9..144,500;0,9..144,600;0,9..144,700;1,9..144,300;1,9..144,400;1,9..144,500;1,9..144,600&family=Montserrat:wght@300;400;500;600;700;800;900&family=Yanone+Kaffeesatz:wght@300;400;500;600;700&display=swap');
 
 /* ===== RESET ===== */
 *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
 
 :root {
-  /* Brand from logo */
-  --mm-blue: #4A90D9;
-  --mm-pink: #D4647A;
-  --mm-blue-light: #5EA3E8;
-  --mm-pink-light: #E8798D;
-
-  /* Mastermind palette - Premium Espresso Theme */
-  --bg-primary: #0a0908;
-  --bg-secondary: #11100e;
-  --bg-card: rgba(22, 20, 18, 0.65);
-  --bg-elevated: rgba(36, 32, 28, 0.7);
-  --bg-surface: rgba(30, 28, 25, 0.8);
-  --bg-hover: rgba(42, 38, 34, 0.8);
-
-  --white: #F8F5F2;
-  --text-primary: #EAE6DF;
-  --text-secondary: #B5AEA4;
-  --text-muted: #827C75;
-  --border: rgba(255, 255, 255, 0.08);
-  --border-light: rgba(255, 255, 255, 0.12);
-  --gold-accent: #D4AF37;
-
-  /* Functional */
-  --success: #3AAA3A;
-  --error: #EA3335;
-  --warning: #F2A73B;
-
-  /* Spacing */
-  --container: 1200px;
-  --nav-height: 80px;
-  --section-gap: 80px;
-
-  /* Transitions */
-  --ease: cubic-bezier(0.25, 0.46, 0.45, 0.94);
+  /* ============================================================
+     MASTERMIND BREWS — REFINED ESPRESSO DESIGN SYSTEM
+     One brand color (amber), warm ivory ink, hairline borders.
+     Legacy aliases preserved so existing classes still work.
+     ============================================================ */
+
+  /* Surfaces — warmer, layered cocoa */
+  --bg-primary:   #0c0a09;
+  --bg-secondary: #15110d;
+  --bg-tertiary:  #1c1814;
+  --bg-card:      rgba(28, 24, 20, 0.72);
+  --bg-elevated:  rgba(40, 34, 28, 0.78);
+  --bg-surface:   rgba(34, 29, 24, 0.85);
+  --bg-hover:     rgba(48, 40, 32, 0.88);
+
+  /* Ink — warm ivory family */
+  --ink-100: #F4ECE0;
+  --ink-200: #E2D6C5;
+  --ink-300: #B8A89B;
+  --ink-400: #8B7C6F;
+  --ink-500: #5C5147;
+
+  /* Legacy aliases (do not remove — many components reference these) */
+  --white:          var(--ink-100);
+  --text-primary:   var(--ink-200);
+  --text-secondary: var(--ink-300);
+  --text-muted:     var(--ink-400);
+
+  /* Hairlines — warm-tinted, not cold white */
+  --border:        rgba(244, 236, 224, 0.06);
+  --border-light:  rgba(244, 236, 224, 0.12);
+  --border-strong: rgba(244, 236, 224, 0.20);
+
+  /* SIGNATURE ACCENT — burnished amber. The one hot color. */
+  --accent:       #C9974A;
+  --accent-soft:  #E8C896;
+  --accent-deep:  #8F6A30;
+  --accent-glow:  rgba(201, 151, 74, 0.18);
+
+  /* Brand secondaries — reserved for brand-moment text only (logo, tagline) */
+  --mm-blue:       #5B9BDB;
+  --mm-blue-light: #82B5E5;
+  --mm-pink:       #D67C8E;
+  --mm-pink-light: #E8A0AE;
+  --gold-accent:   var(--accent);
+
+  /* Functional — desaturated to harmonize with warm palette */
+  --success: #6B9362;
+  --error:   #C25C4A;
+  --warning: #D4A047;
+
+  /* Typography stack */
+  --font-sans:    'Montserrat', system-ui, -apple-system, sans-serif;
+  --font-display: 'Fraunces', 'Yanone Kaffeesatz', Georgia, serif;
+  --font-poster:  'Yanone Kaffeesatz', sans-serif;
+
+  /* Fluid type scale (7 stops + display) */
+  --text-xs:   clamp(0.72rem, 0.70rem + 0.10vw, 0.78rem);
+  --text-sm:   clamp(0.82rem, 0.80rem + 0.10vw, 0.88rem);
+  --text-base: clamp(0.95rem, 0.92rem + 0.15vw, 1.02rem);
+  --text-lg:   clamp(1.12rem, 1.05rem + 0.35vw, 1.32rem);
+  --text-xl:   clamp(1.45rem, 1.25rem + 1.00vw, 1.80rem);
+  --text-2xl:  clamp(2.00rem, 1.50rem + 2.50vw, 2.90rem);
+  --text-3xl:  clamp(2.80rem, 2.00rem + 4.00vw, 4.40rem);
+  --text-4xl:  clamp(3.60rem, 2.40rem + 6.00vw, 6.20rem);
+
+  /* Letter spacing */
+  --ls-tight:    -0.02em;
+  --ls-normal:   0;
+  --ls-wide:     0.08em;
+  --ls-widest:   0.22em;
+
+  /* Spacing — 4pt grid */
+  --s-1: 4px;  --s-2: 8px;  --s-3: 12px; --s-4: 16px;
+  --s-5: 24px; --s-6: 32px; --s-7: 48px; --s-8: 64px;
+  --s-9: 96px; --s-10: 128px;
+
+  /* Radius */
+  --radius-sm:   4px;
+  --radius-md:   8px;
+  --radius-lg:   14px;
+  --radius-xl:   22px;
+  --radius-pill: 999px;
+
+  /* Elevation — warm-tinted shadows */
+  --shadow-1: 0 1px 2px rgba(12, 8, 4, 0.30);
+  --shadow-2: 0 4px 12px rgba(12, 8, 4, 0.35);
+  --shadow-3: 0 12px 28px rgba(12, 8, 4, 0.45);
+  --shadow-4: 0 24px 60px rgba(12, 8, 4, 0.55);
+  --shadow-accent: 0 12px 36px rgba(201, 151, 74, 0.22);
+
+  /* Layout */
+  --container:        1280px;
+  --container-narrow: 920px;
+  --nav-height:       76px;
+  --section-gap:      clamp(72px, 9vw, 128px);
+
+  /* Motion */
+  --ease:     cubic-bezier(0.22, 1, 0.36, 1);
+  --ease-in:  cubic-bezier(0.55, 0, 0.68, 0.30);
   --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
+  --dur-fast: 0.18s;
+  --dur-base: 0.32s;
+  --dur-slow: 0.56s;
 }
 
 html { scroll-behavior: smooth; }
 
 body {
-  font-family: 'Montserrat', sans-serif;
+  font-family: var(--font-sans);
   font-weight: 400;
   font-size: 16px;
-  line-height: 1.8;
-  background: radial-gradient(circle at 50% 20%, #1a1714 0%, #0a0908 70%);
-  color: var(--text-primary);
+  line-height: 1.75;
+  background:
+    radial-gradient(ellipse 80% 50% at 80% 0%, rgba(201, 151, 74, 0.05) 0%, transparent 55%),
+    radial-gradient(ellipse 60% 60% at 20% 30%, rgba(28, 24, 20, 0.8) 0%, transparent 60%),
+    var(--bg-primary);
+  background-attachment: fixed;
+  color: var(--ink-200);
   -webkit-font-smoothing: antialiased;
   -moz-osx-font-smoothing: grayscale;
   overflow-x: hidden;
+  text-rendering: optimizeLegibility;
 }
 
 a { color: inherit; text-decoration: none; }
@@ -61,22 +135,103 @@ button { cursor: pointer; border: none; background: none; font-family: inherit;
 img { max-width: 100%; display: block; }
 ul { list-style: none; }
 
-::selection { background: var(--mm-blue); color: var(--white); }
+::selection { background: var(--accent); color: #1a1208; }
 
 .app { min-height: 100vh; display: flex; flex-direction: column; }
 .main-content { flex: 1; padding-top: var(--nav-height); }
 
 /* ===== TYPOGRAPHY ===== */
 .display-font {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
+  font-family: var(--font-poster);
   letter-spacing: 0.04em;
   text-transform: uppercase;
 }
 
+.serif {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+}
+
 h1, h2, h3, h4, h5, h6 {
-  font-family: 'Montserrat', sans-serif;
+  font-family: var(--font-display);
+  font-weight: 500;
+  line-height: 1.08;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
+}
+
+h1 { font-variation-settings: 'opsz' 96, 'SOFT' 30; }
+h2 { font-variation-settings: 'opsz' 72, 'SOFT' 30; }
+h3 { font-variation-settings: 'opsz' 48, 'SOFT' 30; }
+h4, h5, h6 { font-variation-settings: 'opsz' 32, 'SOFT' 30; }
+
+/* ===== EDITORIAL PRIMITIVES (shared across pages) ===== */
+.eyebrow {
+  display: inline-flex;
+  align-items: center;
+  gap: 14px;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
   font-weight: 700;
-  line-height: 1.2;
+  letter-spacing: var(--ls-widest);
+  text-transform: uppercase;
+  color: var(--accent);
+}
+.eyebrow::before {
+  content: '';
+  width: 28px;
+  height: 1px;
+  background: linear-gradient(90deg, transparent, var(--accent) 30%);
+}
+.eyebrow--center::before { display: none; }
+.eyebrow--center { gap: 0; }
+
+.surface {
+  position: relative;
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
+  backdrop-filter: blur(18px) saturate(1.05);
+  -webkit-backdrop-filter: blur(18px) saturate(1.05);
+  transition: border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease),
+              background-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease);
+}
+.surface::before {
+  content: '';
+  position: absolute;
+  inset: 0 0 auto 0;
+  height: 1px;
+  background: linear-gradient(90deg, transparent, rgba(244, 236, 224, 0.10), transparent);
+  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
+  pointer-events: none;
+}
+.surface:hover {
+  border-color: var(--border-strong);
+  box-shadow: var(--shadow-3);
+}
+.surface--interactive:hover {
+  transform: translateY(-2px);
+}
+
+.divider-rule {
+  height: 1px;
+  background: linear-gradient(90deg, transparent, var(--border-strong) 50%, transparent);
+  border: 0;
+  margin: var(--s-7) 0;
+}
+
+.text-accent { color: var(--accent); }
+.text-accent-soft { color: var(--accent-soft); }
+.text-ink-300 { color: var(--ink-300); }
+
+.text-gradient {
+  background: linear-gradient(135deg, var(--ink-100) 0%, var(--accent-soft) 50%, var(--accent) 100%);
+  -webkit-background-clip: text;
+  background-clip: text;
+  -webkit-text-fill-color: transparent;
+  color: transparent;
 }
 
 /* ===== CONTAINER ===== */
@@ -92,86 +247,83 @@ h1, h2, h3, h4, h5, h6 {
   align-items: center;
   justify-content: center;
   gap: 10px;
-  padding: 16px 36px;
-  font-family: 'Montserrat', sans-serif;
-  font-weight: 700;
-  font-size: 0.8rem;
-  letter-spacing: 0.12em;
+  padding: 14px 28px;
+  font-family: var(--font-sans);
+  font-weight: 600;
+  font-size: 11.5px;
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
-  transition: all 0.35s var(--ease);
+  border-radius: var(--radius-md);
+  border: 1px solid transparent;
+  transition: transform var(--dur-base) var(--ease),
+              background-color var(--dur-base) var(--ease),
+              border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease),
+              color var(--dur-base) var(--ease);
   position: relative;
   overflow: hidden;
+  will-change: transform;
+  white-space: nowrap;
+}
+.btn:hover { transform: translateY(-1px); }
+.btn:active { transform: translateY(0); }
+.btn:focus-visible {
+  outline: 2px solid var(--accent);
+  outline-offset: 3px;
 }
 
+/* PRIMARY — warm ivory fill. The site's default CTA. */
 .btn-primary {
-  background: var(--white);
-  color: var(--bg-primary);
+  background: var(--ink-100);
+  color: #1a1208;
+  box-shadow: var(--shadow-2);
 }
 .btn-primary:hover {
-  background: var(--mm-blue);
-  color: var(--white);
-  transform: translateY(-2px);
-}
-
-.btn-outline {
-  border: 2px solid var(--white);
-  color: var(--white);
-  background: transparent;
-}
-.btn-outline:hover {
-  background: var(--white);
-  color: var(--bg-primary);
-  transform: translateY(-2px);
-}
-
-.btn-blue {
-  background: var(--mm-blue);
-  color: var(--white);
-}
-.btn-blue:hover {
-  background: var(--mm-blue-light);
-  transform: translateY(-2px);
+  background: #FBF5EB;
+  box-shadow: var(--shadow-3);
 }
 
-.btn-pink {
-  background: var(--mm-pink);
-  color: var(--white);
+/* ACCENT family (blue/gold/pink legacy aliases) — burnished amber */
+.btn-blue, .btn-gold, .btn-pink {
+  background: var(--accent);
+  color: #1a1208;
+  box-shadow: var(--shadow-accent);
 }
-.btn-pink:hover {
-  background: var(--mm-pink-light);
-  transform: translateY(-2px);
+.btn-blue:hover, .btn-gold:hover, .btn-pink:hover {
+  background: var(--accent-soft);
+  box-shadow: var(--shadow-accent), var(--shadow-3);
 }
 
-.btn-gold {
-  background: linear-gradient(135deg, #E6C27A 0%, #D4AF37 100%);
-  color: #111;
-  border: none;
-  box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
+/* OUTLINE — hairline ivory ghost */
+.btn-outline {
+  border: 1px solid var(--border-strong);
+  color: var(--ink-100);
+  background: transparent;
 }
-.btn-gold:hover {
-  background: linear-gradient(135deg, #F3D08A 0%, #E6C27A 100%);
-  transform: translateY(-2px);
-  box-shadow: 0 6px 20px rgba(212, 175, 55, 0.4);
+.btn-outline:hover {
+  background: rgba(244, 236, 224, 0.04);
+  border-color: var(--ink-100);
 }
 
+/* GHOST — link-style tertiary */
 .btn-ghost {
   background: transparent;
-  color: var(--text-primary);
-  padding: 10px 20px;
-  font-size: 0.75rem;
+  color: var(--ink-300);
+  padding: 10px 14px;
+  font-size: 11px;
 }
-.btn-ghost:hover { color: var(--mm-blue); }
+.btn-ghost:hover { color: var(--accent-soft); }
 
 .btn-success {
   background: var(--success);
-  color: var(--white);
-  opacity: 0.85;
+  color: var(--ink-100);
+  opacity: 0.95;
   pointer-events: none;
 }
 
 .btn-sm {
-  padding: 10px 20px;
-  font-size: 0.7rem;
+  padding: 10px 18px;
+  font-size: 10.5px;
 }
 
 .full-width { width: 100%; }
@@ -182,10 +334,15 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   align-items: center;
   justify-content: center;
-  border-radius: 0;
-  transition: all 0.2s;
+  border-radius: var(--radius-md);
+  color: var(--ink-300);
+  transition: background-color var(--dur-fast) var(--ease),
+              color var(--dur-fast) var(--ease);
+}
+.icon-btn:hover {
+  background: var(--bg-hover);
+  color: var(--ink-100);
 }
-.icon-btn:hover { background: var(--bg-hover); }
 
 /* ===== NAVBAR ===== */
 .navbar {
@@ -195,13 +352,18 @@ h1, h2, h3, h4, h5, h6 {
   right: 0;
   z-index: 100;
   height: var(--nav-height);
-  transition: all 0.4s var(--ease);
+  transition: height var(--dur-base) var(--ease),
+              background-color var(--dur-base) var(--ease),
+              border-color var(--dur-base) var(--ease),
+              backdrop-filter var(--dur-base) var(--ease);
 }
 
 .navbar.scrolled {
-  background: rgba(13, 13, 13, 0.95);
-  backdrop-filter: blur(20px);
-  border-bottom: 1px solid var(--border);
+  height: 64px;
+  background: rgba(12, 10, 9, 0.78);
+  backdrop-filter: blur(28px) saturate(1.4);
+  -webkit-backdrop-filter: blur(28px) saturate(1.4);
+  border-bottom: 1px solid var(--border-light);
 }
 
 .nav-container {
@@ -218,17 +380,17 @@ h1, h2, h3, h4, h5, h6 {
 .nav-logo {
   display: flex;
   align-items: center;
-  gap: 12px;
+  gap: 14px;
   z-index: 101;
   flex-shrink: 0;
 }
 
 .nav-logo img {
-  width: 42px;
-  height: 42px;
-  transition: transform 0.3s var(--ease);
+  width: 40px;
+  height: 40px;
+  transition: transform var(--dur-base) var(--ease);
 }
-.nav-logo:hover img { transform: scale(1.05); }
+.nav-logo:hover img { transform: rotate(-4deg) scale(1.04); }
 
 .nav-logo-text {
   display: flex;
@@ -237,62 +399,64 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .nav-logo-text .brand-name {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.2rem;
-  letter-spacing: 0.16em;
-  text-transform: uppercase;
-  color: var(--white);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-weight: 500;
+  font-size: 1.05rem;
+  letter-spacing: 0.005em;
+  color: var(--ink-100);
 }
 
 .nav-logo-text .brand-sub {
-  font-size: 0.55rem;
-  font-weight: 500;
-  letter-spacing: 0.26em;
+  font-size: 9.5px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-muted);
-  margin-top: 3px;
+  color: var(--ink-400);
+  margin-top: 5px;
 }
 
 .nav-center {
   display: flex;
   align-items: center;
-  gap: 2px;
+  gap: 4px;
   flex: 1;
   justify-content: center;
 }
 
 .nav-link {
-  padding: 10px 14px;
+  padding: 10px 16px;
   font-weight: 600;
-  font-size: 0.72rem;
-  letter-spacing: 0.14em;
+  font-size: 11px;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-secondary);
-  transition: color 0.25s var(--ease);
+  color: var(--ink-300);
+  transition: color var(--dur-fast) var(--ease);
   position: relative;
   white-space: nowrap;
 }
 .nav-link::after {
   content: '';
   position: absolute;
-  bottom: 2px;
-  left: 14px;
-  right: 14px;
-  height: 2px;
-  background: var(--mm-blue);
-  transform: scaleX(0);
-  transform-origin: center;
-  transition: transform 0.3s var(--ease);
+  bottom: 0;
+  left: 50%;
+  width: 5px;
+  height: 5px;
+  border-radius: 50%;
+  background: var(--accent);
+  transform: translateX(-50%) scale(0);
+  transition: transform var(--dur-base) var(--ease);
+  box-shadow: 0 0 12px var(--accent-glow);
 }
-.nav-link:hover { color: var(--white); }
-.nav-link:hover::after { transform: scaleX(1); }
-.nav-link.active { color: var(--white); }
-.nav-link.active::after { transform: scaleX(1); }
+.nav-link:hover { color: var(--ink-100); }
+.nav-link:hover::after { transform: translateX(-50%) scale(1); }
+.nav-link.active { color: var(--ink-100); }
+.nav-link.active::after { transform: translateX(-50%) scale(1); }
 
 .nav-actions {
   display: flex;
   align-items: center;
-  gap: 10px;
+  gap: 6px;
   flex-shrink: 0;
 }
 
@@ -303,20 +467,29 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   align-items: center;
   justify-content: center;
-  transition: all 0.2s;
+  color: var(--ink-300);
+  border-radius: var(--radius-md);
+  transition: background-color var(--dur-fast) var(--ease),
+              color var(--dur-fast) var(--ease);
+}
+.cart-btn:hover {
+  color: var(--ink-100);
+  background: var(--bg-hover);
 }
-.cart-btn:hover { color: var(--mm-blue); }
 
 .cart-badge {
   position: absolute;
-  top: 4px;
+  top: 6px;
   right: 4px;
-  background: var(--mm-pink);
-  color: var(--white);
-  font-size: 0.6rem;
+  background: var(--accent);
+  color: #1a1208;
+  font-size: 9.5px;
   font-weight: 800;
-  width: 18px;
+  letter-spacing: 0;
+  min-width: 18px;
   height: 18px;
+  padding: 0 5px;
+  border-radius: var(--radius-pill);
   display: flex;
   align-items: center;
   justify-content: center;
@@ -345,41 +518,48 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   align-items: center;
   gap: 8px;
-  padding: 9px 18px;
-  font-size: 0.7rem;
-  font-weight: 700;
-  letter-spacing: 0.1em;
+  padding: 9px 16px;
+  font-size: 10.5px;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
-  color: var(--text-secondary);
-  border: 1px solid var(--border);
-  border-radius: 6px;
-  transition: all 0.3s;
+  color: var(--ink-300);
+  border-radius: var(--radius-md);
+  transition: color var(--dur-fast) var(--ease),
+              background-color var(--dur-fast) var(--ease);
 }
 .login-btn:hover {
-  color: var(--white);
-  border-color: var(--white);
+  color: var(--ink-100);
+  background: var(--bg-hover);
 }
 
 .auth-actions {
   display: flex;
   align-items: center;
-  gap: 10px;
+  gap: 8px;
+  padding-left: 6px;
+  margin-left: 4px;
+  border-left: 1px solid var(--border);
 }
 
 .signup-btn {
-  padding: 9px 20px;
-  font-size: 0.7rem;
-  font-weight: 700;
-  letter-spacing: 0.1em;
+  padding: 10px 20px;
+  font-size: 10.5px;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
-  background: var(--mm-blue);
-  color: var(--white);
-  border-radius: 6px;
-  transition: all 0.3s;
+  background: var(--accent);
+  color: #1a1208;
+  border-radius: var(--radius-md);
+  box-shadow: var(--shadow-accent);
+  transition: background-color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease),
+              box-shadow var(--dur-fast) var(--ease);
 }
 .signup-btn:hover {
-  background: var(--mm-blue-light);
+  background: var(--accent-soft);
   transform: translateY(-1px);
+  box-shadow: var(--shadow-accent), var(--shadow-2);
 }
 
 .mobile-menu-btn { display: none; z-index: 101; }
@@ -405,17 +585,24 @@ h1, h2, h3, h4, h5, h6 {
   position: absolute;
   inset: 0;
   background: center/cover no-repeat;
-  opacity: 0.42;
-  filter: saturate(0.5) brightness(0.85);
-  transform: scale(1.05);
+  opacity: 0.38;
+  filter: saturate(0.7) brightness(0.92) contrast(1.08);
+  transform: scale(1.04);
+  animation: heroKenBurns 30s ease-in-out infinite alternate;
+}
+
+@keyframes heroKenBurns {
+  0%   { transform: scale(1.04) translate3d(0, 0, 0); }
+  100% { transform: scale(1.12) translate3d(-2%, -1%, 0); }
 }
 
 .hero-gradient {
   position: absolute;
   inset: 0;
   background:
-    radial-gradient(ellipse 60% 70% at 50% 50%, transparent 0%, rgba(10,9,8,0.55) 60%, var(--bg-primary) 100%),
-    linear-gradient(180deg, var(--bg-primary) 0%, transparent 28%, transparent 62%, var(--bg-primary) 100%);
+    radial-gradient(ellipse 70% 80% at 85% 0%, rgba(201, 151, 74, 0.10) 0%, transparent 50%),
+    radial-gradient(ellipse 70% 80% at 50% 60%, transparent 0%, rgba(12, 10, 9, 0.65) 65%, var(--bg-primary) 100%),
+    linear-gradient(180deg, var(--bg-primary) 0%, transparent 22%, transparent 68%, var(--bg-primary) 100%);
 }
 
 .hero-content {
@@ -432,39 +619,45 @@ h1, h2, h3, h4, h5, h6 {
 .hero-badge {
   display: inline-flex;
   align-items: center;
-  gap: 10px;
-  padding: 8px 20px;
+  gap: 12px;
+  padding: 9px 18px;
   border: 1px solid var(--border-light);
-  font-size: 0.65rem;
-  font-weight: 700;
-  letter-spacing: 0.2em;
+  font-size: 10px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-muted);
+  color: var(--ink-300);
   margin-bottom: 36px;
-  border-radius: 999px;
-  background: rgba(255,255,255,0.02);
-  backdrop-filter: blur(8px);
+  border-radius: var(--radius-pill);
+  background: rgba(28, 24, 20, 0.55);
+  backdrop-filter: blur(16px);
+  -webkit-backdrop-filter: blur(16px);
+  box-shadow: var(--shadow-1);
 }
 
 .hero-badge .dot {
   width: 6px;
   height: 6px;
-  background: var(--mm-blue);
-  animation: pulse 2s ease infinite;
+  border-radius: 50%;
+  background: var(--accent);
+  box-shadow: 0 0 12px var(--accent-glow);
+  animation: pulse 2.4s ease infinite;
 }
 
 @keyframes pulse {
-  0%, 100% { opacity: 1; }
-  50% { opacity: 0.3; }
+  0%, 100% { opacity: 1; transform: scale(1); }
+  50% { opacity: 0.5; transform: scale(0.85); }
 }
 
 .hero h1 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
   font-size: clamp(3.5rem, 8vw, 7rem);
-  line-height: 0.95;
-  letter-spacing: 0.04em;
-  text-transform: uppercase;
+  line-height: 0.98;
+  letter-spacing: var(--ls-tight);
   margin-bottom: 28px;
+  color: var(--ink-100);
 }
 
 .hero h1 .text-blue { color: var(--mm-blue); }
@@ -472,7 +665,7 @@ h1, h2, h3, h4, h5, h6 {
 
 .hero-logo-wrap {
   position: relative;
-  margin-bottom: 28px;
+  margin-bottom: 32px;
   display: flex;
   justify-content: center;
   align-items: center;
@@ -484,46 +677,46 @@ h1, h2, h3, h4, h5, h6 {
   display: block;
   width: clamp(40px, 8vw, 120px);
   height: 1px;
-  background: linear-gradient(90deg, transparent, var(--border-light), transparent);
-  margin: 0 24px;
+  background: linear-gradient(90deg, transparent, var(--border-strong), transparent);
+  margin: 0 28px;
 }
 
 .hero-logo {
-  width: clamp(120px, 14vw, 180px);
+  width: clamp(96px, 11vw, 132px);
   height: auto;
-  filter: drop-shadow(0 12px 32px rgba(0,0,0,0.55));
+  filter: drop-shadow(0 18px 36px rgba(0, 0, 0, 0.55));
 }
 
 .hero-tagline {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2.8rem, 7vw, 5.6rem);
-  line-height: 0.98;
-  letter-spacing: 0.03em;
-  text-transform: uppercase;
-  margin: 0 auto 24px;
-  max-width: 880px;
-  font-weight: 600;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.6rem, 7vw, 5.6rem);
+  line-height: 1.02;
+  letter-spacing: var(--ls-tight);
+  margin: 0 auto 28px;
+  max-width: 920px;
   text-wrap: balance;
+  color: var(--ink-100);
 }
 
-.hero-tagline .text-blue {
-  color: var(--mm-blue);
-  font-style: italic;
-  font-weight: 500;
-}
+.hero-tagline .text-blue,
 .hero-tagline .text-pink {
-  color: var(--mm-pink);
   font-style: italic;
-  font-weight: 500;
+  font-weight: 400;
+  font-variation-settings: 'opsz' 144, 'SOFT' 100;
+  color: var(--accent-soft);
 }
 
 .hero-kicker {
-  font-size: 1.02rem;
-  line-height: 1.75;
-  color: var(--text-secondary);
+  font-family: var(--font-sans);
+  font-size: 1.04rem;
+  line-height: 1.7;
+  color: var(--ink-300);
   max-width: 560px;
-  margin: 0 auto 44px;
+  margin: 0 auto 48px;
   text-wrap: balance;
+  font-weight: 400;
 }
 
 .hero .hero-btns {
@@ -533,15 +726,15 @@ h1, h2, h3, h4, h5, h6 {
 .hero-desc {
   font-size: 1rem;
   font-weight: 400;
-  line-height: 1.9;
-  color: var(--text-secondary);
+  line-height: 1.85;
+  color: var(--ink-300);
   max-width: 520px;
   margin-bottom: 48px;
 }
 
 .hero-btns {
   display: flex;
-  gap: 16px;
+  gap: 14px;
   flex-wrap: wrap;
 }
 
@@ -553,18 +746,18 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   flex-direction: column;
   align-items: center;
-  gap: 8px;
-  color: var(--text-muted);
-  font-size: 0.6rem;
+  gap: 10px;
+  color: var(--ink-400);
+  font-size: 10px;
   font-weight: 600;
-  letter-spacing: 0.2em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  animation: bobble 2s ease infinite;
+  animation: bobble 2.4s ease infinite;
 }
 
 @keyframes bobble {
-  0%, 100% { transform: translateX(-50%) translateY(0); }
-  50% { transform: translateX(-50%) translateY(8px); }
+  0%, 100% { transform: translateX(-50%) translateY(0); opacity: 0.7; }
+  50%      { transform: translateX(-50%) translateY(6px); opacity: 1; }
 }
 
 /* ===== SECTION TITLES ===== */
@@ -573,47 +766,64 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .section-label {
-  font-size: 0.65rem;
+  display: inline-flex;
+  align-items: center;
+  gap: 14px;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
   font-weight: 700;
-  letter-spacing: 0.25em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--mm-blue);
-  margin-bottom: 16px;
+  color: var(--accent);
+  margin-bottom: 20px;
+}
+.section-label::before {
+  content: '';
+  width: 28px;
+  height: 1px;
+  background: linear-gradient(90deg, transparent, var(--accent) 30%);
+}
+.section-header.center .section-label {
+  margin-left: auto;
+  margin-right: auto;
 }
+.section-header.center .section-label::before { display: none; }
+.section-header.center .section-label { gap: 0; }
 
 .section-title {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2rem, 4vw, 3.2rem);
-  letter-spacing: 0.04em;
-  text-transform: uppercase;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.2rem, 4.2vw, 3.6rem);
+  letter-spacing: var(--ls-tight);
   line-height: 1.05;
+  color: var(--ink-100);
 }
 
 .section-desc {
-  font-size: 0.95rem;
-  color: var(--text-secondary);
-  max-width: 560px;
-  margin-top: 16px;
-  line-height: 1.8;
+  font-size: 1rem;
+  color: var(--ink-300);
+  max-width: 580px;
+  margin-top: 18px;
+  line-height: 1.75;
 }
 
 .section-header.center { text-align: center; }
-.section-header.center .section-desc { margin: 16px auto 0; }
+.section-header.center .section-desc { margin: 18px auto 0; }
 
 /* ===== ABOUT STRIP ===== */
 .about-strip {
   padding: var(--section-gap) 0;
-  background: rgba(17, 16, 14, 0.5);
-  backdrop-filter: blur(10px);
-  -webkit-backdrop-filter: blur(10px);
+  background:
+    linear-gradient(180deg, transparent 0%, rgba(21, 17, 13, 0.5) 50%, transparent 100%);
   border-top: 1px solid var(--border);
   border-bottom: 1px solid var(--border);
 }
 
 .about-grid {
   display: grid;
-  grid-template-columns: 1fr 1fr;
-  gap: 80px;
+  grid-template-columns: 5fr 7fr;
+  gap: clamp(48px, 7vw, 96px);
   align-items: center;
 }
 
@@ -621,74 +831,89 @@ h1, h2, h3, h4, h5, h6 {
   position: relative;
   aspect-ratio: 4/5;
   overflow: hidden;
+  border-radius: var(--radius-lg);
+  border: 1px solid var(--border);
 }
 
 .about-image img {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  filter: saturate(0.8);
-  transition: transform 0.6s var(--ease);
+  filter: saturate(0.85) brightness(0.95);
+  transition: transform var(--dur-slow) var(--ease);
 }
-.about-image:hover img { transform: scale(1.03); }
+.about-image:hover img { transform: scale(1.04); }
 
 .about-image .accent-line {
   position: absolute;
-  bottom: 0;
-  left: 0;
-  width: 4px;
-  height: 40%;
-  background: linear-gradient(to top, var(--mm-blue), var(--mm-pink));
+  bottom: -24px;
+  left: -16px;
+  width: 2px;
+  height: 80%;
+  background: linear-gradient(to top, transparent, var(--accent) 30%, var(--accent-soft) 100%);
+  box-shadow: 0 0 24px var(--accent-glow);
 }
 
 .about-text h2 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 2.8rem;
-  letter-spacing: 0.04em;
-  text-transform: uppercase;
-  margin-bottom: 24px;
-  line-height: 1.05;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.2rem, 3.6vw, 3.2rem);
+  letter-spacing: var(--ls-tight);
+  margin-bottom: 28px;
+  line-height: 1.06;
+  color: var(--ink-100);
 }
 
 .about-text p {
-  color: var(--text-secondary);
-  margin-bottom: 16px;
-  font-size: 0.95rem;
-}
-
+  color: var(--ink-300);
+  margin-bottom: 18px;
+  font-size: 1rem;
+  line-height: 1.8;
+}
+
 .about-text p.highlight {
-  color: var(--text-primary);
-  font-weight: 600;
-  font-size: 1.05rem;
-  border-left: 3px solid var(--mm-blue);
-  padding-left: 20px;
-  margin: 28px 0;
+  position: relative;
+  color: var(--ink-100);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 100;
+  font-style: italic;
+  font-weight: 400;
+  font-size: 1.32rem;
+  line-height: 1.5;
+  padding-left: 24px;
+  border-left: 2px solid var(--accent);
+  margin: 32px 0;
 }
 
 .about-stats {
   display: flex;
-  gap: 48px;
-  margin-top: 40px;
-  padding-top: 32px;
+  gap: clamp(28px, 4vw, 56px);
+  margin-top: 48px;
+  padding-top: 36px;
   border-top: 1px solid var(--border);
 }
 
 .stat-item .stat-number {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 2.4rem;
-  letter-spacing: 0.04em;
-  color: var(--white);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 72, 'SOFT' 30;
+  font-weight: 500;
+  font-size: 2.8rem;
+  letter-spacing: var(--ls-tight);
+  line-height: 1;
+  color: var(--ink-100);
 }
 
-.stat-item .stat-number .stat-accent { color: var(--mm-blue); }
+.stat-item .stat-number .stat-accent { color: var(--accent); margin-left: 2px; }
 
 .stat-item .stat-label {
-  font-size: 0.7rem;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
   font-weight: 600;
-  letter-spacing: 0.12em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-muted);
-  margin-top: 4px;
+  color: var(--ink-400);
+  margin-top: 10px;
 }
 
 /* ===== FEATURED PRODUCTS ===== */
@@ -700,67 +925,73 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   justify-content: space-between;
   align-items: flex-end;
-  margin-bottom: 48px;
+  margin-bottom: 56px;
+  gap: 24px;
+  flex-wrap: wrap;
 }
 
 .featured-grid {
   display: grid;
   grid-template-columns: repeat(4, 1fr);
-  gap: 2px;
+  gap: 20px;
 }
 
 .featured-product {
+  position: relative;
   background: var(--bg-card);
   backdrop-filter: blur(16px);
   -webkit-backdrop-filter: blur(16px);
   border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
   padding: 0;
-  transition: all 0.4s var(--ease);
-  position: relative;
   overflow: hidden;
-  group: true;
+  transition: border-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
 }
-.featured-product:hover { 
-  background: var(--bg-hover);
-  border-color: var(--border-light);
-  box-shadow: 0 10px 30px rgba(0,0,0,0.5);
+.featured-product:hover {
+  border-color: var(--border-strong);
+  transform: translateY(-3px);
+  box-shadow: var(--shadow-3);
 }
 
 .featured-product-image {
-  aspect-ratio: 1;
+  aspect-ratio: 4 / 5;
   overflow: hidden;
   position: relative;
+  background: var(--bg-tertiary);
 }
 
 .featured-product-image img {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  transition: transform 0.6s var(--ease);
-  filter: saturate(0.85);
+  transition: transform var(--dur-slow) var(--ease);
+  filter: saturate(0.92);
 }
 .featured-product:hover .featured-product-image img {
-  transform: scale(1.08);
+  transform: scale(1.06);
   filter: saturate(1);
 }
 
 .featured-product-overlay {
   position: absolute;
   inset: 0;
-  background: linear-gradient(180deg, transparent 50%, rgba(0,0,0,0.7) 100%);
+  background: linear-gradient(180deg, transparent 55%, rgba(12, 10, 9, 0.85) 100%);
   opacity: 0;
-  transition: opacity 0.4s;
+  transition: opacity var(--dur-base) var(--ease);
 }
 .featured-product:hover .featured-product-overlay { opacity: 1; }
 
 .featured-product-quick {
   position: absolute;
-  bottom: 16px;
-  left: 16px;
-  right: 16px;
+  bottom: 14px;
+  left: 14px;
+  right: 14px;
   opacity: 0;
-  transform: translateY(10px);
-  transition: all 0.4s var(--ease);
+  transform: translateY(12px);
+  transition: opacity var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease);
 }
 .featured-product:hover .featured-product-quick {
   opacity: 1;
@@ -768,64 +999,79 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .featured-product-info {
-  padding: 20px;
+  padding: 22px 22px 24px;
 }
 
 .featured-product-category {
-  font-size: 0.6rem;
+  font-family: var(--font-sans);
+  font-size: 9.5px;
   font-weight: 700;
-  letter-spacing: 0.2em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--mm-blue);
-  margin-bottom: 8px;
+  color: var(--accent);
+  margin-bottom: 10px;
 }
 
 .featured-product-name {
-  font-size: 0.85rem;
-  font-weight: 600;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.12rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
   margin-bottom: 4px;
-  line-height: 1.4;
+  line-height: 1.3;
+  color: var(--ink-100);
 }
 
 .featured-product-weight {
-  font-size: 0.7rem;
-  color: var(--text-muted);
-  margin-bottom: 12px;
+  font-size: 11.5px;
+  font-weight: 500;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--ink-400);
+  margin-bottom: 16px;
 }
 
 .featured-product-bottom {
   display: flex;
   align-items: center;
   justify-content: space-between;
+  padding-top: 14px;
+  border-top: 1px solid var(--border);
 }
 
 .featured-product-price {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-weight: 500;
   font-size: 1.4rem;
-  letter-spacing: 0.04em;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 
 .add-btn {
-  width: 40px;
-  height: 40px;
+  width: 38px;
+  height: 38px;
   display: flex;
   align-items: center;
   justify-content: center;
-  border: 1px solid var(--border);
-  transition: all 0.3s;
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-md);
+  color: var(--ink-200);
+  transition: background-color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease),
+              color var(--dur-fast) var(--ease);
 }
 .add-btn:hover {
-  background: var(--mm-blue);
-  border-color: var(--mm-blue);
-  color: var(--white);
+  background: var(--accent);
+  border-color: var(--accent);
+  color: #1a1208;
 }
 
 /* ===== CATEGORIES BAND ===== */
 .categories-band {
-  padding: 80px 0;
-  background: rgba(17, 16, 14, 0.5);
-  backdrop-filter: blur(10px);
-  -webkit-backdrop-filter: blur(10px);
+  padding: var(--section-gap) 0;
+  background: linear-gradient(180deg, transparent, rgba(21, 17, 13, 0.6) 50%, transparent);
   border-top: 1px solid var(--border);
   border-bottom: 1px solid var(--border);
 }
@@ -833,25 +1079,28 @@ h1, h2, h3, h4, h5, h6 {
 .categories-grid {
   display: grid;
   grid-template-columns: repeat(3, 1fr);
-  gap: 2px;
+  gap: 16px;
 }
 
 .category-card {
   position: relative;
-  aspect-ratio: 16/9;
+  aspect-ratio: 4/5;
   overflow: hidden;
   cursor: pointer;
+  border-radius: var(--radius-lg);
+  border: 1px solid var(--border);
 }
 
 .category-card img {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  filter: saturate(0.4) brightness(0.5);
-  transition: all 0.6s var(--ease);
+  filter: saturate(0.55) brightness(0.6);
+  transition: filter var(--dur-slow) var(--ease),
+              transform var(--dur-slow) var(--ease);
 }
 .category-card:hover img {
-  filter: saturate(0.8) brightness(0.7);
+  filter: saturate(0.9) brightness(0.8);
   transform: scale(1.05);
 }
 
@@ -862,28 +1111,33 @@ h1, h2, h3, h4, h5, h6 {
   flex-direction: column;
   justify-content: flex-end;
   padding: 32px;
+  background: linear-gradient(180deg, transparent 50%, rgba(12, 10, 9, 0.8) 100%);
 }
 
 .category-card-content span {
-  font-size: 0.6rem;
+  font-family: var(--font-sans);
+  font-size: 10px;
   font-weight: 700;
-  letter-spacing: 0.2em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--mm-blue);
-  margin-bottom: 8px;
+  color: var(--accent);
+  margin-bottom: 10px;
 }
 
 .category-card-content h3 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 72, 'SOFT' 30;
+  font-weight: 400;
   font-size: 2rem;
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 
 .category-card-content p {
-  font-size: 0.8rem;
-  color: var(--text-secondary);
-  margin-top: 4px;
+  font-size: 13px;
+  color: var(--ink-300);
+  margin-top: 8px;
+  line-height: 1.55;
 }
 
 /* ===== GALLERY GRID ===== */
@@ -941,8 +1195,8 @@ h1, h2, h3, h4, h5, h6 {
   position: relative;
   overflow: hidden;
   background:
-    radial-gradient(ellipse 800px 500px at 20% 30%, rgba(74,144,217,0.10), transparent 70%),
-    radial-gradient(ellipse 700px 500px at 90% 80%, rgba(212,100,122,0.10), transparent 70%),
+    radial-gradient(ellipse 900px 600px at 15% 30%, rgba(201, 151, 74, 0.08), transparent 65%),
+    radial-gradient(ellipse 700px 500px at 90% 80%, rgba(28, 24, 20, 0.5), transparent 70%),
     var(--bg-primary);
 }
 .academy-preview-glow {
@@ -950,9 +1204,9 @@ h1, h2, h3, h4, h5, h6 {
   inset: 0;
   pointer-events: none;
   background-image:
-    radial-gradient(circle at 20% 20%, rgba(255,255,255,0.04) 1px, transparent 1px);
-  background-size: 38px 38px;
-  opacity: 0.4;
+    radial-gradient(circle at 20% 20%, rgba(244, 236, 224, 0.03) 1px, transparent 1px);
+  background-size: 42px 42px;
+  opacity: 0.5;
   mask-image: radial-gradient(ellipse at center, black 30%, transparent 75%);
   -webkit-mask-image: radial-gradient(ellipse at center, black 30%, transparent 75%);
 }
@@ -987,15 +1241,15 @@ h1, h2, h3, h4, h5, h6 {
 .academy-visual-frame::before {
   content: '';
   position: absolute;
-  inset: -2px;
-  border-radius: 30px;
-  padding: 2px;
-  background: linear-gradient(135deg, var(--mm-blue), transparent 35%, transparent 65%, var(--mm-pink));
+  inset: -1px;
+  border-radius: 25px;
+  padding: 1px;
+  background: linear-gradient(135deg, var(--accent), transparent 40%, transparent 60%, var(--accent-deep));
   -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
   -webkit-mask-composite: xor;
           mask-composite: exclude;
   pointer-events: none;
-  opacity: 0.85;
+  opacity: 0.7;
   z-index: 2;
 }
 .academy-visual-frame:hover { transform: perspective(1200px) rotateY(-1deg) rotateX(1deg) translateY(-4px); }
@@ -1022,24 +1276,24 @@ h1, h2, h3, h4, h5, h6 {
   display: inline-flex;
   align-items: center;
   gap: 8px;
-  padding: 8px 14px;
-  border-radius: 999px;
-  font-size: 0.72rem;
+  padding: 9px 16px;
+  border-radius: var(--radius-pill);
+  font-size: 10.5px;
   font-weight: 700;
-  letter-spacing: 0.1em;
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
-  background: rgba(13,13,13,0.78);
-  backdrop-filter: blur(14px);
-  color: var(--white);
-  border: 1px solid rgba(255,255,255,0.12);
+  background: rgba(12, 10, 9, 0.78);
+  backdrop-filter: blur(16px);
+  color: var(--ink-100);
+  border: 1px solid var(--border-strong);
 }
 .academy-tag-top { top: 22px; left: 22px; }
 .academy-tag-dot {
-  width: 8px; height: 8px;
+  width: 7px; height: 7px;
   border-radius: 50%;
-  background: #3AAA3A;
-  box-shadow: 0 0 0 3px rgba(58,170,58,0.25);
-  animation: tag-blink 1.6s ease-in-out infinite;
+  background: var(--success);
+  box-shadow: 0 0 0 3px rgba(107, 147, 98, 0.2);
+  animation: tag-blink 1.8s ease-in-out infinite;
 }
 @keyframes tag-blink { 50% { opacity: 0.5; } }
 
@@ -1050,43 +1304,46 @@ h1, h2, h3, h4, h5, h6 {
   left: 18px;
   display: flex;
   align-items: center;
-  gap: 12px;
-  padding: 10px 14px;
-  border-radius: 14px;
-  background: rgba(13,13,13,0.86);
+  gap: 14px;
+  padding: 12px 16px;
+  border-radius: var(--radius-lg);
+  background: rgba(12, 10, 9, 0.86);
   backdrop-filter: blur(16px);
-  border: 1px solid rgba(255,255,255,0.1);
-  box-shadow: 0 12px 30px rgba(0,0,0,0.5);
+  border: 1px solid var(--border-light);
+  box-shadow: var(--shadow-3);
 }
 .academy-floating-card-icon {
-  width: 36px;
-  height: 36px;
-  border-radius: 10px;
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-pink));
-  color: #fff;
+  width: 38px;
+  height: 38px;
+  border-radius: var(--radius-md);
+  background: linear-gradient(135deg, var(--accent), var(--accent-deep));
+  color: #1a1208;
   display: inline-flex;
   align-items: center;
   justify-content: center;
 }
 .academy-floating-card-num {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.5rem;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.6rem;
+  font-weight: 500;
   line-height: 1;
-  letter-spacing: 0.04em;
-  color: #fff;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 .academy-floating-card-label {
-  font-size: 0.66rem;
+  font-size: 10px;
+  font-weight: 600;
   text-transform: uppercase;
-  letter-spacing: 0.12em;
-  color: var(--text-secondary);
-  margin-top: 2px;
+  letter-spacing: var(--ls-widest);
+  color: var(--ink-300);
+  margin-top: 4px;
 }
 
 .academy-floating-stats {
   display: flex;
-  gap: 12px;
-  margin-top: 24px;
+  gap: 10px;
+  margin-top: 28px;
   flex-wrap: wrap;
   justify-content: center;
 }
@@ -1094,113 +1351,117 @@ h1, h2, h3, h4, h5, h6 {
   display: inline-flex;
   align-items: center;
   gap: 8px;
-  padding: 10px 16px;
-  border-radius: 999px;
+  padding: 9px 14px;
+  border-radius: var(--radius-pill);
   background: var(--bg-card);
   border: 1px solid var(--border);
-  color: var(--text-secondary);
-  font-size: 0.78rem;
+  color: var(--ink-300);
+  font-size: 12px;
+  letter-spacing: 0.02em;
 }
-.academy-mini-stat strong { color: var(--white); font-weight: 700; }
-.academy-mini-stat svg:first-child { color: var(--warning); }
-.academy-mini-stat:nth-child(2) svg:first-child { color: var(--mm-blue-light); }
-.academy-mini-stat:nth-child(3) svg:first-child { color: var(--mm-pink-light); }
+.academy-mini-stat strong { color: var(--ink-100); font-weight: 700; }
+.academy-mini-stat svg:first-child { color: var(--accent); }
 
 /* Content side */
 .academy-preview-content .section-label {
-  margin-bottom: 18px;
-  display: inline-flex;
-  align-items: center;
-  gap: 12px;
+  margin-bottom: 22px;
 }
 .section-label-bar {
-  width: 32px;
-  height: 2px;
-  background: linear-gradient(90deg, var(--mm-blue), var(--mm-pink));
+  width: 28px;
+  height: 1px;
+  background: linear-gradient(90deg, var(--accent), transparent);
   display: inline-block;
 }
 
-.academy-title { font-size: clamp(2rem, 4vw, 3.2rem); margin-bottom: 20px; }
-
-.text-gradient {
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-pink));
-  -webkit-background-clip: text;
-          background-clip: text;
-  -webkit-text-fill-color: transparent;
-  color: transparent;
+.academy-title {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.2rem, 4.2vw, 3.6rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin-bottom: 24px;
+  color: var(--ink-100);
 }
 
 .academy-desc {
   max-width: 520px;
-  margin-bottom: 36px;
+  margin-bottom: 40px;
+  color: var(--ink-300);
+  font-size: 1.02rem;
+  line-height: 1.75;
 }
 
 .academy-features {
   display: grid;
   grid-template-columns: 1fr 1fr;
-  gap: 16px;
-  margin: 0 0 36px;
+  gap: 14px;
+  margin: 0 0 40px;
 }
 
 .academy-feature {
   display: flex;
   gap: 14px;
   align-items: flex-start;
-  padding: 16px;
-  border-radius: 14px;
+  padding: 18px;
+  border-radius: var(--radius-lg);
   background: var(--bg-card);
   border: 1px solid var(--border);
-  transition: all 0.3s var(--ease);
+  transition: border-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
 }
 .academy-feature:hover {
-  border-color: var(--mm-blue);
-  transform: translateY(-3px);
-  box-shadow: 0 16px 40px rgba(0,0,0,0.4);
+  border-color: var(--border-strong);
+  transform: translateY(-2px);
+  box-shadow: var(--shadow-3);
 }
 
 .academy-feature-icon {
-  width: 44px;
-  height: 44px;
-  border-radius: 12px;
+  width: 42px;
+  height: 42px;
+  border-radius: var(--radius-md);
   flex-shrink: 0;
   display: flex;
   align-items: center;
   justify-content: center;
-  background: linear-gradient(135deg, rgba(74,144,217,0.18), rgba(212,100,122,0.18));
-  color: var(--mm-blue-light);
-  border: 1px solid rgba(74,144,217,0.25);
+  background: rgba(201, 151, 74, 0.10);
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.25);
 }
-.academy-feature:nth-child(2) .academy-feature-icon { color: var(--mm-pink-light); border-color: rgba(212,100,122,0.25); }
-.academy-feature:nth-child(3) .academy-feature-icon { color: #F2A73B; border-color: rgba(242,167,59,0.25); }
-.academy-feature:nth-child(4) .academy-feature-icon { color: #3AAA3A; border-color: rgba(58,170,58,0.25); }
+.academy-feature:nth-child(n) .academy-feature-icon { color: var(--accent); border-color: rgba(201, 151, 74, 0.25); }
 
 .academy-feature-text h4 {
-  font-size: 0.95rem;
-  font-weight: 700;
-  margin-bottom: 4px;
-  color: var(--white);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 1.02rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin-bottom: 6px;
+  color: var(--ink-100);
 }
 .academy-feature-text p {
-  font-size: 0.82rem;
-  color: var(--text-secondary);
+  font-size: 13px;
+  color: var(--ink-300);
   line-height: 1.55;
 }
 
 .academy-cta-row {
   display: flex;
   align-items: center;
-  gap: 16px;
+  gap: 14px;
   flex-wrap: wrap;
-  margin-bottom: 24px;
+  margin-bottom: 28px;
 }
 .academy-ghost {
-  padding: 14px 22px;
-  border: 1px solid var(--border-light);
-  border-radius: 999px;
+  padding: 13px 22px;
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-md);
 }
 .academy-ghost:hover {
-  border-color: var(--mm-pink);
-  color: var(--mm-pink-light);
+  border-color: var(--ink-100);
+  color: var(--ink-100);
+  background: rgba(244, 236, 224, 0.04);
 }
 
 .academy-chips {
@@ -1209,20 +1470,21 @@ h1, h2, h3, h4, h5, h6 {
   gap: 8px;
 }
 .academy-chip {
-  font-size: 0.72rem;
+  font-size: 11px;
   font-weight: 600;
-  letter-spacing: 0.06em;
-  padding: 6px 12px;
-  border-radius: 999px;
-  background: var(--bg-card);
-  border: 1px dashed var(--border-light);
-  color: var(--text-muted);
-  transition: all 0.2s var(--ease);
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  padding: 7px 12px;
+  border-radius: var(--radius-pill);
+  background: transparent;
+  border: 1px solid var(--border);
+  color: var(--ink-400);
+  transition: color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease);
 }
 .academy-chip:hover {
-  color: var(--text-primary);
-  border-color: var(--mm-blue);
-  border-style: solid;
+  color: var(--accent-soft);
+  border-color: var(--accent);
 }
 
 @media (max-width: 900px) {
@@ -1251,27 +1513,31 @@ h1, h2, h3, h4, h5, h6 {
   display: block;
   aspect-ratio: 1 / 1;
   overflow: hidden;
-  border-radius: 14px;
+  border-radius: var(--radius-md);
   border: 1px solid var(--border);
   background: var(--bg-card);
-  transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1), border-color 0.3s ease, box-shadow 0.4s ease;
+  transition: transform var(--dur-base) var(--ease),
+              border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
 }
 
 .instagram-tile:hover {
-  transform: translateY(-4px);
-  border-color: rgba(228, 64, 95, 0.5);
-  box-shadow: 0 16px 40px -16px rgba(228, 64, 95, 0.4);
+  transform: translateY(-3px);
+  border-color: var(--accent);
+  box-shadow: 0 16px 40px -16px var(--accent-glow);
 }
 
 .instagram-tile img {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
+  transition: transform var(--dur-slow) var(--ease);
+  filter: saturate(0.9);
 }
 
 .instagram-tile:hover img {
-  transform: scale(1.1);
+  transform: scale(1.08);
+  filter: saturate(1);
 }
 
 .instagram-tile-overlay {
@@ -1280,10 +1546,10 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   align-items: center;
   justify-content: center;
-  background: linear-gradient(135deg, rgba(254, 175, 71, 0.85), rgba(228, 64, 95, 0.85) 45%, rgba(131, 58, 180, 0.85));
-  color: #fff;
+  background: linear-gradient(135deg, rgba(201, 151, 74, 0.55), rgba(143, 106, 48, 0.78));
+  color: var(--ink-100);
   opacity: 0;
-  transition: opacity 0.35s ease;
+  transition: opacity var(--dur-base) var(--ease);
 }
 
 .instagram-tile:hover .instagram-tile-overlay {
@@ -1319,7 +1585,7 @@ h1, h2, h3, h4, h5, h6 {
   right: -150px;
   width: 500px;
   height: 500px;
-  background: radial-gradient(circle, rgba(64, 156, 255, 0.06), transparent 70%);
+  background: radial-gradient(circle, rgba(201, 151, 74, 0.07), transparent 70%);
   pointer-events: none;
 }
 
@@ -1334,11 +1600,13 @@ h1, h2, h3, h4, h5, h6 {
 .project-card {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 20px;
+  border-radius: var(--radius-lg);
   overflow: hidden;
   display: flex;
   flex-direction: column;
-  transition: transform 0.45s cubic-bezier(0.16, 1, 0.3, 1), border-color 0.3s ease, box-shadow 0.45s ease;
+  transition: transform var(--dur-base) var(--ease),
+              border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
   position: relative;
 }
 
@@ -1346,62 +1614,64 @@ h1, h2, h3, h4, h5, h6 {
   content: '';
   position: absolute;
   inset: 0;
-  border-radius: 20px;
-  background: linear-gradient(135deg, transparent 60%, rgba(64,156,255,0.08));
+  border-radius: var(--radius-lg);
+  background: linear-gradient(135deg, transparent 60%, rgba(201, 151, 74, 0.08));
   opacity: 0;
-  transition: opacity 0.4s ease;
+  transition: opacity var(--dur-base) var(--ease);
   pointer-events: none;
 }
 
 .project-card:hover {
-  transform: translateY(-8px);
-  border-color: rgba(64,156,255,0.5);
-  box-shadow: 0 28px 60px -20px rgba(0,0,0,0.55);
+  transform: translateY(-5px);
+  border-color: var(--border-strong);
+  box-shadow: var(--shadow-4);
 }
 
 .project-card:hover::after { opacity: 1; }
 
 .project-card-image {
   position: relative;
-  aspect-ratio: 16 / 11;
+  aspect-ratio: 4 / 3;
   overflow: hidden;
-  background: var(--bg-secondary);
+  background: var(--bg-tertiary);
 }
 
 .project-card-image img {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  transition: transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
+  transition: transform var(--dur-slow) var(--ease);
+  filter: saturate(0.9);
 }
 
 .project-card:hover .project-card-image img {
   transform: scale(1.06);
+  filter: saturate(1);
 }
 
 .project-card-image::after {
   content: '';
   position: absolute;
   inset: 0;
-  background: linear-gradient(180deg, transparent 50%, rgba(10,9,8,0.45));
+  background: linear-gradient(180deg, transparent 55%, rgba(12, 10, 9, 0.65));
   pointer-events: none;
 }
 
 .project-card-tag {
   position: absolute;
-  top: 16px;
-  left: 16px;
+  top: 18px;
+  left: 18px;
   z-index: 2;
-  background: rgba(10,9,8,0.82);
-  color: #fff;
+  background: rgba(12, 10, 9, 0.82);
+  color: var(--accent-soft);
   padding: 7px 14px;
-  border-radius: 999px;
-  font-size: 0.68rem;
-  letter-spacing: 0.12em;
+  border-radius: var(--radius-pill);
+  font-size: 10px;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
   font-weight: 700;
   backdrop-filter: blur(8px);
-  border: 1px solid rgba(255,255,255,0.08);
+  border: 1px solid var(--border-strong);
 }
 
 .project-card-body {
@@ -1416,26 +1686,28 @@ h1, h2, h3, h4, h5, h6 {
   display: inline-flex;
   align-items: center;
   gap: 6px;
-  font-size: 0.72rem;
-  color: var(--mm-blue);
+  font-size: 10.5px;
+  color: var(--accent);
   text-transform: uppercase;
-  letter-spacing: 0.14em;
+  letter-spacing: var(--ls-widest);
   font-weight: 700;
 }
 
 .project-card-title {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.9rem;
-  line-height: 1.05;
-  letter-spacing: 0.015em;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 72, 'SOFT' 30;
+  font-weight: 500;
+  font-size: 1.7rem;
+  line-height: 1.1;
+  letter-spacing: var(--ls-tight);
   margin: 0;
-  color: var(--text-primary);
+  color: var(--ink-100);
 }
 
 .project-card-summary {
-  font-size: 0.94rem;
+  font-size: 0.95rem;
   line-height: 1.7;
-  color: var(--text-secondary);
+  color: var(--ink-300);
   margin: 0;
 }
 
@@ -1452,7 +1724,9 @@ h1, h2, h3, h4, h5, h6 {
 /* ===== TESTIMONIALS ===== */
 .testimonials-section {
   padding: var(--section-gap) 0;
-  background: var(--bg-secondary);
+  background:
+    radial-gradient(ellipse 800px 500px at 20% 0%, rgba(201, 151, 74, 0.05), transparent 60%),
+    linear-gradient(180deg, transparent, var(--bg-secondary) 50%, transparent);
   border-top: 1px solid var(--border);
   border-bottom: 1px solid var(--border);
 }
@@ -1460,67 +1734,103 @@ h1, h2, h3, h4, h5, h6 {
 .testimonials-grid {
   display: grid;
   grid-template-columns: repeat(3, 1fr);
-  gap: 2px;
+  gap: 20px;
 }
 
 .testimonial-card {
+  position: relative;
   background: var(--bg-card);
-  padding: 40px 32px;
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
+  padding: 44px 32px 32px;
   display: flex;
   flex-direction: column;
+  transition: border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease);
+}
+.testimonial-card::before {
+  content: '\201C';
+  position: absolute;
+  top: -20px;
+  left: 20px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 100;
+  font-style: italic;
+  font-size: 6rem;
+  line-height: 1;
+  color: var(--accent);
+  opacity: 0.7;
+}
+.testimonial-card:hover {
+  border-color: var(--border-strong);
+  transform: translateY(-3px);
+  box-shadow: var(--shadow-3);
 }
 
 .testimonial-stars {
   display: flex;
   gap: 4px;
   margin-bottom: 20px;
-  color: var(--mm-blue);
+  color: var(--accent);
 }
 
 .testimonial-text {
-  font-size: 0.9rem;
-  color: var(--text-secondary);
-  line-height: 1.8;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 100;
+  font-style: italic;
+  font-weight: 400;
+  font-size: 1.06rem;
+  color: var(--ink-200);
+  line-height: 1.65;
   flex: 1;
   margin-bottom: 28px;
-  font-style: italic;
+  text-wrap: balance;
 }
 
 .testimonial-author {
   display: flex;
   align-items: center;
   gap: 14px;
-  padding-top: 20px;
+  padding-top: 22px;
   border-top: 1px solid var(--border);
 }
 
 .testimonial-avatar {
-  width: 40px;
-  height: 40px;
-  background: var(--bg-elevated);
+  width: 42px;
+  height: 42px;
+  border-radius: 50%;
+  background: linear-gradient(135deg, var(--accent-deep), var(--accent));
   display: flex;
   align-items: center;
   justify-content: center;
-  font-weight: 800;
-  font-size: 0.75rem;
-  color: var(--mm-blue);
+  font-family: var(--font-sans);
+  font-weight: 700;
+  font-size: 12px;
+  letter-spacing: var(--ls-wide);
+  color: #1a1208;
 }
 
 .testimonial-author-info .name {
-  font-size: 0.8rem;
+  font-family: var(--font-sans);
+  font-size: 13px;
   font-weight: 700;
+  color: var(--ink-100);
+  letter-spacing: 0.01em;
 }
 
 .testimonial-author-info .role {
-  font-size: 0.65rem;
-  color: var(--text-muted);
-  letter-spacing: 0.08em;
+  font-size: 10px;
+  color: var(--ink-400);
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
+  font-weight: 600;
+  margin-top: 4px;
 }
 
 /* ===== NEWSLETTER CTA ===== */
 .newsletter-section {
-  padding: 70px 0;
+  padding: var(--section-gap) 0;
   position: relative;
   overflow: hidden;
 }
@@ -1528,56 +1838,78 @@ h1, h2, h3, h4, h5, h6 {
 .newsletter-bg {
   position: absolute;
   inset: 0;
-  background: linear-gradient(135deg, var(--mm-blue) 0%, var(--mm-pink) 100%);
-  opacity: 0.08;
+  background:
+    radial-gradient(ellipse 800px 400px at 50% 50%, var(--accent-glow), transparent 65%);
+  opacity: 0.5;
 }
 
 .newsletter-content {
   position: relative;
   text-align: center;
-  max-width: 600px;
+  max-width: 640px;
   margin: 0 auto;
 }
 
 .newsletter-content h2 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 2.5rem;
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 16px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.2rem, 4vw, 3.2rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.06;
+  margin-bottom: 18px;
+  color: var(--ink-100);
 }
 
 .newsletter-content p {
-  color: var(--text-secondary);
-  margin-bottom: 36px;
-  font-size: 0.95rem;
+  color: var(--ink-300);
+  margin-bottom: 40px;
+  font-size: 1.04rem;
+  line-height: 1.7;
 }
 
 .newsletter-content .hero-btns { justify-content: center; }
 
 /* ===== STORE PAGE ===== */
 .store-page {
-  padding: 0 0 80px;
+  padding: 0 0 var(--section-gap);
 }
 
 .store-hero {
-  padding: 60px 32px 48px;
+  padding: clamp(60px, 8vw, 96px) 32px clamp(48px, 6vw, 72px);
   text-align: center;
   border-bottom: 1px solid var(--border);
-  margin-bottom: 48px;
+  margin-bottom: clamp(40px, 5vw, 56px);
+  position: relative;
+}
+.store-hero::after {
+  content: '';
+  position: absolute;
+  left: 50%;
+  bottom: -1px;
+  width: 60px;
+  height: 1px;
+  background: var(--accent);
+  transform: translateX(-50%);
 }
 
 .store-hero h1 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2.5rem, 5vw, 4rem);
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 8px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.6rem, 5.4vw, 4.4rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin-bottom: 16px;
+  color: var(--ink-100);
 }
 
 .store-hero p {
-  color: var(--text-secondary);
-  font-size: 0.95rem;
+  color: var(--ink-300);
+  font-size: 1.04rem;
+  line-height: 1.7;
+  max-width: 580px;
+  margin: 0 auto;
 }
 
 .store-container {
@@ -1589,33 +1921,41 @@ h1, h2, h3, h4, h5, h6 {
 .filter-bar {
   display: flex;
   align-items: center;
-  gap: 12px;
-  margin-bottom: 40px;
-  color: var(--text-muted);
+  gap: 10px;
+  margin-bottom: 48px;
+  padding: 14px 18px;
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
+  color: var(--ink-400);
 }
 
 .filter-btn {
-  padding: 10px 24px;
-  font-size: 0.7rem;
-  font-weight: 700;
-  letter-spacing: 0.12em;
+  padding: 9px 18px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
-  color: var(--text-muted);
-  border: 1px solid var(--border);
-  transition: all 0.3s;
+  color: var(--ink-300);
+  border: 1px solid transparent;
+  border-radius: var(--radius-md);
+  transition: color var(--dur-fast) var(--ease),
+              background-color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease);
   background: transparent;
 }
-.filter-btn:hover { border-color: var(--white); color: var(--white); }
+.filter-btn:hover { color: var(--ink-100); background: var(--bg-hover); }
 .filter-btn.active {
-  background: var(--white);
-  color: var(--bg-primary);
-  border-color: var(--white);
+  background: var(--accent);
+  color: #1a1208;
+  border-color: var(--accent);
 }
 
 .products-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
-  gap: 2px;
+  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
+  gap: 20px;
 }
 
 .product-card {
@@ -1623,33 +1963,36 @@ h1, h2, h3, h4, h5, h6 {
   backdrop-filter: blur(16px);
   -webkit-backdrop-filter: blur(16px);
   border: 1px solid var(--border);
-  border-radius: 12px;
-  transition: all 0.4s var(--ease);
+  border-radius: var(--radius-lg);
+  transition: border-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
   position: relative;
   overflow: hidden;
 }
 .product-card:hover {
-  background: var(--bg-hover);
-  border-color: var(--border-light);
-  box-shadow: 0 12px 30px rgba(0,0,0,0.4);
-  transform: translateY(-4px);
+  border-color: var(--border-strong);
+  box-shadow: var(--shadow-3);
+  transform: translateY(-3px);
 }
 
 .product-image {
   position: relative;
-  aspect-ratio: 1;
+  aspect-ratio: 4 / 5;
   overflow: hidden;
+  background: var(--bg-tertiary);
 }
 
 .product-image img {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  filter: saturate(0.8);
-  transition: all 0.5s var(--ease);
+  filter: saturate(0.92);
+  transition: transform var(--dur-slow) var(--ease),
+              filter var(--dur-slow) var(--ease);
 }
 .product-card:hover .product-image img {
-  transform: scale(1.05);
+  transform: scale(1.06);
   filter: saturate(1);
 }
 
@@ -1657,14 +2000,17 @@ h1, h2, h3, h4, h5, h6 {
   position: absolute;
   top: 16px;
   left: 16px;
-  padding: 6px 14px;
-  font-size: 0.55rem;
+  padding: 6px 12px;
+  font-family: var(--font-sans);
+  font-size: 9.5px;
   font-weight: 700;
-  letter-spacing: 0.15em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  background: var(--bg-primary);
-  color: var(--text-secondary);
-  border: 1px solid var(--border);
+  background: rgba(12, 10, 9, 0.82);
+  color: var(--accent-soft);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-pill);
+  backdrop-filter: blur(8px);
 }
 
 .product-info {
@@ -1679,10 +2025,11 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .product-weight {
-  font-size: 0.7rem;
-  color: var(--text-muted);
-  letter-spacing: 0.06em;
+  font-size: 11px;
+  color: var(--ink-400);
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
+  font-weight: 500;
   display: inline-block;
   margin-right: 10px;
   vertical-align: middle;
@@ -1698,55 +2045,81 @@ h1, h2, h3, h4, h5, h6 {
   align-items: center;
   justify-content: space-between;
   margin-top: 20px;
+  padding-top: 16px;
+  border-top: 1px solid var(--border);
 }
 
 .product-price {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-weight: 500;
   font-size: 1.5rem;
-  letter-spacing: 0.04em;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 
 .add-to-cart-btn {
   display: flex;
   align-items: center;
   gap: 8px;
-  padding: 10px 20px;
-  font-size: 0.65rem;
-  font-weight: 700;
-  letter-spacing: 0.12em;
+  padding: 10px 18px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
-  background: var(--white);
-  color: var(--bg-primary);
-  transition: all 0.3s;
+  background: var(--accent);
+  color: #1a1208;
+  border-radius: var(--radius-md);
+  box-shadow: var(--shadow-accent);
+  transition: background-color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease);
 }
 .add-to-cart-btn:hover {
-  background: var(--mm-blue);
-  color: var(--white);
+  background: var(--accent-soft);
+  transform: translateY(-1px);
 }
 
 /* ===== ACADEMY PAGE ===== */
 .academy-page {
-  padding: 0 0 80px;
+  padding: 0 0 var(--section-gap);
 }
 
 .academy-hero {
-  padding: 60px 32px 48px;
+  padding: clamp(60px, 8vw, 96px) 32px clamp(48px, 6vw, 72px);
   text-align: center;
   border-bottom: 1px solid var(--border);
-  margin-bottom: 48px;
+  margin-bottom: clamp(40px, 5vw, 56px);
+  position: relative;
+}
+.academy-hero::after {
+  content: '';
+  position: absolute;
+  left: 50%;
+  bottom: -1px;
+  width: 60px;
+  height: 1px;
+  background: var(--accent);
+  transform: translateX(-50%);
 }
 
 .academy-hero h1 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2.5rem, 5vw, 4rem);
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 8px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.6rem, 5.4vw, 4.4rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin-bottom: 16px;
+  color: var(--ink-100);
 }
 
 .academy-hero p {
-  color: var(--text-secondary);
-  font-size: 0.95rem;
+  color: var(--ink-300);
+  font-size: 1.04rem;
+  line-height: 1.7;
+  max-width: 580px;
+  margin: 0 auto;
 }
 
 .academy-container {
@@ -1757,29 +2130,39 @@ h1, h2, h3, h4, h5, h6 {
 
 .courses-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(360px, 1fr));
-  gap: 2px;
+  grid-template-columns: repeat(auto-fill, minmax(340px, 1fr));
+  gap: 20px;
 }
 
 .course-card {
   background: var(--bg-card);
-  transition: all 0.4s var(--ease);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
+  transition: border-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
   overflow: hidden;
 }
-.course-card:hover { background: var(--bg-hover); }
+.course-card:hover {
+  border-color: var(--border-strong);
+  transform: translateY(-3px);
+  box-shadow: var(--shadow-3);
+}
 
 .course-image {
   position: relative;
   aspect-ratio: 16/9;
   overflow: hidden;
+  background: var(--bg-tertiary);
 }
 
 .course-image img {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  filter: saturate(0.7);
-  transition: all 0.5s var(--ease);
+  filter: saturate(0.85);
+  transition: transform var(--dur-slow) var(--ease),
+              filter var(--dur-slow) var(--ease);
 }
 .course-card:hover .course-image img {
   transform: scale(1.05);
@@ -1790,30 +2173,44 @@ h1, h2, h3, h4, h5, h6 {
   position: absolute;
   top: 16px;
   right: 16px;
-  padding: 8px 16px;
-  font-size: 0.7rem;
-  font-weight: 800;
-  letter-spacing: 0.08em;
-  background: var(--bg-primary);
-  border: 1px solid var(--border);
+  padding: 7px 14px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 14px;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  background: rgba(12, 10, 9, 0.82);
+  color: var(--ink-100);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-md);
+  backdrop-filter: blur(8px);
 }
 .course-badge.free {
-  background: var(--success);
-  color: var(--white);
-  border: none;
+  background: var(--accent);
+  color: #1a1208;
+  border-color: var(--accent);
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
 }
 
 .course-level {
   position: absolute;
   bottom: 16px;
   left: 16px;
-  padding: 6px 14px;
-  font-size: 0.55rem;
+  padding: 7px 12px;
+  font-family: var(--font-sans);
+  font-size: 10px;
   font-weight: 700;
-  letter-spacing: 0.15em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  background: rgba(13, 13, 13, 0.85);
-  backdrop-filter: blur(10px);
+  color: var(--accent-soft);
+  background: rgba(12, 10, 9, 0.82);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-pill);
+  backdrop-filter: blur(8px);
 }
 
 .course-info {
@@ -1821,21 +2218,25 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .course-info h3 {
-  font-size: 1rem;
-  font-weight: 700;
-  margin-bottom: 10px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.4rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin-bottom: 12px;
+  color: var(--ink-100);
 }
 
 .course-info > p {
-  color: var(--text-secondary);
-  font-size: 0.85rem;
-  margin-bottom: 20px;
+  color: var(--ink-300);
+  font-size: 0.94rem;
+  margin-bottom: 22px;
   line-height: 1.7;
 }
 
 .course-meta {
   display: flex;
-  gap: 20px;
+  gap: 18px;
   margin-bottom: 24px;
   padding-bottom: 20px;
   border-bottom: 1px solid var(--border);
@@ -1845,12 +2246,14 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   align-items: center;
   gap: 6px;
-  font-size: 0.7rem;
+  font-family: var(--font-sans);
+  font-size: 11px;
   font-weight: 600;
-  color: var(--text-muted);
-  letter-spacing: 0.04em;
+  color: var(--ink-400);
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
 }
+.course-meta span svg { color: var(--accent); }
 
 .course-info .btn { width: 100%; }
 
@@ -1885,11 +2288,9 @@ h1, h2, h3, h4, h5, h6 {
 .auth-visual-overlay {
   position: absolute;
   inset: 0;
-  background: linear-gradient(
-    135deg,
-    rgba(13, 13, 13, 0.92) 0%,
-    rgba(74, 144, 217, 0.3) 100%
-  );
+  background:
+    radial-gradient(ellipse 70% 60% at 0% 50%, rgba(201, 151, 74, 0.15), transparent 60%),
+    linear-gradient(135deg, rgba(12, 10, 9, 0.92) 0%, rgba(28, 24, 20, 0.85) 100%);
   backdrop-filter: blur(2px);
 }
 
@@ -1897,31 +2298,33 @@ h1, h2, h3, h4, h5, h6 {
   position: relative;
   z-index: 1;
   padding: 60px 48px;
-  max-width: 480px;
+  max-width: 520px;
 }
 
 .auth-visual-content svg {
-  color: var(--mm-blue);
-  margin-bottom: 24px;
+  color: var(--accent);
+  margin-bottom: 28px;
 }
 
 .auth-visual-content h2 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 2.8rem;
-  letter-spacing: 0.04em;
-  text-transform: uppercase;
-  line-height: 1.1;
-  margin-bottom: 16px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.4rem, 4vw, 3.2rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.06;
+  margin-bottom: 20px;
+  color: var(--ink-100);
 }
 
 .auth-visual-content p {
-  color: var(--text-secondary);
-  font-size: 0.95rem;
+  color: var(--ink-300);
+  font-size: 1.04rem;
   line-height: 1.7;
 }
 
 .auth-visual-features {
-  margin-top: 36px;
+  margin-top: 40px;
   display: flex;
   flex-direction: column;
   gap: 14px;
@@ -1930,13 +2333,13 @@ h1, h2, h3, h4, h5, h6 {
 .auth-visual-feature {
   display: flex;
   align-items: center;
-  gap: 12px;
-  color: var(--text-secondary);
-  font-size: 0.88rem;
+  gap: 14px;
+  color: var(--ink-200);
+  font-size: 0.94rem;
 }
 
 .auth-visual-feature svg {
-  color: var(--success);
+  color: var(--accent);
   margin-bottom: 0;
   flex-shrink: 0;
 }
@@ -1948,6 +2351,7 @@ h1, h2, h3, h4, h5, h6 {
   justify-content: center;
   padding: 48px 32px;
   background: var(--bg-secondary);
+  border-left: 1px solid var(--border);
 }
 
 .auth-form-container {
@@ -1957,94 +2361,102 @@ h1, h2, h3, h4, h5, h6 {
 
 .auth-logo {
   display: inline-block;
-  margin-bottom: 32px;
+  margin-bottom: 36px;
 }
 
 .auth-logo img {
-  height: 48px;
+  height: 44px;
   width: auto;
 }
 
 .auth-form-header {
-  margin-bottom: 36px;
+  margin-bottom: 40px;
 }
 
 .auth-form-header h2 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 2.2rem;
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 6px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-weight: 400;
+  font-size: 2.6rem;
+  letter-spacing: var(--ls-tight);
+  line-height: 1.06;
+  margin-bottom: 10px;
+  color: var(--ink-100);
 }
 
 .auth-form-header p {
-  color: var(--text-muted);
-  font-size: 0.88rem;
+  color: var(--ink-300);
+  font-size: 0.96rem;
+  line-height: 1.55;
 }
 
 /* Form inputs */
 .auth-form {
   display: flex;
   flex-direction: column;
-  gap: 20px;
+  gap: 22px;
 }
 
 .auth-name-row {
   display: grid;
   grid-template-columns: 1fr 1fr;
-  gap: 12px;
+  gap: 14px;
 }
 
 .auth-input-group {
   display: flex;
   flex-direction: column;
-  gap: 6px;
+  gap: 8px;
 }
 
 .auth-input-group label {
-  font-size: 0.72rem;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
   font-weight: 700;
-  letter-spacing: 0.12em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-secondary);
+  color: var(--ink-300);
 }
 
 .input-group {
   display: flex;
   align-items: center;
-  gap: 14px;
+  gap: 12px;
   padding: 14px 18px;
   background: var(--bg-surface);
   border: 1px solid var(--border);
-  transition: border-color 0.3s, box-shadow 0.3s;
+  border-radius: var(--radius-md);
+  transition: border-color var(--dur-fast) var(--ease),
+              box-shadow var(--dur-fast) var(--ease);
 }
 .input-group:focus-within {
-  border-color: var(--mm-blue);
-  box-shadow: 0 0 0 3px rgba(74, 144, 217, 0.1);
+  border-color: var(--accent);
+  box-shadow: 0 0 0 3px var(--accent-glow);
 }
 
 .input-group.input-error {
   border-color: var(--error);
-  box-shadow: 0 0 0 3px rgba(234, 51, 53, 0.1);
+  box-shadow: 0 0 0 3px rgba(194, 92, 74, 0.12);
 }
 
 .input-group.input-success {
   border-color: var(--success);
-  box-shadow: 0 0 0 3px rgba(58, 170, 58, 0.1);
+  box-shadow: 0 0 0 3px rgba(107, 147, 98, 0.12);
 }
 
-.input-group svg { color: var(--text-muted); flex-shrink: 0; }
+.input-group svg { color: var(--ink-400); flex-shrink: 0; }
+.input-group:focus-within svg { color: var(--accent); }
 
 .input-group input {
   flex: 1;
   background: none;
   border: none;
   outline: none;
-  color: var(--text-primary);
-  font-size: 0.9rem;
-  font-family: 'Montserrat', sans-serif;
+  color: var(--ink-100);
+  font-size: 0.95rem;
+  font-family: var(--font-sans);
 }
-.input-group input::placeholder { color: var(--text-muted); }
+.input-group input::placeholder { color: var(--ink-400); }
 
 /* Password toggle */
 .password-toggle {
@@ -2063,29 +2475,32 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   align-items: center;
   gap: 12px;
-  margin-top: 8px;
+  margin-top: 10px;
 }
 
 .strength-bar {
   flex: 1;
   height: 3px;
   background: var(--border);
-  border-radius: 3px;
+  border-radius: var(--radius-pill);
   overflow: hidden;
 }
 
 .strength-fill {
   height: 100%;
-  border-radius: 3px;
-  transition: width 0.4s var(--ease), background 0.4s;
+  background: linear-gradient(90deg, var(--accent-deep), var(--accent));
+  border-radius: var(--radius-pill);
+  transition: width var(--dur-base) var(--ease);
 }
 
 .strength-label {
-  font-size: 0.7rem;
+  font-family: var(--font-sans);
+  font-size: 10px;
   font-weight: 700;
-  letter-spacing: 0.05em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
   white-space: nowrap;
+  color: var(--ink-300);
 }
 
 /* Password requirements */
@@ -2204,34 +2619,41 @@ h1, h2, h3, h4, h5, h6 {
   justify-content: center;
   gap: 12px;
   width: 100%;
-  padding: 14px;
-  background: var(--white);
-  color: var(--bg-primary);
-  font-weight: 700;
-  font-size: 0.8rem;
-  letter-spacing: 0.04em;
-  transition: all 0.3s;
+  padding: 13px;
+  background: var(--ink-100);
+  color: #1a1208;
+  font-family: var(--font-sans);
+  font-weight: 600;
+  font-size: 12.5px;
+  letter-spacing: var(--ls-wide);
+  border-radius: var(--radius-md);
+  box-shadow: var(--shadow-2);
+  transition: background-color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease),
+              box-shadow var(--dur-fast) var(--ease);
 }
 .google-btn:hover {
-  opacity: 0.9;
+  background: #FBF5EB;
   transform: translateY(-1px);
+  box-shadow: var(--shadow-3);
 }
 
 /* Toggle auth link */
 .toggle-auth {
   text-align: center;
-  margin-top: 28px;
-  color: var(--text-muted);
-  font-size: 0.85rem;
+  margin-top: 32px;
+  color: var(--ink-400);
+  font-size: 0.92rem;
 }
 .toggle-auth a,
 .toggle-auth button {
-  color: var(--mm-blue);
-  font-weight: 700;
+  color: var(--accent);
+  font-weight: 600;
   margin-left: 4px;
+  position: relative;
 }
 .toggle-auth a:hover,
-.toggle-auth button:hover { text-decoration: underline; }
+.toggle-auth button:hover { color: var(--accent-soft); }
 
 /* ===== ADMIN PANEL ===== */
 .admin-page {
@@ -3013,19 +3435,20 @@ h1, h2, h3, h4, h5, h6 {
 
 /* Admin link in navbar */
 .admin-link {
-  width: 36px;
-  height: 36px;
+  width: 38px;
+  height: 38px;
   display: flex;
   align-items: center;
   justify-content: center;
-  color: var(--mm-blue);
-  border: 1px solid rgba(74, 144, 217, 0.3);
-  border-radius: 6px;
-  transition: all 0.2s;
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.32);
+  border-radius: var(--radius-md);
+  transition: background-color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease);
 }
 .admin-link:hover {
-  background: rgba(74, 144, 217, 0.1);
-  border-color: var(--mm-blue);
+  background: rgba(201, 151, 74, 0.10);
+  border-color: var(--accent);
 }
 
 /* ===== ADMIN MODAL ===== */
@@ -3174,29 +3597,36 @@ h1, h2, h3, h4, h5, h6 {
   display: inline-flex;
   align-items: center;
   gap: 6px;
-  font-size: 0.82rem;
-  font-weight: 600;
-  color: var(--text-secondary);
-  margin-bottom: 24px;
-  transition: color 0.2s;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--ink-400);
+  margin-bottom: 28px;
+  transition: color var(--dur-fast) var(--ease);
 }
-.checkout-back:hover { color: var(--mm-blue); }
+.checkout-back:hover { color: var(--accent); }
 
 .checkout-header {
-  margin-bottom: 40px;
+  margin-bottom: 48px;
 }
 
 .checkout-header h1 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2rem, 4vw, 3rem);
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 4px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.2rem, 4.4vw, 3.4rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin-bottom: 8px;
+  color: var(--ink-100);
 }
 
 .checkout-header p {
-  color: var(--text-muted);
-  font-size: 0.9rem;
+  color: var(--ink-300);
+  font-size: 0.98rem;
+  line-height: 1.55;
 }
 
 .checkout-grid {
@@ -3215,12 +3645,15 @@ h1, h2, h3, h4, h5, h6 {
   border-bottom: 1px solid var(--border);
 }
 
-.checkout-section-header svg { color: var(--mm-blue); }
+.checkout-section-header svg { color: var(--accent); }
 
 .checkout-section-header h2 {
-  font-size: 1.1rem;
-  font-weight: 700;
-  letter-spacing: 0.02em;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.25rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 
 .checkout-form {
@@ -3246,20 +3679,23 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .checkout-field label {
-  font-size: 0.72rem;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
   font-weight: 700;
-  letter-spacing: 0.12em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-secondary);
+  color: var(--ink-300);
 }
 
 /* Order summary sidebar */
 .checkout-summary {
   background: var(--bg-card);
   border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
   padding: 28px;
   position: sticky;
   top: calc(var(--nav-height) + 24px);
+  backdrop-filter: blur(18px);
 }
 
 .checkout-items {
@@ -3278,10 +3714,11 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .checkout-item img {
-  width: 56px;
-  height: 56px;
+  width: 60px;
+  height: 60px;
   object-fit: cover;
-  border: 1px solid var(--border);
+  border: 1px solid var(--border-light);
+  border-radius: var(--radius-md);
 }
 
 .checkout-item-info {
@@ -3290,37 +3727,50 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .checkout-item-info h4 {
-  font-size: 0.82rem;
-  font-weight: 600;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 0.95rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
   white-space: nowrap;
   overflow: hidden;
   text-overflow: ellipsis;
+  color: var(--ink-100);
 }
 
 .checkout-item-qty {
-  font-size: 0.72rem;
-  color: var(--text-muted);
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--ink-400);
+  margin-top: 4px;
 }
 
 .checkout-item-price {
-  font-size: 0.88rem;
-  font-weight: 700;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 0.95rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
   white-space: nowrap;
+  color: var(--ink-100);
 }
 
 .checkout-totals {
   border-top: 1px solid var(--border);
-  padding-top: 20px;
-  margin-bottom: 24px;
+  padding-top: 22px;
+  margin-bottom: 26px;
 }
 
 .checkout-total-row {
   display: flex;
   justify-content: space-between;
   align-items: center;
-  padding: 8px 0;
-  font-size: 0.88rem;
-  color: var(--text-secondary);
+  padding: 9px 0;
+  font-size: 0.92rem;
+  color: var(--ink-300);
 }
 
 .checkout-total-row span {
@@ -3332,40 +3782,52 @@ h1, h2, h3, h4, h5, h6 {
 .free-shipping {
   color: var(--success);
   font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  font-size: 11px;
 }
 
 .shipping-hint {
-  font-size: 0.72rem;
-  color: var(--text-muted);
-  margin-top: 2px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  color: var(--ink-400);
+  margin-top: 4px;
   margin-bottom: 4px;
+  line-height: 1.55;
 }
 
 .checkout-grand-total {
   border-top: 1px solid var(--border);
-  margin-top: 8px;
-  padding-top: 16px;
-  font-size: 1.1rem;
-  font-weight: 700;
-  color: var(--text-primary);
+  margin-top: 10px;
+  padding-top: 18px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.3rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 
 .checkout-pay-btn {
   padding: 18px 36px;
-  font-size: 0.88rem;
+  font-size: 12px;
 }
 
 .checkout-secure {
   display: flex;
   align-items: center;
   justify-content: center;
-  gap: 6px;
-  margin-top: 16px;
-  font-size: 0.72rem;
-  color: var(--text-muted);
+  gap: 8px;
+  margin-top: 18px;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
+  text-transform: uppercase;
+  color: var(--ink-400);
 }
 
-.checkout-secure svg { color: var(--success); }
+.checkout-secure svg { color: var(--accent); }
 
 /* ===== ORDER CONFIRMATION ===== */
 .order-confirmation {
@@ -3391,42 +3853,57 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .confirmation-card h1 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 2.4rem;
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 12px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.4rem, 4vw, 3.2rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin-bottom: 16px;
+  color: var(--ink-100);
 }
 
 .confirmation-desc {
-  color: var(--text-secondary);
-  font-size: 0.95rem;
-  margin-bottom: 32px;
+  color: var(--ink-300);
+  font-size: 1rem;
+  line-height: 1.65;
+  margin-bottom: 36px;
 }
 
+.confirmation-icon svg { color: var(--accent); }
+
 .confirmation-details {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  padding: 24px;
-  margin-bottom: 24px;
+  border-radius: var(--radius-lg);
+  padding: 26px;
+  margin-bottom: 26px;
   display: flex;
   flex-direction: column;
-  gap: 12px;
+  gap: 14px;
+  text-align: left;
 }
 
 .confirmation-detail {
   display: flex;
   justify-content: space-between;
   align-items: center;
-  font-size: 0.88rem;
+  font-size: 0.92rem;
+  padding: 6px 0;
 }
 
 .confirmation-detail span {
-  color: var(--text-muted);
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 700;
+  letter-spacing: var(--ls-widest);
+  text-transform: uppercase;
+  color: var(--ink-400);
 }
 
 .confirmation-detail strong {
-  color: var(--text-primary);
+  color: var(--ink-100);
+  font-weight: 600;
 }
 
 .confirmation-info {
@@ -3435,15 +3912,17 @@ h1, h2, h3, h4, h5, h6 {
   justify-content: center;
   gap: 10px;
   padding: 16px 24px;
-  background: rgba(74, 144, 217, 0.08);
-  border: 1px solid rgba(74, 144, 217, 0.2);
+  background: rgba(201, 151, 74, 0.08);
+  border: 1px solid rgba(201, 151, 74, 0.22);
+  border-radius: var(--radius-lg);
   margin-bottom: 32px;
-  font-size: 0.85rem;
-  color: var(--text-secondary);
+  font-size: 0.9rem;
+  color: var(--ink-200);
+  line-height: 1.55;
 }
 
 .confirmation-info svg {
-  color: var(--mm-blue);
+  color: var(--accent);
   flex-shrink: 0;
 }
 
@@ -3458,7 +3937,9 @@ h1, h2, h3, h4, h5, h6 {
   position: fixed;
   inset: 0;
   z-index: 200;
-  background: rgba(0, 0, 0, 0.7);
+  background: rgba(0, 0, 0, 0.6);
+  backdrop-filter: blur(6px);
+  -webkit-backdrop-filter: blur(6px);
 }
 
 .cart-drawer {
@@ -3466,13 +3947,14 @@ h1, h2, h3, h4, h5, h6 {
   right: 0;
   top: 0;
   bottom: 0;
-  width: 420px;
+  width: 440px;
   max-width: 100vw;
   background: var(--bg-secondary);
   border-left: 1px solid var(--border);
   display: flex;
   flex-direction: column;
-  animation: slideIn 0.3s var(--ease-out);
+  animation: slideIn 0.32s var(--ease);
+  box-shadow: -24px 0 80px rgba(0, 0, 0, 0.45);
 }
 
 @keyframes slideIn {
@@ -3492,11 +3974,14 @@ h1, h2, h3, h4, h5, h6 {
   display: flex;
   align-items: center;
   gap: 12px;
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.4rem;
-  letter-spacing: 0.08em;
-  text-transform: uppercase;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.5rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
+.cart-header h2 svg { color: var(--accent); }
 
 .cart-empty {
   flex: 1;
@@ -3504,126 +3989,242 @@ h1, h2, h3, h4, h5, h6 {
   flex-direction: column;
   align-items: center;
   justify-content: center;
-  color: var(--text-muted);
-  gap: 16px;
+  color: var(--ink-400);
+  gap: 18px;
+  padding: 48px 28px;
+  text-align: center;
 }
+.cart-empty svg { color: var(--accent); opacity: 0.6; }
 
 .cart-empty p {
-  font-size: 0.85rem;
-  letter-spacing: 0.04em;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 100;
+  font-style: italic;
+  font-size: 1.15rem;
+  color: var(--ink-300);
+  letter-spacing: var(--ls-tight);
 }
 
 .cart-items {
   flex: 1;
   overflow-y: auto;
-  padding: 20px 28px;
+  padding: 8px 28px;
 }
 
 .cart-item {
   display: flex;
   gap: 16px;
-  padding: 18px 0;
+  padding: 20px 0;
   border-bottom: 1px solid var(--border);
 }
 
 .cart-item img {
-  width: 72px;
-  height: 72px;
+  width: 76px;
+  height: 76px;
   object-fit: cover;
+  border-radius: var(--radius-md);
+  border: 1px solid var(--border-light);
 }
 
 .cart-item-info { flex: 1; }
-.cart-item-info h4 { font-size: 0.85rem; margin-bottom: 4px; }
+.cart-item-info h4 {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 0.98rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin-bottom: 4px;
+  color: var(--ink-100);
+}
 
 .cart-item-price {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.1rem;
-  letter-spacing: 0.04em;
-  color: var(--white);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.12rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 
 .qty-controls {
   display: flex;
   align-items: center;
-  gap: 10px;
-  margin-top: 10px;
+  gap: 4px;
+  margin-top: 12px;
+  padding: 2px;
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-pill);
+  width: fit-content;
 }
 
 .qty-controls button {
-  width: 28px;
-  height: 28px;
+  width: 26px;
+  height: 26px;
   display: flex;
   align-items: center;
   justify-content: center;
-  border: 1px solid var(--border);
-  transition: all 0.2s;
+  border-radius: 50%;
+  color: var(--ink-300);
+  transition: background-color var(--dur-fast) var(--ease),
+              color var(--dur-fast) var(--ease);
 }
-.qty-controls button:hover { border-color: var(--white); }
-.qty-controls .remove-btn:hover { border-color: var(--error); color: var(--error); }
+.qty-controls button:hover { background: var(--bg-hover); color: var(--ink-100); }
+.qty-controls .remove-btn { margin-left: 6px; }
+.qty-controls .remove-btn:hover { background: rgba(194, 92, 74, 0.12); color: var(--error); }
 
 .qty-controls span {
-  font-size: 0.85rem;
-  font-weight: 700;
-  min-width: 20px;
+  font-family: var(--font-sans);
+  font-size: 13px;
+  font-weight: 600;
+  min-width: 24px;
   text-align: center;
+  color: var(--ink-100);
 }
 
 .cart-footer {
-  padding: 24px 28px;
+  padding: 24px 28px 28px;
   border-top: 1px solid var(--border);
+  background: var(--bg-tertiary);
 }
 
 .cart-total {
   display: flex;
   justify-content: space-between;
-  margin-bottom: 20px;
+  align-items: baseline;
+  margin-bottom: 22px;
 }
 
 .cart-total span:first-child {
-  font-size: 0.75rem;
+  font-family: var(--font-sans);
+  font-size: 11px;
   font-weight: 700;
-  letter-spacing: 0.12em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-muted);
+  color: var(--ink-400);
 }
 
 .cart-total span:last-child {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.6rem;
-  letter-spacing: 0.04em;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 72, 'SOFT' 30;
+  font-size: 1.8rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 
 .checkout-btn {
   width: 100%;
   padding: 16px;
-  background: var(--white);
-  color: var(--bg-primary);
-  font-family: 'Montserrat', sans-serif;
-  font-weight: 800;
-  font-size: 0.75rem;
-  letter-spacing: 0.15em;
+  background: var(--accent);
+  color: #1a1208;
+  font-family: var(--font-sans);
+  font-weight: 700;
+  font-size: 12px;
+  letter-spacing: var(--ls-wide);
   text-transform: uppercase;
-  transition: all 0.3s;
+  border-radius: var(--radius-md);
+  box-shadow: var(--shadow-accent);
+  transition: background-color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease);
 }
-.checkout-btn:hover { background: var(--mm-blue); color: var(--white); }
+.checkout-btn:hover { background: var(--accent-soft); transform: translateY(-1px); }
 
 /* ===== FOOTER ===== */
 .footer {
-  background: var(--bg-secondary);
+  background:
+    linear-gradient(180deg, transparent 0%, var(--bg-secondary) 30%, var(--bg-secondary) 100%);
   border-top: 1px solid var(--border);
+  margin-top: var(--section-gap);
+}
+
+/* Newsletter band */
+.footer-newsletter {
+  border-bottom: 1px solid var(--border);
+  background:
+    radial-gradient(ellipse 600px 300px at 50% 0%, var(--accent-glow), transparent 70%);
+}
+.footer-newsletter-inner {
+  max-width: var(--container);
+  margin: 0 auto;
+  padding: 56px 32px;
+  display: grid;
+  grid-template-columns: 1.1fr 1fr;
+  gap: 48px;
+  align-items: center;
+}
+.footer-newsletter-copy .eyebrow { margin-bottom: 14px; }
+.footer-newsletter-title {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(1.8rem, 3.2vw, 2.6rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.08;
+  color: var(--ink-100);
+  margin-bottom: 12px;
+}
+.footer-newsletter-copy p {
+  color: var(--ink-300);
+  font-size: 1rem;
+  line-height: 1.65;
+  max-width: 460px;
+}
+.footer-newsletter-form {
+  display: flex;
+  gap: 8px;
+  padding: 6px;
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-pill);
+  transition: border-color var(--dur-fast) var(--ease);
+}
+.footer-newsletter-form:focus-within {
+  border-color: var(--accent);
+  box-shadow: 0 0 0 4px var(--accent-glow);
+}
+.footer-newsletter-form input {
+  flex: 1;
+  background: transparent;
+  border: none;
+  outline: none;
+  padding: 12px 18px;
+  color: var(--ink-100);
+  font-family: var(--font-sans);
+  font-size: 0.95rem;
+}
+.footer-newsletter-form input::placeholder { color: var(--ink-400); }
+.footer-newsletter-submit {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  padding: 12px 22px;
+  background: var(--accent);
+  color: #1a1208;
+  font-family: var(--font-sans);
+  font-size: 11.5px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  border-radius: var(--radius-pill);
+  transition: background-color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease);
+}
+.footer-newsletter-submit:hover {
+  background: var(--accent-soft);
+  transform: translateX(2px);
 }
 
 .footer-main {
   max-width: var(--container);
   margin: 0 auto;
-  padding: 80px 32px 48px;
+  padding: 80px 32px 56px;
   display: grid;
-  grid-template-columns: 1.5fr 1fr 1fr 1fr;
+  grid-template-columns: 1.4fr 1fr 1fr 1fr 1.4fr;
   gap: 48px;
 }
 
 .footer-brand {
-  max-width: 280px;
+  max-width: 320px;
 }
 
 .footer-brand-logo {
@@ -3634,89 +4235,198 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .footer-brand-logo img {
-  width: 44px;
-  height: 44px;
+  width: 40px;
+  height: 40px;
 }
 
 .footer-brand-logo span {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.2rem;
-  letter-spacing: 0.12em;
-  text-transform: uppercase;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-weight: 500;
+  font-size: 1.08rem;
+  color: var(--ink-100);
 }
 
-.footer-brand > p {
-  color: var(--text-muted);
-  font-size: 0.85rem;
-  line-height: 1.8;
+.footer-brand-blurb {
+  color: var(--ink-300);
+  font-size: 0.92rem;
+  line-height: 1.75;
 }
 
 .footer-social {
   display: flex;
-  gap: 4px;
+  gap: 6px;
   margin-top: 24px;
 }
 
 .footer-social a {
-  width: 40px;
-  height: 40px;
+  width: 38px;
+  height: 38px;
   display: flex;
   align-items: center;
   justify-content: center;
-  border: 1px solid var(--border);
-  color: var(--text-muted);
-  transition: all 0.3s;
+  border: 1px solid var(--border-light);
+  border-radius: var(--radius-md);
+  color: var(--ink-300);
+  transition: color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease),
+              background-color var(--dur-fast) var(--ease);
 }
 .footer-social a:hover {
-  border-color: var(--mm-blue);
-  color: var(--mm-blue);
+  border-color: var(--accent);
+  color: var(--accent);
+  background: rgba(201, 151, 74, 0.06);
 }
 
 .footer-col h4 {
-  font-size: 0.65rem;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
   font-weight: 700;
-  letter-spacing: 0.2em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-muted);
-  margin-bottom: 20px;
+  color: var(--accent);
+  margin-bottom: 22px;
 }
 
 .footer-col a {
   display: block;
-  padding: 6px 0;
-  font-size: 0.85rem;
-  color: var(--text-secondary);
-  transition: color 0.2s;
+  padding: 7px 0;
+  font-size: 0.94rem;
+  color: var(--ink-200);
+  transition: color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease);
+  font-weight: 400;
 }
-.footer-col a:hover { color: var(--white); }
+.footer-col a:hover { color: var(--accent-soft); transform: translateX(3px); }
 
-.footer-bottom {
-  max-width: var(--container);
-  margin: 0 auto;
-  padding: 24px 32px;
-  display: flex;
-  justify-content: space-between;
-  align-items: center;
-  border-top: 1px solid var(--border);
+/* Cafe card */
+.footer-cafe-card {
+  position: relative;
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
+  padding: 28px;
+  backdrop-filter: blur(18px);
+}
+.footer-cafe-card::before {
+  content: '';
+  position: absolute;
+  inset: 0 0 auto 0;
+  height: 1px;
+  background: linear-gradient(90deg, transparent, var(--accent) 50%, transparent);
+  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
+}
+.footer-cafe-card .eyebrow { margin-bottom: 14px; }
+.footer-cafe-title {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 72, 'SOFT' 30;
+  font-weight: 400;
+  font-size: 1.6rem;
+  line-height: 1.08;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
+  margin-bottom: 20px;
+}
+.footer-cafe-meta {
+  display: flex;
+  flex-direction: column;
+  gap: 10px;
+  padding-top: 14px;
+  border-top: 1px solid var(--border);
+}
+.footer-cafe-row {
+  display: flex;
+  align-items: flex-start;
+  gap: 10px;
+  font-size: 0.86rem;
+  color: var(--ink-300);
+  line-height: 1.5;
+}
+.footer-cafe-row svg {
+  color: var(--accent);
+  flex-shrink: 0;
+  margin-top: 3px;
+}
+.footer-cafe-row a {
+  color: var(--ink-300);
+  transition: color var(--dur-fast) var(--ease);
+}
+.footer-cafe-row a:hover { color: var(--accent-soft); }
+.footer-cafe-map {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  margin-top: 18px;
+  padding: 9px 14px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.35);
+  border-radius: var(--radius-pill);
+  transition: background-color var(--dur-fast) var(--ease),
+              color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease);
+}
+.footer-cafe-map:hover {
+  background: var(--accent);
+  color: #1a1208;
+  border-color: var(--accent);
+}
+
+.footer-bottom {
+  max-width: var(--container);
+  margin: 0 auto;
+  padding: 28px 32px;
+  display: flex;
+  justify-content: space-between;
+  align-items: center;
+  border-top: 1px solid var(--border);
+  flex-wrap: wrap;
+  gap: 16px;
 }
 
 .footer-bottom p {
-  font-size: 0.7rem;
-  color: var(--text-muted);
-  letter-spacing: 0.04em;
+  font-size: 12px;
+  color: var(--ink-400);
+  letter-spacing: 0.02em;
 }
 
 .footer-bottom-links {
   display: flex;
   gap: 24px;
+  flex-wrap: wrap;
 }
 
 .footer-bottom-links a {
-  font-size: 0.7rem;
-  color: var(--text-muted);
-  transition: color 0.2s;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--ink-400);
+  transition: color var(--dur-fast) var(--ease);
+}
+.footer-bottom-links a:hover { color: var(--accent-soft); }
+
+@media (max-width: 1100px) {
+  .footer-newsletter-inner { grid-template-columns: 1fr; gap: 32px; }
+  .footer-main {
+    grid-template-columns: 1.4fr 1fr 1fr;
+    gap: 40px;
+  }
+  .footer-cafe-card { grid-column: 1 / -1; }
+}
+
+@media (max-width: 720px) {
+  .footer-main { grid-template-columns: 1fr 1fr; gap: 32px; padding: 56px 24px 40px; }
+  .footer-brand { grid-column: 1 / -1; max-width: 100%; }
+  .footer-newsletter-inner { padding: 40px 24px; }
+  .footer-newsletter-form { flex-direction: column; padding: 14px; border-radius: var(--radius-lg); }
+  .footer-newsletter-submit { justify-content: center; }
 }
-.footer-bottom-links a:hover { color: var(--white); }
 
 /* ===== RESPONSIVE ===== */
 @media (max-width: 1200px) {
@@ -3877,81 +4587,113 @@ h1, h2, h3, h4, h5, h6 {
 
 /* ===== PAGE HEADER (shared for store/academy) ===== */
 .page-header {
-  padding: 60px 32px 48px;
+  padding: clamp(60px, 8vw, 96px) 32px clamp(48px, 6vw, 72px);
   text-align: center;
   border-bottom: 1px solid var(--border);
-  margin-bottom: 48px;
+  margin-bottom: clamp(40px, 5vw, 56px);
+  position: relative;
+}
+.page-header::after {
+  content: '';
+  position: absolute;
+  left: 50%;
+  bottom: -1px;
+  width: 60px;
+  height: 1px;
+  background: var(--accent);
+  transform: translateX(-50%);
 }
 
 .page-header h1 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2.5rem, 5vw, 4rem);
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 8px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.6rem, 5.4vw, 4.4rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin-bottom: 16px;
+  color: var(--ink-100);
 }
 
 .page-header p {
-  color: var(--text-secondary);
-  font-size: 0.95rem;
+  color: var(--ink-300);
+  font-size: 1.04rem;
+  line-height: 1.7;
+  max-width: 580px;
+  margin: 0 auto;
 }
 
 /* ===== POLICY PAGES ===== */
 .policy-page {
   min-height: 80vh;
-  padding: 80px 0 120px;
+  padding: clamp(72px, 9vw, 112px) 0 var(--section-gap);
 }
 
 .policy-container {
-  max-width: 860px;
+  max-width: var(--container-narrow);
   margin: 0 auto;
   padding: 0 32px;
 }
 
 .policy-container h1 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2.2rem, 5vw, 3.5rem);
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 4px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.4rem, 5vw, 3.8rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin-bottom: 12px;
+  color: var(--ink-100);
 }
 
 .policy-updated {
-  color: var(--text-muted);
-  font-size: 0.85rem;
-  margin-bottom: 48px;
-  padding-bottom: 32px;
+  color: var(--ink-400);
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
+  text-transform: uppercase;
+  margin-bottom: 56px;
+  padding-bottom: 36px;
   border-bottom: 1px solid var(--border);
 }
 
 .policy-container section {
-  margin-bottom: 40px;
+  margin-bottom: 48px;
+  padding-bottom: 48px;
+  border-bottom: 1px solid var(--border);
 }
+.policy-container section:last-child { border-bottom: none; }
 
 .policy-container h2 {
-  font-size: 1.2rem;
-  font-weight: 700;
-  margin-bottom: 16px;
-  color: var(--mm-blue-light);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-weight: 500;
+  font-size: 1.6rem;
+  letter-spacing: var(--ls-tight);
+  margin-bottom: 20px;
+  color: var(--ink-100);
 }
 
 .policy-container h3 {
-  font-size: 1rem;
-  font-weight: 600;
-  margin: 20px 0 10px;
-  color: var(--text-primary);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 1.15rem;
+  font-weight: 500;
+  margin: 24px 0 12px;
+  color: var(--ink-100);
 }
 
 .policy-container p {
-  color: var(--text-secondary);
-  font-size: 0.92rem;
-  line-height: 1.8;
-  margin-bottom: 12px;
+  color: var(--ink-200);
+  font-size: 1rem;
+  line-height: 1.85;
+  margin-bottom: 14px;
 }
 
 .policy-container ul,
 .policy-container ol {
-  margin: 12px 0 16px 24px;
+  margin: 14px 0 18px 24px;
   list-style: disc;
 }
 
@@ -3960,57 +4702,59 @@ h1, h2, h3, h4, h5, h6 {
 }
 
 .policy-container li {
-  color: var(--text-secondary);
-  font-size: 0.92rem;
-  line-height: 1.8;
-  margin-bottom: 6px;
+  color: var(--ink-200);
+  font-size: 1rem;
+  line-height: 1.85;
+  margin-bottom: 8px;
 }
 
 .policy-container a {
-  color: var(--mm-blue-light);
+  color: var(--accent);
   text-decoration: underline;
-  text-underline-offset: 3px;
-  transition: color 0.2s;
+  text-underline-offset: 4px;
+  text-decoration-thickness: 1px;
+  transition: color var(--dur-fast) var(--ease);
 }
 
 .policy-container a:hover {
-  color: var(--mm-pink-light);
+  color: var(--accent-soft);
 }
 
 .policy-contact {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 12px;
-  padding: 24px 28px;
-  margin-top: 12px;
+  border-radius: var(--radius-lg);
+  padding: 28px 32px;
+  margin-top: 16px;
 }
 
 .policy-contact p {
-  margin-bottom: 6px;
+  margin-bottom: 8px;
 }
 
 .policy-table {
   width: 100%;
   border-collapse: collapse;
-  margin: 16px 0 20px;
-  font-size: 0.88rem;
+  margin: 20px 0 24px;
+  font-size: 0.92rem;
 }
 
 .policy-table th,
 .policy-table td {
   text-align: left;
-  padding: 14px 16px;
+  padding: 14px 18px;
   border: 1px solid var(--border);
-  color: var(--text-secondary);
+  color: var(--ink-200);
 }
 
 .policy-table th {
   background: var(--bg-card);
-  color: var(--text-primary);
-  font-weight: 600;
-  font-size: 0.85rem;
+  color: var(--ink-100);
+  font-family: var(--font-sans);
+  font-weight: 700;
+  font-size: 11px;
   text-transform: uppercase;
-  letter-spacing: 0.05em;
+  letter-spacing: var(--ls-widest);
 }
 
 .policy-table tr:hover td {
@@ -4021,63 +4765,75 @@ h1, h2, h3, h4, h5, h6 {
 .contact-grid {
   display: grid;
   grid-template-columns: repeat(2, 1fr);
-  gap: 20px;
-  margin-top: 16px;
+  gap: 18px;
+  margin-top: 18px;
 }
 
 .contact-card {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 12px;
-  padding: 28px 24px;
-  transition: border-color 0.3s var(--ease);
+  border-radius: var(--radius-lg);
+  padding: 28px 26px;
+  transition: border-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease);
 }
 
 .contact-card:hover {
-  border-color: var(--mm-blue);
+  border-color: var(--border-strong);
+  transform: translateY(-2px);
 }
 
 .contact-card svg {
-  color: var(--mm-blue-light);
-  margin-bottom: 12px;
+  color: var(--accent);
+  margin-bottom: 14px;
 }
 
 .contact-card h3 {
   margin: 0 0 8px;
-  font-size: 1rem;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 1.1rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 
 .contact-card a {
-  color: var(--mm-blue-light);
-  font-size: 0.92rem;
+  color: var(--accent-soft);
+  font-size: 0.95rem;
 }
 
 .contact-card p {
-  font-size: 0.85rem;
+  font-size: 0.9rem;
+  color: var(--ink-300);
   margin-bottom: 4px;
+  line-height: 1.55;
 }
 
 .contact-socials {
   display: flex;
-  gap: 20px;
-  margin-top: 12px;
+  gap: 14px;
+  margin-top: 14px;
+  flex-wrap: wrap;
 }
 
 .contact-socials a {
   display: inline-flex;
   align-items: center;
   gap: 8px;
-  color: var(--text-secondary);
+  color: var(--ink-300);
   font-size: 0.9rem;
-  padding: 10px 20px;
-  border: 1px solid var(--border);
-  border-radius: 8px;
-  transition: all 0.3s var(--ease);
+  padding: 9px 16px;
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-md);
+  transition: color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease),
+              background-color var(--dur-fast) var(--ease);
 }
-
 .contact-socials a:hover {
-  color: var(--mm-blue-light);
-  border-color: var(--mm-blue);
+  color: var(--accent-soft);
+  border-color: var(--accent);
+  background: rgba(201, 151, 74, 0.04);
 }
 
 @media (max-width: 600px) {
@@ -4331,7 +5087,7 @@ h1, h2, h3, h4, h5, h6 {
   display: inline-flex;
   align-items: center;
   gap: 2px;
-  color: #F2A73B;
+  color: var(--accent);
 }
 .star-pickable {
   cursor: pointer;
@@ -4343,11 +5099,13 @@ h1, h2, h3, h4, h5, h6 {
 .product-rating {
   display: inline-flex;
   align-items: center;
-  gap: 4px;
+  gap: 5px;
   margin: 4px 0 8px;
-  color: #F2A73B;
-  font-size: 0.78rem;
+  color: var(--accent);
+  font-family: var(--font-sans);
+  font-size: 12px;
   font-weight: 600;
+  letter-spacing: 0.02em;
 }
 .product-reviews-toggle {
   display: inline-flex;
@@ -4366,7 +5124,7 @@ h1, h2, h3, h4, h5, h6 {
   cursor: pointer;
   transition: color 0.2s;
 }
-.product-reviews-toggle:hover { color: var(--mm-blue); }
+.product-reviews-toggle:hover { color: var(--accent); }
 .product-reviews {
   margin-top: 12px;
   padding-top: 12px;
@@ -4436,7 +5194,7 @@ h1, h2, h3, h4, h5, h6 {
   outline: none;
   resize: vertical;
 }
-.review-form textarea:focus { border-color: var(--mm-blue); }
+.review-form textarea:focus { border-color: var(--accent); }
 .review-submit {
   align-self: flex-start;
   padding: 8px 14px;
@@ -4454,89 +5212,122 @@ h1, h2, h3, h4, h5, h6 {
   font-size: 0.9rem;
 }
 .myorders-page {
-  padding: 40px 0 80px;
+  padding: clamp(56px, 7vw, 88px) 0 var(--section-gap);
   min-height: calc(100vh - var(--nav-height));
 }
 .myorders-header {
-  margin-bottom: 32px;
+  margin-bottom: 48px;
+  padding-bottom: 32px;
+  border-bottom: 1px solid var(--border);
 }
 .myorders-header h1 {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2rem, 4vw, 3rem);
-  letter-spacing: 0.06em;
-  text-transform: uppercase;
-  margin-bottom: 4px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.2rem, 4.2vw, 3.4rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin-bottom: 8px;
+  color: var(--ink-100);
 }
 .myorders-header p {
-  color: var(--text-muted);
-  font-size: 0.9rem;
+  color: var(--ink-300);
+  font-size: 0.98rem;
+  line-height: 1.6;
 }
 .myorders-empty {
   text-align: center;
   padding: 80px 32px;
   color: var(--text-muted);
 }
-.myorders-empty svg { margin-bottom: 16px; opacity: 0.4; }
-.myorders-empty h3 { color: var(--text-primary); margin-bottom: 8px; }
-.myorders-empty p { margin-bottom: 24px; font-size: 0.88rem; }
+.myorders-empty svg { margin-bottom: 18px; color: var(--accent); opacity: 0.6; }
+.myorders-empty h3 {
+  color: var(--ink-100);
+  margin-bottom: 10px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.5rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+}
+.myorders-empty p { margin-bottom: 28px; font-size: 0.95rem; color: var(--ink-300); }
 
 .myorders-list {
   display: flex;
   flex-direction: column;
-  gap: 12px;
+  gap: 14px;
 }
 .myorders-card {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 8px;
+  border-radius: var(--radius-lg);
   overflow: hidden;
+  transition: border-color var(--dur-base) var(--ease);
 }
+.myorders-card:hover { border-color: var(--border-strong); }
 .myorders-row {
   display: flex;
   justify-content: space-between;
   align-items: center;
-  padding: 16px 20px;
-  gap: 16px;
+  padding: 20px 24px;
+  gap: 18px;
   flex-wrap: wrap;
 }
 .myorders-id {
   display: flex;
-  gap: 12px;
+  gap: 14px;
   align-items: center;
-  color: var(--text-secondary);
+  color: var(--ink-300);
 }
 .myorders-id strong {
   display: block;
-  color: var(--text-primary);
-  font-size: 0.95rem;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  color: var(--ink-100);
+  font-size: 1.02rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
 }
 .myorders-id span {
-  font-size: 0.75rem;
-  color: var(--text-muted);
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--ink-400);
+  margin-top: 4px;
 }
 .myorders-meta {
   display: flex;
   align-items: center;
-  gap: 14px;
+  gap: 16px;
 }
 .myorders-total {
-  font-weight: 700;
-  font-size: 1.05rem;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-weight: 500;
+  font-size: 1.2rem;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 .myorders-toggle {
-  width: 28px;
-  height: 28px;
+  width: 30px;
+  height: 30px;
   display: flex;
   align-items: center;
   justify-content: center;
-  border-radius: 6px;
-  background: var(--bg-surface);
-  border: 1px solid var(--border);
-  color: var(--text-muted);
+  border-radius: var(--radius-md);
+  background: transparent;
+  border: 1px solid var(--border-strong);
+  color: var(--ink-300);
+  transition: color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease),
+              background-color var(--dur-fast) var(--ease);
 }
 .myorders-toggle:hover {
-  color: var(--mm-blue);
-  border-color: var(--mm-blue);
+  color: var(--accent);
+  border-color: var(--accent);
+  background: rgba(201, 151, 74, 0.06);
 }
 .myorders-details {
   display: grid;
@@ -4608,15 +5399,24 @@ h1, h2, h3, h4, h5, h6 {
 .mycourse-card {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 8px;
+  border-radius: var(--radius-lg);
   overflow: hidden;
   display: flex;
   flex-direction: column;
+  transition: border-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
+}
+.mycourse-card:hover {
+  border-color: var(--border-strong);
+  transform: translateY(-3px);
+  box-shadow: var(--shadow-3);
 }
 .mycourse-image {
   aspect-ratio: 16 / 9;
-  background: var(--bg-surface);
+  background: var(--bg-tertiary);
   position: relative;
+  overflow: hidden;
 }
 .mycourse-card-expired { opacity: 0.7; }
 .mycourse-card-expired .mycourse-image { filter: grayscale(0.8); }
@@ -4661,15 +5461,19 @@ h1, h2, h3, h4, h5, h6 {
   object-fit: cover;
 }
 .mycourse-info {
-  padding: 16px;
+  padding: 22px;
   display: flex;
   flex-direction: column;
-  gap: 10px;
+  gap: 12px;
   flex: 1;
 }
 .mycourse-info h3 {
-  font-size: 1rem;
-  font-weight: 700;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.2rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 .mycourse-meta {
   display: flex;
@@ -4722,28 +5526,32 @@ h1, h2, h3, h4, h5, h6 {
 .player-info {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 8px;
-  padding: 24px;
+  border-radius: var(--radius-lg);
+  padding: 26px;
   display: flex;
   flex-direction: column;
-  gap: 14px;
+  gap: 16px;
   position: sticky;
   top: calc(var(--nav-height) + 16px);
 }
 .player-info h1 {
-  font-size: 1.3rem;
-  font-weight: 700;
-  line-height: 1.3;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.4rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  line-height: 1.18;
+  color: var(--ink-100);
 }
 .player-desc {
-  font-size: 0.88rem;
-  color: var(--text-secondary);
-  line-height: 1.6;
+  font-size: 0.94rem;
+  color: var(--ink-300);
+  line-height: 1.65;
 }
 .player-meta {
   display: flex;
   flex-direction: column;
-  gap: 8px;
+  gap: 10px;
   list-style: none;
   padding: 0;
   margin: 0;
@@ -4751,10 +5559,11 @@ h1, h2, h3, h4, h5, h6 {
 .player-meta li {
   display: flex;
   align-items: center;
-  gap: 6px;
-  font-size: 0.85rem;
-  color: var(--text-secondary);
+  gap: 8px;
+  font-size: 0.92rem;
+  color: var(--ink-300);
 }
+.player-meta li svg { color: var(--accent); }
 
 @media (max-width: 900px) {
   .player-grid { grid-template-columns: 1fr; }
@@ -4790,9 +5599,12 @@ h1, h2, h3, h4, h5, h6 {
 }
 .course-detail-price-block { text-align: right; }
 .course-detail-price {
-  font-size: 1.5rem;
-  font-weight: 700;
-  color: var(--accent-blue, #3b82f6);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.6rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--accent);
 }
 .course-detail-description {
   border-top: 1px solid var(--border);
@@ -4872,8 +5684,8 @@ h1, h2, h3, h4, h5, h6 {
   background: var(--bg-elev);
 }
 .course-content-item.active {
-  background: rgba(74, 144, 217, 0.12);
-  border-left: 3px solid var(--mm-blue, #4A90D9);
+  background: rgba(201, 151, 74, 0.12);
+  border-left: 3px solid var(--accent);
   padding-left: 19px;
 }
 .course-content-item.locked {
@@ -5013,53 +5825,61 @@ h1, h2, h3, h4, h5, h6 {
 .user-trigger {
   display: inline-flex;
   align-items: center;
-  gap: 6px;
-  padding: 6px 10px;
-  border-radius: 6px;
+  gap: 8px;
+  padding: 8px 12px;
+  border-radius: var(--radius-md);
   background: transparent;
-  color: var(--text-secondary);
-  font-size: 0.85rem;
+  color: var(--ink-300);
+  font-family: var(--font-sans);
+  font-size: 11px;
   font-weight: 600;
-  transition: background 0.2s, color 0.2s;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  transition: background-color var(--dur-fast) var(--ease),
+              color var(--dur-fast) var(--ease);
 }
 .user-trigger:hover {
   background: var(--bg-hover);
-  color: var(--text-primary);
+  color: var(--ink-100);
 }
 .user-menu { position: relative; }
 .user-dropdown {
   position: absolute;
-  top: calc(100% + 8px);
+  top: calc(100% + 10px);
   right: 0;
-  background: var(--bg-card);
-  border: 1px solid var(--border);
-  border-radius: 8px;
-  min-width: 180px;
+  background: var(--bg-elevated);
+  border: 1px solid var(--border-light);
+  border-radius: var(--radius-lg);
+  min-width: 200px;
   display: flex;
   flex-direction: column;
   z-index: 200;
-  box-shadow: 0 8px 24px rgba(0,0,0,0.4);
+  box-shadow: var(--shadow-4);
   overflow: hidden;
+  backdrop-filter: blur(20px);
 }
 .user-dropdown-item {
   display: flex;
   align-items: center;
-  gap: 10px;
-  padding: 11px 14px;
-  font-size: 0.85rem;
-  color: var(--text-secondary);
+  gap: 12px;
+  padding: 12px 16px;
+  font-size: 0.92rem;
+  color: var(--ink-200);
   background: transparent;
   border: none;
   text-align: left;
   cursor: pointer;
-  transition: background 0.15s, color 0.15s;
+  transition: background-color var(--dur-fast) var(--ease),
+              color var(--dur-fast) var(--ease);
   border-bottom: 1px solid var(--border);
 }
+.user-dropdown-item svg { color: var(--ink-400); transition: color var(--dur-fast) var(--ease); }
 .user-dropdown-item:last-child { border-bottom: none; }
 .user-dropdown-item:hover {
   background: var(--bg-hover);
-  color: var(--text-primary);
+  color: var(--ink-100);
 }
+.user-dropdown-item:hover svg { color: var(--accent); }
 
 /* ===== AUTH: forgot password row ===== */
 .auth-label-row {
@@ -5068,11 +5888,14 @@ h1, h2, h3, h4, h5, h6 {
   align-items: baseline;
 }
 .auth-forgot-link {
-  font-size: 0.75rem;
-  color: var(--mm-blue);
-  font-weight: 600;
+  font-size: 11px;
+  font-family: var(--font-sans);
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--accent);
 }
-.auth-forgot-link:hover { text-decoration: underline; }
+.auth-forgot-link:hover { color: var(--accent-soft); text-decoration: underline; text-underline-offset: 3px; }
 .auth-success-state {
   display: flex;
   flex-direction: column;
@@ -5126,17 +5949,25 @@ h1, h2, h3, h4, h5, h6 {
   z-index: 10;
   width: 44px;
   height: 44px;
-  border-radius: 999px;
-  background: rgba(255, 255, 255, 0.05);
-  color: var(--text-primary);
-  border: 1px solid rgba(255, 255, 255, 0.1);
+  border-radius: 50%;
+  background: rgba(28, 24, 20, 0.7);
+  color: var(--ink-100);
+  border: 1px solid var(--border-strong);
   display: flex;
   align-items: center;
   justify-content: center;
-  transition: all 0.2s;
+  backdrop-filter: blur(12px);
+  transition: background-color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease);
   cursor: pointer;
 }
-.detail-modal-close:hover { background: rgba(255, 255, 255, 0.15); transform: scale(1.05); }
+.detail-modal-close:hover {
+  background: rgba(28, 24, 20, 0.9);
+  border-color: var(--accent);
+  color: var(--accent);
+  transform: scale(1.05);
+}
 
 .detail-modal-media {
   background: transparent;
@@ -5153,9 +5984,10 @@ h1, h2, h3, h4, h5, h6 {
   max-width: 500px;
   aspect-ratio: 1;
   object-fit: cover;
-  border-radius: 24px;
-  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.6);
-  border: 1px solid rgba(255, 255, 255, 0.05);
+  border-radius: var(--radius-xl);
+  box-shadow: var(--shadow-4);
+  border: 1px solid var(--border-light);
+  filter: saturate(0.95);
 }
 .detail-modal-placeholder {
   color: var(--text-muted);
@@ -5180,64 +6012,71 @@ h1, h2, h3, h4, h5, h6 {
 .detail-chip {
   display: inline-flex;
   align-items: center;
-  gap: 4px;
-  font-size: 0.65rem;
+  gap: 5px;
+  font-family: var(--font-sans);
+  font-size: 10px;
   font-weight: 700;
-  letter-spacing: 0.1em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  padding: 4px 10px;
-  border-radius: 4px;
-  background: rgba(255, 255, 255, 0.05);
-  border: 1px solid rgba(255, 255, 255, 0.15);
-  color: var(--text-primary);
+  padding: 5px 12px;
+  border-radius: var(--radius-pill);
+  background: rgba(244, 236, 224, 0.04);
+  border: 1px solid var(--border-strong);
+  color: var(--ink-200);
 }
 .detail-chip-success {
-  border-color: rgba(58, 170, 58, 0.4);
+  border-color: rgba(107, 147, 98, 0.45);
   color: var(--success);
 }
 .detail-chip-danger {
-  border-color: rgba(234, 51, 53, 0.4);
+  border-color: rgba(194, 92, 74, 0.45);
   color: var(--error);
 }
 .detail-modal-title {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 3rem;
-  margin: 12px 0 8px;
-  line-height: 1;
-  letter-spacing: 0.04em;
-  text-transform: uppercase;
-  text-shadow: 0 4px 20px rgba(0,0,0,0.5);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.2rem, 4vw, 3.2rem);
+  margin: 14px 0 12px;
+  line-height: 1.04;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 .detail-modal-rating {
   display: inline-flex;
   align-items: center;
-  gap: 6px;
-  color: #F2A73B;
-  font-size: 0.9rem;
-  margin-bottom: 10px;
+  gap: 8px;
+  color: var(--accent);
+  font-family: var(--font-sans);
+  font-size: 0.92rem;
+  font-weight: 600;
+  margin-bottom: 12px;
 }
 .detail-modal-price {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 2.2rem;
-  font-weight: 400;
-  color: var(--gold-accent);
-  margin: 8px 0 24px;
-  letter-spacing: 0.04em;
-  text-shadow: 0 2px 10px rgba(212, 175, 55, 0.2);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-size: 2.4rem;
+  font-weight: 500;
+  color: var(--accent);
+  margin: 10px 0 28px;
+  letter-spacing: var(--ls-tight);
 }
-.detail-modal-section { margin-top: 18px; }
+.detail-modal-section { margin-top: 22px; }
 .detail-modal-section h3 {
-  font-size: 0.95rem;
-  margin: 0 0 8px;
-  color: var(--text-primary);
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 700;
+  margin: 0 0 12px;
+  color: var(--accent);
   text-transform: uppercase;
-  letter-spacing: 0.06em;
+  letter-spacing: var(--ls-widest);
 }
 .detail-modal-desc {
   margin: 0;
-  color: var(--text-secondary);
-  line-height: 1.6;
+  color: var(--ink-300);
+  line-height: 1.75;
   white-space: pre-wrap;
+  font-size: 0.98rem;
 }
 .detail-modal-actions {
   margin-top: 22px;
@@ -5271,7 +6110,7 @@ h1, h2, h3, h4, h5, h6 {
 }
 .product-card.clickable:focus-visible,
 .course-card.clickable:focus-visible {
-  outline: 2px solid var(--mm-blue);
+  outline: 2px solid var(--accent);
   outline-offset: 3px;
 }
 
@@ -5418,67 +6257,79 @@ h1, h2, h3, h4, h5, h6 {
 }
 .player-lesson-item:hover { background: var(--bg-elev); }
 .player-lesson-item.active {
-  background: rgba(74, 144, 217, 0.18);
-  border-left: 3px solid #4A90D9;
+  background: rgba(201, 151, 74, 0.10);
+  border-left: 3px solid var(--accent);
   padding-left: 11px;
 }
-.player-lesson-item.active .player-lesson-meta { color: var(--text-secondary); }
+.player-lesson-item.active .player-lesson-meta { color: var(--ink-300); }
 .player-lesson-item.completed .player-lesson-num {
-  background: #16a34a;
-  color: #fff;
+  background: var(--success);
+  color: #1a1208;
 }
 .player-lesson-num {
-  width: 28px;
-  height: 28px;
+  width: 30px;
+  height: 30px;
   border-radius: 50%;
-  background: var(--bg-elev);
+  background: var(--bg-surface);
+  border: 1px solid var(--border);
   display: flex;
   align-items: center;
   justify-content: center;
-  font-size: 0.85rem;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 13px;
+  font-weight: 500;
   flex-shrink: 0;
+  color: var(--ink-200);
 }
 .player-lesson-body { flex: 1; min-width: 0; }
-.player-lesson-title { font-weight: 500; }
-.player-lesson-meta { font-size: 0.8rem; color: var(--text-muted); }
+.player-lesson-title { font-weight: 500; color: var(--ink-100); }
+.player-lesson-meta { font-size: 11px; color: var(--ink-400); margin-top: 4px; letter-spacing: var(--ls-wide); text-transform: uppercase; font-weight: 600; }
 .player-progress-bar {
-  height: 6px;
-  background: var(--bg-elev);
-  border-radius: 4px;
+  height: 5px;
+  background: var(--bg-surface);
+  border-radius: var(--radius-pill);
   overflow: hidden;
-  margin: 8px 0 12px;
+  margin: 10px 0 14px;
 }
 .player-progress-fill {
   height: 100%;
-  background: linear-gradient(90deg, var(--accent), #f59e0b);
+  background: linear-gradient(90deg, var(--accent-deep), var(--accent));
   transition: width 0.3s ease;
 }
 .player-certificate {
   background: var(--bg-card);
-  border: 2px solid var(--accent);
-  padding: 16px;
-  border-radius: 12px;
+  border: 1px solid var(--accent);
+  padding: 18px;
+  border-radius: var(--radius-lg);
   margin-top: 16px;
   text-align: center;
 }
 
 /* ===== Wishlist Button ===== */
 .wishlist-btn {
-  background: rgba(0,0,0,0.5);
-  border: none;
-  color: #fff;
-  width: 36px;
-  height: 36px;
+  background: rgba(12, 10, 9, 0.65);
+  border: 1px solid var(--border-light);
+  color: var(--ink-100);
+  width: 38px;
+  height: 38px;
   border-radius: 50%;
   display: inline-flex;
   align-items: center;
   justify-content: center;
   cursor: pointer;
-  transition: all 0.2s;
+  backdrop-filter: blur(8px);
+  transition: background-color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease);
+}
+.wishlist-btn:hover {
+  background: rgba(12, 10, 9, 0.85);
+  border-color: var(--accent);
+  transform: scale(1.06);
 }
-.wishlist-btn:hover { background: rgba(0,0,0,0.75); transform: scale(1.05); }
-.wishlist-btn.active { color: #ef4444; }
-.wishlist-btn.active svg { fill: #ef4444; }
+.wishlist-btn.active { color: var(--accent); border-color: var(--accent); }
+.wishlist-btn.active svg { fill: var(--accent); }
 .product-wishlist {
   position: absolute;
   top: 10px;
@@ -5490,27 +6341,39 @@ h1, h2, h3, h4, h5, h6 {
 .checkout-coupon {
   display: flex;
   gap: 8px;
-  margin: 12px 0;
+  margin: 14px 0;
 }
 .checkout-coupon input {
   flex: 1;
-  padding: 10px 14px;
-  background: var(--bg-elev);
-  border: 1px solid var(--border);
-  border-radius: 8px;
-  color: var(--text-primary);
+  padding: 12px 16px;
+  background: var(--bg-surface);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-md);
+  color: var(--ink-100);
+  font-family: var(--font-sans);
+  font-size: 0.95rem;
+  transition: border-color var(--dur-fast) var(--ease),
+              box-shadow var(--dur-fast) var(--ease);
+}
+.checkout-coupon input:focus {
+  outline: none;
+  border-color: var(--accent);
+  box-shadow: 0 0 0 3px var(--accent-glow);
 }
 .checkout-coupon-applied {
   display: flex;
   justify-content: space-between;
   align-items: center;
-  padding: 10px 14px;
-  background: rgba(34, 197, 94, 0.1);
-  border: 1px solid #22c55e;
-  border-radius: 8px;
-  color: #22c55e;
+  padding: 12px 16px;
+  background: rgba(107, 147, 98, 0.10);
+  border: 1px solid rgba(107, 147, 98, 0.45);
+  border-radius: var(--radius-md);
+  color: var(--success);
   margin: 12px 0;
+  font-family: var(--font-sans);
   font-size: 0.9rem;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
 }
 
 /* ===== Certificate Page ===== */
@@ -5786,15 +6649,28 @@ h1, h2, h3, h4, h5, h6 {
 
 /* ===== TAGLINE MARQUEE ===== */
 .tagline-marquee {
-  background: linear-gradient(90deg, var(--mm-blue), var(--mm-pink));
+  background: linear-gradient(90deg, var(--accent-deep), var(--accent) 50%, var(--accent-deep));
   overflow: hidden;
-  padding: 18px 0;
-  border-top: 1px solid rgba(255,255,255,0.08);
-  border-bottom: 1px solid rgba(255,255,255,0.08);
+  padding: 20px 0;
+  border-top: 1px solid rgba(244, 236, 224, 0.08);
+  border-bottom: 1px solid rgba(244, 236, 224, 0.08);
+  position: relative;
+}
+.tagline-marquee::before,
+.tagline-marquee::after {
+  content: '';
+  position: absolute;
+  top: 0;
+  bottom: 0;
+  width: 80px;
+  pointer-events: none;
+  z-index: 2;
 }
+.tagline-marquee::before { left: 0; background: linear-gradient(90deg, var(--accent-deep), transparent); }
+.tagline-marquee::after  { right: 0; background: linear-gradient(-90deg, var(--accent-deep), transparent); }
 .tagline-track {
   display: flex;
-  gap: 48px;
+  gap: 56px;
   white-space: nowrap;
   animation: tagline-scroll 28s linear infinite;
   width: max-content;
@@ -5802,13 +6678,14 @@ h1, h2, h3, h4, h5, h6 {
 .tagline-item {
   display: inline-flex;
   align-items: center;
-  gap: 18px;
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.6rem;
-  letter-spacing: 0.16em;
-  color: #fff;
-  text-transform: uppercase;
-  font-weight: 700;
+  gap: 22px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 100;
+  font-style: italic;
+  font-size: 1.5rem;
+  letter-spacing: var(--ls-tight);
+  color: #1a1208;
+  font-weight: 500;
 }
 .tagline-sep {
   opacity: 0.55;
@@ -5816,7 +6693,7 @@ h1, h2, h3, h4, h5, h6 {
   transform: translateY(-2px);
 }
 .tagline-dot {
-  font-size: 0.6rem;
+  font-size: 0.7rem;
   opacity: 0.7;
 }
 @keyframes tagline-scroll {
@@ -5827,52 +6704,55 @@ h1, h2, h3, h4, h5, h6 {
 /* ===== CAFE EXPERIENCE (Gallery + Menu Highlights) ===== */
 .cafe-experience-section {
   position: relative;
-  padding: 80px 0;
+  padding: var(--section-gap) 0;
   overflow: hidden;
   background:
-    radial-gradient(ellipse 900px 600px at 15% 0%, rgba(74,144,217,0.10), transparent 65%),
-    radial-gradient(ellipse 800px 500px at 90% 100%, rgba(212,100,122,0.08), transparent 65%),
+    radial-gradient(ellipse 900px 600px at 15% 0%, rgba(201, 151, 74, 0.08), transparent 65%),
+    radial-gradient(ellipse 800px 500px at 90% 100%, rgba(28, 24, 20, 0.5), transparent 65%),
     var(--bg-primary);
 }
 .cafe-experience-glow {
   position: absolute;
   inset: 0;
-  background: radial-gradient(ellipse 1100px 400px at 50% 50%, rgba(74,144,217,0.04), transparent 70%);
+  background: radial-gradient(ellipse 1100px 400px at 50% 50%, var(--accent-glow), transparent 70%);
+  opacity: 0.3;
   pointer-events: none;
 }
 .cafe-experience-section .container { position: relative; z-index: 1; }
 .cafe-experience-gallery {
   margin-top: 56px;
-  border-radius: 20px;
+  border-radius: var(--radius-lg);
   overflow: hidden;
-  box-shadow: 0 30px 80px rgba(0,0,0,0.5);
+  box-shadow: var(--shadow-4);
+  border: 1px solid var(--border);
 }
 .cafe-experience-divider {
   display: flex;
   align-items: center;
   gap: 24px;
-  margin: 72px auto 0;
+  margin: 80px auto 0;
   max-width: 520px;
 }
 .cafe-experience-divider .divider-line {
   flex: 1;
   height: 1px;
-  background: linear-gradient(90deg, transparent, var(--border), transparent);
+  background: linear-gradient(90deg, transparent, var(--border-strong), transparent);
 }
 .cafe-experience-divider .divider-label {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  letter-spacing: 0.24em;
-  font-size: 0.95rem;
-  color: var(--mm-blue-light);
+  font-family: var(--font-sans);
+  letter-spacing: var(--ls-widest);
+  font-size: 11px;
+  font-weight: 700;
+  color: var(--accent);
   text-transform: uppercase;
   white-space: nowrap;
 }
 
 /* legacy alias kept for the cafe-menu styles below */
 .cafe-menu-section {
-  padding: 80px 0;
+  padding: var(--section-gap) 0;
   background:
-    radial-gradient(ellipse at top, rgba(74,144,217,0.08), transparent 60%),
+    radial-gradient(ellipse at top, rgba(201, 151, 74, 0.06), transparent 60%),
     var(--bg-primary);
 }
 .menu-pages-grid {
@@ -5903,8 +6783,8 @@ h1, h2, h3, h4, h5, h6 {
 .menu-page-card:hover {
   transform: scale(2);
   z-index: 10;
-  border-color: var(--mm-blue);
-  box-shadow: 0 30px 60px rgba(0,0,0,0.7), 0 0 0 1px rgba(74,144,217,0.3);
+  border-color: var(--accent);
+  box-shadow: 0 30px 60px rgba(0,0,0,0.7), 0 0 0 1px var(--accent-glow);
   transition: transform 0.45s var(--ease), box-shadow 0.45s var(--ease), border-color 0.45s var(--ease), z-index 0s linear 0s;
 }
 .menu-page-card:hover img {
@@ -5923,21 +6803,24 @@ h1, h2, h3, h4, h5, h6 {
 
 /* ===== VISIT SECTION ===== */
 .visit-section {
-  padding: 80px 0;
-  background: var(--bg-secondary);
+  padding: var(--section-gap) 0;
+  background:
+    linear-gradient(180deg, transparent, rgba(21, 17, 13, 0.5) 50%, transparent);
+  border-top: 1px solid var(--border);
+  border-bottom: 1px solid var(--border);
 }
 .visit-grid {
   display: grid;
-  grid-template-columns: 1.05fr 1fr;
-  gap: 64px;
+  grid-template-columns: 7fr 5fr;
+  gap: clamp(48px, 7vw, 96px);
   align-items: center;
 }
-.visit-info .section-desc { margin-bottom: 36px; max-width: 520px; }
+.visit-info .section-desc { margin-bottom: 40px; max-width: 540px; }
 .visit-details {
   display: grid;
   grid-template-columns: 1fr 1fr;
-  gap: 24px;
-  margin-bottom: 36px;
+  gap: 22px;
+  margin-bottom: 40px;
 }
 .visit-detail {
   display: flex;
@@ -5946,33 +6829,37 @@ h1, h2, h3, h4, h5, h6 {
 }
 .visit-detail-icon {
   flex-shrink: 0;
-  width: 42px;
-  height: 42px;
-  border-radius: 10px;
+  width: 44px;
+  height: 44px;
+  border-radius: var(--radius-md);
   display: inline-flex;
   align-items: center;
   justify-content: center;
-  background: rgba(74,144,217,0.12);
-  color: var(--mm-blue-light);
-  border: 1px solid rgba(74,144,217,0.25);
+  background: rgba(201, 151, 74, 0.10);
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.25);
 }
 .visit-detail h4 {
-  font-size: 0.95rem;
-  margin-bottom: 4px;
-  color: var(--white);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 1rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin-bottom: 6px;
+  color: var(--ink-100);
 }
 .visit-detail p {
-  color: var(--text-secondary);
-  font-size: 0.88rem;
-  line-height: 1.5;
+  color: var(--ink-300);
+  font-size: 0.92rem;
+  line-height: 1.55;
   margin: 0;
 }
-.visit-detail p a { color: var(--text-secondary); transition: color 0.2s; }
-.visit-detail p a:hover { color: var(--mm-blue-light); }
+.visit-detail p a { color: var(--ink-300); transition: color var(--dur-fast) var(--ease); }
+.visit-detail p a:hover { color: var(--accent-soft); }
 .visit-socials {
   display: flex;
   align-items: center;
-  gap: 16px;
+  gap: 12px;
   flex-wrap: wrap;
 }
 .visit-social {
@@ -5980,17 +6867,24 @@ h1, h2, h3, h4, h5, h6 {
   align-items: center;
   gap: 8px;
   padding: 10px 16px;
-  border-radius: 999px;
+  border-radius: var(--radius-pill);
   background: var(--bg-card);
-  border: 1px solid var(--border);
-  color: var(--text-secondary);
-  font-size: 0.82rem;
-  font-weight: 600;
-  transition: all 0.25s var(--ease);
+  border: 1px solid var(--border-strong);
+  color: var(--ink-200);
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  transition: color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease),
+              background-color var(--dur-fast) var(--ease),
+              transform var(--dur-fast) var(--ease);
 }
 .visit-social:hover {
-  border-color: var(--mm-pink);
-  color: var(--mm-pink-light);
+  border-color: var(--accent);
+  color: var(--accent);
+  background: rgba(201, 151, 74, 0.06);
   transform: translateY(-1px);
 }
 .visit-card {
@@ -5998,47 +6892,55 @@ h1, h2, h3, h4, h5, h6 {
 }
 .visit-card-image {
   position: relative;
-  border-radius: 20px;
+  border-radius: var(--radius-lg);
   overflow: hidden;
   aspect-ratio: 4 / 5;
   max-width: 380px;
   margin: 0 auto;
-  box-shadow: 0 24px 60px rgba(0,0,0,0.5);
+  box-shadow: var(--shadow-4);
+  border: 1px solid var(--border);
 }
 .visit-card-image img {
   width: 100%;
   height: 100%;
   object-fit: cover;
+  filter: saturate(0.92);
 }
 .visit-card-rating {
   position: absolute;
-  bottom: 16px;
-  left: 16px;
-  right: 16px;
-  background: rgba(13,13,13,0.86);
-  backdrop-filter: blur(14px);
-  border: 1px solid rgba(255,255,255,0.08);
-  border-radius: 14px;
-  padding: 14px 16px;
+  bottom: 18px;
+  left: 18px;
+  right: 18px;
+  background: rgba(12, 10, 9, 0.86);
+  backdrop-filter: blur(16px);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-lg);
+  padding: 14px 18px;
   display: flex;
   align-items: center;
   gap: 14px;
   flex-wrap: wrap;
 }
 .visit-card-rating-stars {
-  color: var(--warning);
+  color: var(--accent);
   display: inline-flex;
   gap: 2px;
 }
 .visit-card-rating-score {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
   font-size: 1.4rem;
-  letter-spacing: 0.06em;
-  color: var(--white);
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 .visit-card-rating-label {
-  color: var(--text-secondary);
-  font-size: 0.72rem;
+  color: var(--ink-300);
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
+  text-transform: uppercase;
   margin-left: auto;
 }
 
@@ -6112,7 +7014,7 @@ img {
 ::-webkit-scrollbar { width: 10px; height: 10px; }
 ::-webkit-scrollbar-track { background: var(--bg-primary); }
 ::-webkit-scrollbar-thumb {
-  background: linear-gradient(180deg, var(--mm-blue), var(--mm-pink));
+  background: linear-gradient(180deg, var(--accent-deep), var(--accent));
   border-radius: 999px;
   border: 2px solid var(--bg-primary);
 }
@@ -6124,7 +7026,7 @@ button:focus-visible,
 input:focus-visible,
 select:focus-visible,
 textarea:focus-visible {
-  outline: 2px solid var(--mm-blue);
+  outline: 2px solid var(--accent);
   outline-offset: 3px;
   border-radius: 6px;
 }
@@ -6169,12 +7071,12 @@ textarea:focus-visible {
   left: 0;
   height: 100%;
   border-radius: 999px;
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-pink));
+  background: linear-gradient(135deg, var(--accent-deep), var(--accent));
   z-index: 0;
   pointer-events: none;
 }
 .slide-btn-blue .slide-btn-fill {
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-blue-light, #6fb1ff));
+  background: linear-gradient(135deg, var(--accent-deep), var(--accent));
 }
 
 .slide-btn-label {
@@ -6200,21 +7102,21 @@ textarea:focus-visible {
   width: 48px;
   height: 48px;
   border-radius: 999px;
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-pink));
-  color: var(--white);
+  background: linear-gradient(135deg, var(--accent-soft), var(--accent));
+  color: #1a1208;
   display: flex;
   align-items: center;
   justify-content: center;
   cursor: grab;
   z-index: 2;
-  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.35), 0 0 0 1px rgba(255, 255, 255, 0.08) inset;
+  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.35), 0 0 0 1px rgba(244, 236, 224, 0.10) inset;
   transition: transform 0.15s ease;
   touch-action: none;
 }
 .slide-btn-handle:active { cursor: grabbing; }
 .slide-btn-handle.is-dragging { transform: scale(1.06); }
 .slide-btn-blue .slide-btn-handle {
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-blue-light, #6fb1ff));
+  background: linear-gradient(135deg, var(--accent-soft), var(--accent));
 }
 
 .slide-btn-status {
@@ -6223,13 +7125,13 @@ textarea:focus-visible {
   display: flex;
   align-items: center;
   justify-content: center;
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-pink));
-  color: var(--white);
+  background: linear-gradient(135deg, var(--accent-deep), var(--accent));
+  color: #1a1208;
   border-radius: 999px;
   z-index: 3;
 }
 .slide-btn-blue .slide-btn-status {
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-blue-light, #6fb1ff));
+  background: linear-gradient(135deg, var(--accent-deep), var(--accent));
 }
 .slide-btn-status-icon {
   display: inline-flex;
@@ -6267,45 +7169,59 @@ textarea:focus-visible {
 .profile-card {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 12px;
-  padding: 28px;
+  border-radius: var(--radius-lg);
+  padding: 32px;
+  transition: border-color var(--dur-base) var(--ease);
 }
+.profile-card:hover { border-color: var(--border-strong); }
 
 .profile-card-head {
   display: flex;
   align-items: center;
   gap: 16px;
   margin-bottom: 24px;
-  padding-bottom: 20px;
+  padding-bottom: 22px;
   border-bottom: 1px solid var(--border);
 }
 .profile-card-head h3 {
-  font-size: 1.1rem;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.2rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
   margin: 0 0 4px;
-  color: var(--text-primary);
+  color: var(--ink-100);
 }
 .profile-card-head p {
-  font-size: 0.8rem;
-  color: var(--text-muted);
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
+  text-transform: uppercase;
+  color: var(--ink-400);
   margin: 0;
 }
 
 .profile-avatar {
-  width: 48px;
-  height: 48px;
+  width: 50px;
+  height: 50px;
   border-radius: 50%;
   display: flex;
   align-items: center;
   justify-content: center;
-  background: linear-gradient(135deg, var(--mm-blue), #6cb0f0);
-  color: #fff;
-  font-weight: 700;
-  font-size: 1rem;
-  letter-spacing: 0.04em;
+  background: linear-gradient(135deg, var(--accent), var(--accent-deep));
+  color: #1a1208;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-weight: 500;
+  font-size: 1.05rem;
+  letter-spacing: var(--ls-tight);
   flex-shrink: 0;
+  border: 1px solid rgba(244, 236, 224, 0.10);
 }
 .profile-avatar-secure {
-  background: linear-gradient(135deg, #3AAA3A, #6dd06d);
+  background: linear-gradient(135deg, var(--success), #4f7549);
+  color: var(--ink-100);
 }
 
 .input-group.input-readonly {
@@ -6325,35 +7241,35 @@ textarea:focus-visible {
 /* ==== Cookie consent banner (DPDP) ==== */
 .cookie-consent {
   position: fixed;
-  bottom: 12px;
-  left: 12px;
-  right: 12px;
+  bottom: 16px;
+  left: 16px;
+  right: 16px;
   z-index: 9999;
-  background: rgba(15, 15, 30, 0.96);
-  backdrop-filter: blur(10px);
-  border: 1px solid rgba(255, 255, 255, 0.12);
-  border-radius: 12px;
-  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.45);
-  padding: 14px 18px;
-  color: #fff;
+  background: var(--bg-elevated);
+  backdrop-filter: blur(20px);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-lg);
+  box-shadow: var(--shadow-4);
+  padding: 16px 22px;
+  color: var(--ink-100);
 }
 .cookie-consent-inner {
   display: flex;
   flex-wrap: wrap;
   align-items: center;
-  gap: 12px;
+  gap: 14px;
   justify-content: space-between;
   max-width: 1100px;
   margin: 0 auto;
 }
 .cookie-consent-text {
   margin: 0;
-  font-size: 0.88rem;
-  line-height: 1.45;
-  color: rgba(255, 255, 255, 0.9);
+  font-size: 0.92rem;
+  line-height: 1.5;
+  color: var(--ink-200);
   flex: 1 1 320px;
 }
-.cookie-consent-text a { color: #7ec3ff; text-decoration: underline; }
+.cookie-consent-text a { color: var(--accent); text-decoration: underline; text-underline-offset: 3px; }
 .cookie-consent-actions { display: flex; gap: 8px; flex-wrap: wrap; }
 @media (max-width: 520px) {
   .cookie-consent-actions { width: 100%; }
@@ -6375,78 +7291,109 @@ textarea:focus-visible {
 /* ============================================== */
 
 .page-shell { min-height: 60vh; }
-.container.narrow { max-width: 760px; margin: 0 auto; padding: 0 20px; }
+.container.narrow { max-width: var(--container-narrow); margin: 0 auto; padding: 0 24px; }
 
 .page-hero {
-  padding: 56px 0 28px;
-  background: linear-gradient(180deg, rgba(20, 20, 50, 0.6) 0%, transparent 100%);
+  padding: clamp(72px, 9vw, 112px) 0 clamp(48px, 6vw, 72px);
+  background:
+    radial-gradient(ellipse 800px 400px at 50% 0%, rgba(201, 151, 74, 0.06), transparent 60%);
+  position: relative;
+}
+.page-hero::after {
+  content: '';
+  position: absolute;
+  left: 50%;
+  bottom: 0;
+  width: 60px;
+  height: 1px;
+  background: var(--accent);
+  transform: translateX(-50%);
 }
 .page-title {
-  font-size: clamp(2rem, 4.5vw, 3.4rem);
-  font-weight: 700;
-  margin: 8px 0 16px;
-  letter-spacing: -0.02em;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.4rem, 5vw, 4rem);
+  margin: 18px 0 20px;
+  letter-spacing: var(--ls-tight);
+  line-height: 1.05;
+  color: var(--ink-100);
 }
 .page-lede {
-  font-size: 1.05rem;
-  max-width: 720px;
-  opacity: 0.85;
-  line-height: 1.6;
+  font-family: var(--font-sans);
+  font-size: 1.06rem;
+  max-width: 680px;
+  color: var(--ink-300);
+  line-height: 1.7;
 }
 
 /* ===== ABOUT PAGE — Three Intros ===== */
 .about-hero {
-  padding: 88px 0 48px;
+  padding: clamp(72px, 9vw, 112px) 0 clamp(48px, 6vw, 72px);
   text-align: center;
 }
-.about-hero .section-label { justify-content: center; }
+.about-hero .section-label { justify-content: center; margin-left: auto; margin-right: auto; }
+.about-hero .section-label::before { display: none; }
+.about-hero .section-label { gap: 0; }
 .about-hero .page-title {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2.6rem, 6vw, 4.6rem);
-  letter-spacing: 0.02em;
-  text-transform: uppercase;
-  line-height: 1.0;
-  margin: 18px auto 20px;
-  max-width: 880px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.8rem, 6vw, 5rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.04;
+  margin: 22px auto 24px;
+  max-width: 920px;
   text-wrap: balance;
+  color: var(--ink-100);
+}
+.about-hero .page-title .text-blue {
+  color: var(--accent-soft);
+  font-style: italic;
+  font-weight: 400;
+  font-variation-settings: 'opsz' 144, 'SOFT' 100;
 }
-.about-hero .page-title .text-blue { color: var(--mm-blue); font-style: italic; font-weight: 500; }
 .about-hero .page-lede {
   margin: 0 auto;
   text-align: center;
-  font-size: 1.08rem;
+  font-size: 1.12rem;
+  color: var(--ink-300);
+  max-width: 640px;
 }
 
 .about-intro {
   position: relative;
-  padding: 88px 0;
+  padding: clamp(72px, 9vw, 112px) 0;
   border-top: 1px solid var(--border);
 }
 
-.about-intro:first-of-type { border-top: none; padding-top: 40px; }
+.about-intro:first-of-type { border-top: none; padding-top: 48px; }
 
-.about-intro-namrata { background: var(--bg-secondary); overflow: hidden; }
+.about-intro-namrata {
+  background: linear-gradient(180deg, transparent, rgba(21, 17, 13, 0.5) 50%, transparent);
+  overflow: hidden;
+}
 .about-intro-namrata .about-intro-glow {
   position: absolute;
   top: -200px;
   left: -200px;
   width: 600px;
   height: 600px;
-  background: radial-gradient(circle, rgba(212,100,122,0.10), transparent 70%);
+  background: radial-gradient(circle, rgba(201, 151, 74, 0.07), transparent 70%);
   pointer-events: none;
 }
 
-.about-intro-cafe { background: rgba(255,255,255,0.015); }
+.about-intro-cafe { background: rgba(244, 236, 224, 0.012); }
 
 .about-intro-grid {
   display: grid;
-  grid-template-columns: 1.1fr 1fr;
-  gap: 72px;
+  grid-template-columns: 5fr 7fr;
+  gap: clamp(48px, 7vw, 96px);
   align-items: center;
 }
 
 .about-intro-grid.reverse {
-  grid-template-columns: 1fr 1.1fr;
+  grid-template-columns: 7fr 5fr;
 }
 
 .about-intro-grid.reverse .about-intro-media { order: 2; }
@@ -6456,12 +7403,12 @@ textarea:focus-visible {
 
 .about-intro-frame {
   position: relative;
-  border-radius: 22px;
+  border-radius: var(--radius-lg);
   overflow: hidden;
   aspect-ratio: 4 / 3;
   background: var(--bg-card);
   border: 1px solid var(--border);
-  box-shadow: 0 28px 60px -28px rgba(0,0,0,0.6);
+  box-shadow: var(--shadow-3);
 }
 
 .about-intro-frame.portrait { aspect-ratio: 4 / 5; }
@@ -6470,13 +7417,16 @@ textarea:focus-visible {
   width: 100%;
   height: 100%;
   object-fit: cover;
+  filter: saturate(0.92);
+  transition: transform var(--dur-slow) var(--ease);
 }
+.about-intro-frame:hover img { transform: scale(1.03); }
 
 .about-intro-frame::after {
   content: '';
   position: absolute;
   inset: 0;
-  background: linear-gradient(180deg, transparent 60%, rgba(10,9,8,0.4));
+  background: linear-gradient(180deg, transparent 55%, rgba(12, 10, 9, 0.5));
   pointer-events: none;
 }
 
@@ -6489,76 +7439,95 @@ textarea:focus-visible {
   align-items: center;
   gap: 6px;
   padding: 7px 14px;
-  background: rgba(10,9,8,0.82);
-  border: 1px solid rgba(255,255,255,0.1);
-  color: #fff;
-  font-size: 0.68rem;
+  background: rgba(12, 10, 9, 0.82);
+  border: 1px solid var(--border-strong);
+  color: var(--accent-soft);
+  font-family: var(--font-sans);
+  font-size: 10px;
   font-weight: 700;
-  letter-spacing: 0.14em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  border-radius: 999px;
+  border-radius: var(--radius-pill);
   backdrop-filter: blur(8px);
 }
 
 .about-intro-label {
-  font-size: 0.72rem;
+  font-family: var(--font-sans);
+  font-size: 11px;
   font-weight: 700;
-  letter-spacing: 0.22em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--mm-blue);
-  margin-bottom: 18px;
+  color: var(--accent);
+  margin-bottom: 20px;
+  display: inline-flex;
+  align-items: center;
+  gap: 14px;
+}
+.about-intro-label::before {
+  content: '';
+  width: 28px;
+  height: 1px;
+  background: linear-gradient(90deg, transparent, var(--accent) 30%);
 }
 
-.about-intro-label-pink { color: var(--mm-pink); }
-.about-intro-label-amber { color: #f0a64a; }
+.about-intro-label-pink,
+.about-intro-label-amber { color: var(--accent); }
 
 .about-intro-title {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(2.2rem, 4vw, 3.4rem);
-  line-height: 1.02;
-  letter-spacing: 0.015em;
-  margin: 0 0 24px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2.2rem, 4.2vw, 3.6rem);
+  line-height: 1.05;
+  letter-spacing: var(--ls-tight);
+  margin: 0 0 28px;
   text-wrap: balance;
+  color: var(--ink-100);
 }
 
 .about-intro-lede {
-  font-size: 1.08rem;
-  line-height: 1.75;
-  color: var(--text-primary);
-  margin: 0 0 18px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 100;
+  font-style: italic;
+  font-size: 1.32rem;
+  line-height: 1.55;
+  color: var(--ink-100);
+  margin: 0 0 22px;
+  font-weight: 400;
 }
 
 .about-intro-text p {
-  font-size: 0.98rem;
-  line-height: 1.85;
-  color: var(--text-secondary);
-  margin: 0 0 16px;
+  font-size: 1rem;
+  line-height: 1.8;
+  color: var(--ink-300);
+  margin: 0 0 18px;
 }
 
 .about-intro-text p strong {
-  color: var(--text-primary);
+  color: var(--ink-100);
   font-weight: 600;
 }
 
 .about-inline-link {
-  color: var(--mm-pink);
+  color: var(--accent);
   font-weight: 600;
   text-decoration: none;
-  border-bottom: 1px dashed rgba(212,100,122,0.5);
-  transition: color 0.2s, border-color 0.2s;
+  border-bottom: 1px solid rgba(201, 151, 74, 0.4);
+  transition: color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease);
 }
 
 .about-inline-link:hover {
-  color: var(--mm-blue);
-  border-bottom-color: var(--mm-blue);
+  color: var(--accent-soft);
+  border-bottom-color: var(--accent-soft);
 }
 
 .about-intro-stats {
   display: grid;
   grid-template-columns: repeat(3, 1fr);
   gap: 16px;
-  margin: 28px 0 32px;
-  padding: 22px 0;
+  margin: 32px 0 36px;
+  padding: 24px 0;
   border-top: 1px solid var(--border);
   border-bottom: 1px solid var(--border);
 }
@@ -6566,103 +7535,113 @@ textarea:focus-visible {
 .about-intro-stats > div {
   display: flex;
   flex-direction: column;
-  gap: 4px;
+  gap: 6px;
 }
 
 .about-intro-stats strong {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.5rem;
-  letter-spacing: 0.02em;
-  color: var(--text-primary);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 72, 'SOFT' 30;
+  font-size: 1.8rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
   line-height: 1;
 }
 
 .about-intro-stats span {
-  font-size: 0.74rem;
-  letter-spacing: 0.1em;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-secondary);
+  color: var(--ink-400);
 }
 
 .about-intro-actions {
   display: flex;
   gap: 14px;
   flex-wrap: wrap;
-  margin-top: 28px;
+  margin-top: 32px;
 }
 
 /* Namrata championship badge */
 .about-namrata-badge {
   position: absolute;
-  bottom: -22px;
-  right: -22px;
+  bottom: -24px;
+  right: -24px;
   z-index: 3;
   display: flex;
   gap: 14px;
   align-items: center;
-  padding: 16px 20px;
-  background: linear-gradient(135deg, #1a1a2e 0%, #2a1a2e 100%);
-  border: 1px solid rgba(212,100,122,0.4);
-  border-radius: 16px;
-  box-shadow: 0 20px 50px -10px rgba(212,100,122,0.35);
-  min-width: 200px;
+  padding: 16px 22px;
+  background: var(--bg-elevated);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-lg);
+  box-shadow: var(--shadow-3), 0 0 0 1px rgba(201, 151, 74, 0.25);
+  min-width: 220px;
+  backdrop-filter: blur(20px);
 }
 
 .about-namrata-badge-icon {
   width: 44px;
   height: 44px;
-  border-radius: 12px;
-  background: linear-gradient(135deg, var(--mm-pink), #b94060);
+  border-radius: var(--radius-md);
+  background: linear-gradient(135deg, var(--accent), var(--accent-deep));
   display: flex;
   align-items: center;
   justify-content: center;
-  color: #fff;
+  color: #1a1208;
   flex-shrink: 0;
 }
 
 .about-namrata-badge-num {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 1.8rem;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.7rem;
   line-height: 1;
-  color: #fff;
-  font-weight: 600;
+  color: var(--ink-100);
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
 }
 
 .about-namrata-badge-label {
-  font-size: 0.7rem;
-  letter-spacing: 0.08em;
+  font-family: var(--font-sans);
+  font-size: 10px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: rgba(255,255,255,0.75);
-  line-height: 1.35;
-  margin-top: 2px;
+  color: var(--ink-300);
+  line-height: 1.4;
+  margin-top: 4px;
 }
 
 /* Namrata credentials list */
 .about-namrata-creds {
   display: flex;
   flex-direction: column;
-  gap: 14px;
-  margin: 28px 0 8px;
+  gap: 12px;
+  margin: 32px 0 8px;
 }
 
 .about-namrata-cred {
   display: flex;
   gap: 14px;
   align-items: flex-start;
-  padding: 14px 16px;
-  background: rgba(255,255,255,0.025);
+  padding: 16px 18px;
+  background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 12px;
-  transition: border-color 0.25s, background 0.25s;
+  border-radius: var(--radius-lg);
+  transition: border-color var(--dur-base) var(--ease),
+              background-color var(--dur-base) var(--ease);
 }
 
 .about-namrata-cred:hover {
-  border-color: rgba(212,100,122,0.4);
-  background: rgba(212,100,122,0.04);
+  border-color: var(--border-strong);
+  background: var(--bg-hover);
 }
 
 .about-namrata-cred > svg {
-  color: var(--mm-pink);
+  color: var(--accent);
   margin-top: 2px;
   flex-shrink: 0;
 }
@@ -6670,18 +7649,22 @@ textarea:focus-visible {
 .about-namrata-cred > div {
   display: flex;
   flex-direction: column;
-  gap: 2px;
+  gap: 4px;
 }
 
 .about-namrata-cred strong {
-  font-size: 0.95rem;
-  color: var(--text-primary);
-  font-weight: 600;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 1.02rem;
+  color: var(--ink-100);
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
 }
 
 .about-namrata-cred span {
-  font-size: 0.82rem;
-  color: var(--text-secondary);
+  font-size: 0.88rem;
+  color: var(--ink-300);
+  line-height: 1.55;
 }
 
 /* Cafe quick-cards */
@@ -6694,42 +7677,48 @@ textarea:focus-visible {
 
 .about-cafe-card {
   display: flex;
-  gap: 12px;
+  gap: 14px;
   align-items: flex-start;
-  padding: 14px 16px;
-  background: rgba(255,255,255,0.025);
+  padding: 16px 18px;
+  background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 12px;
+  border-radius: var(--radius-lg);
+  transition: border-color var(--dur-base) var(--ease);
 }
+.about-cafe-card:hover { border-color: var(--border-strong); }
 
 .about-cafe-card-icon {
-  width: 34px;
-  height: 34px;
-  border-radius: 9px;
-  background: rgba(240,166,74,0.15);
-  color: #f0a64a;
+  width: 36px;
+  height: 36px;
+  border-radius: var(--radius-md);
+  background: rgba(201, 151, 74, 0.10);
+  color: var(--accent);
   display: flex;
   align-items: center;
   justify-content: center;
   flex-shrink: 0;
+  border: 1px solid rgba(201, 151, 74, 0.25);
 }
 
 .about-cafe-card > div:last-child {
   display: flex;
   flex-direction: column;
-  gap: 2px;
+  gap: 4px;
 }
 
 .about-cafe-card strong {
-  font-size: 0.92rem;
-  color: var(--text-primary);
-  font-weight: 600;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 0.98rem;
+  color: var(--ink-100);
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
 }
 
 .about-cafe-card span {
-  font-size: 0.78rem;
-  color: var(--text-secondary);
-  line-height: 1.4;
+  font-size: 13px;
+  color: var(--ink-300);
+  line-height: 1.5;
 }
 
 @media (max-width: 1024px) {
@@ -6763,79 +7752,143 @@ textarea:focus-visible {
 }
 
 /* Values grid (About + Consultancy) */
-.values-section, .consult-services { padding: 40px 0; }
+.values-section, .consult-services { padding: 56px 0; }
 .values-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
+  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
   gap: 20px;
-  margin-top: 32px;
+  margin-top: 40px;
 }
 .value-card {
-  padding: 28px 22px;
-  background: rgba(255, 255, 255, 0.03);
-  border: 1px solid rgba(255, 255, 255, 0.08);
-  border-radius: 14px;
-  transition: transform 0.2s, border-color 0.2s;
+  padding: 32px 26px;
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
+  transition: transform var(--dur-base) var(--ease),
+              border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
 }
 .value-card:hover {
   transform: translateY(-3px);
-  border-color: rgba(126, 195, 255, 0.4);
+  border-color: var(--border-strong);
+  box-shadow: var(--shadow-3);
 }
 .value-icon {
-  width: 44px; height: 44px;
+  width: 46px; height: 46px;
   display: inline-flex; align-items: center; justify-content: center;
-  border-radius: 10px;
-  background: rgba(126, 195, 255, 0.15);
-  color: #7ec3ff;
-  margin-bottom: 14px;
+  border-radius: var(--radius-md);
+  background: rgba(201, 151, 74, 0.10);
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.25);
+  margin-bottom: 18px;
+}
+.value-card h3 {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.2rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin: 0 0 10px;
+  color: var(--ink-100);
+}
+.value-card p {
+  font-size: 0.95rem;
+  color: var(--ink-300);
+  line-height: 1.65;
+  margin: 0;
 }
-.value-card h3 { font-size: 1.1rem; margin: 0 0 8px; }
-.value-card p { font-size: 0.92rem; opacity: 0.78; line-height: 1.55; margin: 0; }
 
 /* CTA Section */
-.cta-section { padding: 40px 0 56px; }
+.cta-section { padding: 56px 0 72px; }
 .cta-card {
+  position: relative;
   text-align: center;
-  padding: 60px 24px;
-  background: linear-gradient(135deg, rgba(126, 195, 255, 0.1), rgba(212, 100, 122, 0.1));
-  border-radius: 18px;
-  border: 1px solid rgba(255, 255, 255, 0.08);
+  padding: clamp(56px, 7vw, 88px) 32px;
+  background: var(--bg-card);
+  border-radius: var(--radius-lg);
+  border: 1px solid var(--border);
+  overflow: hidden;
+}
+.cta-card::before {
+  content: '';
+  position: absolute;
+  inset: 0;
+  background:
+    radial-gradient(ellipse 600px 300px at 50% 0%, var(--accent-glow), transparent 65%);
+  pointer-events: none;
+}
+.cta-card > * { position: relative; }
+.cta-card h2 {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2rem, 3.6vw, 2.8rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.08;
+  margin: 0 0 16px;
+  color: var(--ink-100);
+}
+.cta-card p {
+  color: var(--ink-300);
+  font-size: 1.04rem;
+  margin: 0 0 32px;
+  line-height: 1.7;
 }
-.cta-card h2 { font-size: 1.8rem; margin: 0 0 12px; }
-.cta-card p { opacity: 0.8; margin: 0 0 24px; }
 .cta-card .hero-btns { justify-content: center; }
 
 /* Consultancy process */
-.consult-process { padding: 40px 0; }
+.consult-process { padding: 56px 0; }
 .process-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
+  grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
   gap: 20px;
-  margin-top: 32px;
+  margin-top: 40px;
 }
 .process-card {
   position: relative;
-  padding: 28px 22px;
-  background: rgba(255, 255, 255, 0.03);
-  border: 1px solid rgba(255, 255, 255, 0.08);
-  border-radius: 14px;
+  padding: 32px 26px;
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
+  transition: border-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease);
+}
+.process-card:hover {
+  border-color: var(--border-strong);
+  transform: translateY(-3px);
 }
 .process-step {
-  font-size: 2rem;
-  font-weight: 700;
-  color: #7ec3ff;
-  opacity: 0.5;
-  margin-bottom: 8px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-size: 3rem;
+  font-weight: 400;
+  letter-spacing: var(--ls-tight);
+  color: var(--accent);
+  line-height: 1;
+  margin-bottom: 14px;
+}
+.process-card h3 {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 1.18rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin: 0 0 10px;
+  color: var(--ink-100);
+}
+.process-card p {
+  font-size: 0.94rem;
+  color: var(--ink-300);
+  line-height: 1.65;
+  margin: 0;
 }
-.process-card h3 { font-size: 1.05rem; margin: 0 0 8px; }
-.process-card p { font-size: 0.9rem; opacity: 0.78; line-height: 1.55; margin: 0; }
 
 /* Suited / contact panel */
-.consult-suited { padding: 40px 0 80px; }
+.consult-suited { padding: 56px 0 var(--section-gap); }
 .suited-grid {
   display: grid;
   grid-template-columns: 1.2fr 1fr;
-  gap: 36px;
+  gap: 48px;
   align-items: center;
 }
 @media (max-width: 760px) {
@@ -6844,48 +7897,66 @@ textarea:focus-visible {
 .suited-list {
   list-style: none;
   padding: 0;
-  margin: 24px 0 0;
+  margin: 28px 0 0;
   display: flex;
   flex-direction: column;
-  gap: 10px;
+  gap: 12px;
 }
 .suited-list li {
   display: flex;
   align-items: center;
-  gap: 10px;
-  font-size: 0.95rem;
+  gap: 12px;
+  font-size: 0.98rem;
+  color: var(--ink-200);
 }
-.suited-list li svg { color: #3AAA3A; flex-shrink: 0; }
+.suited-list li svg { color: var(--accent); flex-shrink: 0; }
 .suited-card {
-  padding: 32px 28px;
-  background: rgba(126, 195, 255, 0.08);
-  border: 1px solid rgba(126, 195, 255, 0.25);
-  border-radius: 14px;
+  padding: 36px 32px;
+  background: var(--bg-card);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-lg);
+  box-shadow: var(--shadow-2);
+}
+.suited-card h3 {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin: 0 0 12px;
+  font-size: 1.4rem;
+  color: var(--ink-100);
+}
+.suited-card p {
+  color: var(--ink-300);
+  margin: 0 0 24px;
+  font-size: 0.98rem;
+  line-height: 1.65;
 }
-.suited-card h3 { margin: 0 0 8px; font-size: 1.2rem; }
-.suited-card p { opacity: 0.8; margin: 0 0 20px; font-size: 0.92rem; }
 
 /* Blog grid */
-.blog-list-section { padding: 20px 0 56px; }
+.blog-list-section { padding: 32px 0 var(--section-gap); }
 .blog-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(290px, 1fr));
+  grid-template-columns: repeat(auto-fill, minmax(310px, 1fr));
   gap: 24px;
 }
 .blog-card {
   display: flex;
   flex-direction: column;
-  background: rgba(255, 255, 255, 0.03);
-  border: 1px solid rgba(255, 255, 255, 0.08);
-  border-radius: 14px;
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
   overflow: hidden;
   text-decoration: none;
   color: inherit;
-  transition: transform 0.2s, border-color 0.2s;
+  transition: transform var(--dur-base) var(--ease),
+              border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
 }
 .blog-card:hover {
   transform: translateY(-4px);
-  border-color: rgba(126, 195, 255, 0.4);
+  border-color: var(--border-strong);
+  box-shadow: var(--shadow-3);
 }
 .blog-card-cover {
   aspect-ratio: 16/10;
@@ -6902,72 +7973,108 @@ textarea:focus-visible {
   display: flex; align-items: center; justify-content: center;
   opacity: 0.3;
 }
-.blog-card-body { padding: 20px; flex: 1; display: flex; flex-direction: column; gap: 8px; }
+.blog-card-body { padding: 24px; flex: 1; display: flex; flex-direction: column; gap: 10px; }
 .blog-card-meta {
-  display: flex; align-items: center; gap: 6px;
-  font-size: 0.78rem;
-  opacity: 0.65;
+  display: flex; align-items: center; gap: 8px;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 700;
+  color: var(--accent);
   text-transform: uppercase;
-  letter-spacing: 0.05em;
+  letter-spacing: var(--ls-widest);
+}
+.blog-card-title {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 48, 'SOFT' 30;
+  font-size: 1.32rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin: 0;
+  line-height: 1.2;
+  color: var(--ink-100);
+}
+.blog-card-excerpt {
+  font-size: 0.94rem;
+  color: var(--ink-300);
+  line-height: 1.65;
+  margin: 0;
+  flex: 1;
 }
-.blog-card-title { font-size: 1.15rem; margin: 0; line-height: 1.35; }
-.blog-card-excerpt { font-size: 0.9rem; opacity: 0.75; line-height: 1.55; margin: 0; flex: 1; }
 .blog-card-link {
   margin-top: 8px;
-  font-size: 0.85rem;
-  color: #7ec3ff;
-  display: inline-flex; align-items: center; gap: 4px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--accent);
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
 }
 
 /* Blog post */
-.blog-post-page { padding: 80px 0 100px; }
+.blog-post-page { padding: clamp(72px, 9vw, 112px) 0 var(--section-gap); }
 .blog-back {
   display: inline-flex; align-items: center; gap: 6px;
-  font-size: 0.85rem;
-  opacity: 0.7;
-  margin-bottom: 32px;
-  transition: opacity 0.2s;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--ink-400);
+  margin-bottom: 36px;
+  transition: color var(--dur-fast) var(--ease);
 }
-.blog-back:hover { opacity: 1; }
+.blog-back:hover { color: var(--accent); }
 .blog-post-meta {
-  display: inline-flex; align-items: center; gap: 6px;
-  font-size: 0.78rem;
-  opacity: 0.65;
+  display: inline-flex; align-items: center; gap: 8px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  color: var(--accent);
   text-transform: uppercase;
-  letter-spacing: 0.08em;
-  margin-bottom: 14px;
+  letter-spacing: var(--ls-widest);
+  margin-bottom: 18px;
 }
 .blog-post-title {
-  font-size: clamp(1.9rem, 4.2vw, 3rem);
-  line-height: 1.15;
-  margin: 0 0 20px;
-  letter-spacing: -0.02em;
-  font-weight: 700;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(2rem, 4.6vw, 3.4rem);
+  line-height: 1.06;
+  margin: 0 0 22px;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
 .blog-post-excerpt {
-  font-size: 1.15rem;
-  opacity: 0.78;
-  line-height: 1.6;
-  margin: 0 0 36px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 100;
+  font-style: italic;
+  font-size: 1.25rem;
+  color: var(--ink-200);
+  line-height: 1.55;
+  margin: 0 0 40px;
   font-weight: 400;
 }
 .blog-post-cover {
-  margin: 32px 0 40px;
-  border-radius: 14px;
+  margin: 36px 0 44px;
+  border-radius: var(--radius-lg);
   overflow: hidden;
+  border: 1px solid var(--border);
 }
 .blog-post-cover img { width: 100%; display: block; }
 .blog-post-body {
   font-size: 1.075rem;
   line-height: 1.85;
-  color: var(--text-primary, #ededed);
+  color: var(--ink-200);
   max-width: 70ch;
 }
 .blog-post-body p {
   font-size: 1.075rem;
   line-height: 1.85;
   margin: 0 0 1.4em;
-  opacity: 0.92;
+  color: var(--ink-200);
   white-space: pre-line;
   word-wrap: break-word;
   overflow-wrap: break-word;
@@ -6996,94 +8103,155 @@ textarea:focus-visible {
 .blog-card-skeleton .blog-card-cover { background: rgba(255, 255, 255, 0.05); }
 
 /* Baristas paywall */
-.paywall-section { padding: 20px 0 56px; }
+.paywall-section { padding: 32px 0 var(--section-gap); }
 .paywall-card {
+  position: relative;
   text-align: center;
-  padding: 48px 32px;
-  background: linear-gradient(135deg, rgba(126, 195, 255, 0.1), rgba(20, 20, 50, 0.4));
-  border: 1px solid rgba(126, 195, 255, 0.25);
-  border-radius: 18px;
+  padding: clamp(48px, 6vw, 72px) 32px;
+  background: var(--bg-card);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-xl);
+  overflow: hidden;
+}
+.paywall-card::before {
+  content: '';
+  position: absolute;
+  inset: 0;
+  background: radial-gradient(ellipse 700px 320px at 50% 0%, var(--accent-glow), transparent 60%);
+  pointer-events: none;
 }
+.paywall-card > * { position: relative; }
 .paywall-icon {
-  width: 64px; height: 64px;
-  margin: 0 auto 18px;
+  width: 68px; height: 68px;
+  margin: 0 auto 22px;
   display: flex; align-items: center; justify-content: center;
   border-radius: 50%;
-  background: rgba(126, 195, 255, 0.18);
-  color: #7ec3ff;
+  background: rgba(201, 151, 74, 0.12);
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.30);
+}
+.paywall-card h2 {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-weight: 400;
+  font-size: clamp(1.8rem, 3.2vw, 2.4rem);
+  letter-spacing: var(--ls-tight);
+  line-height: 1.08;
+  margin: 0 0 14px;
+  color: var(--ink-100);
+}
+.paywall-card > p {
+  color: var(--ink-300);
+  max-width: 540px;
+  margin: 0 auto 28px;
+  line-height: 1.65;
+  font-size: 1rem;
 }
-.paywall-card h2 { font-size: 1.7rem; margin: 0 0 12px; }
-.paywall-card > p { opacity: 0.85; max-width: 520px; margin: 0 auto 24px; line-height: 1.6; }
 .paywall-benefits {
   list-style: none;
   padding: 0;
-  margin: 0 auto 28px;
-  max-width: 360px;
+  margin: 0 auto 32px;
+  max-width: 380px;
   text-align: left;
   display: flex;
   flex-direction: column;
-  gap: 10px;
+  gap: 12px;
 }
 .paywall-benefits li {
-  display: flex; align-items: center; gap: 10px;
-  font-size: 0.92rem;
+  display: flex; align-items: center; gap: 12px;
+  font-size: 0.94rem;
+  color: var(--ink-200);
 }
-.paywall-benefits li svg { color: #7ec3ff; flex-shrink: 0; }
+.paywall-benefits li svg { color: var(--accent); flex-shrink: 0; }
 .paywall-price {
   display: flex; align-items: baseline; justify-content: center; gap: 8px;
-  margin-bottom: 24px;
+  margin-bottom: 28px;
+}
+.paywall-amount {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-size: 2.8rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
+}
+.paywall-period {
+  color: var(--ink-400);
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
+  text-transform: uppercase;
 }
-.paywall-amount { font-size: 2.4rem; font-weight: 700; }
-.paywall-period { opacity: 0.65; font-size: 0.95rem; }
-.paywall-foot { margin-top: 18px; font-size: 0.85rem; opacity: 0.7; }
-.paywall-foot a { color: #7ec3ff; }
+.paywall-foot { margin-top: 20px; font-size: 13px; color: var(--ink-400); }
+.paywall-foot a { color: var(--accent); }
 
 /* Baristas list */
-.baristas-list-section { padding: 20px 0 56px; }
+.baristas-list-section { padding: 32px 0 var(--section-gap); }
 .baristas-grid {
   display: grid;
-  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
+  grid-template-columns: repeat(auto-fill, minmax(310px, 1fr));
   gap: 22px;
 }
 .barista-card {
-  background: rgba(255, 255, 255, 0.03);
-  border: 1px solid rgba(255, 255, 255, 0.08);
-  border-radius: 14px;
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-lg);
   overflow: hidden;
-  transition: transform 0.2s, border-color 0.2s;
+  transition: transform var(--dur-base) var(--ease),
+              border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
   display: flex;
   flex-direction: column;
 }
 .barista-card:hover {
   transform: translateY(-3px);
-  border-color: rgba(126, 195, 255, 0.4);
+  border-color: var(--border-strong);
+  box-shadow: var(--shadow-3);
 }
 .barista-card-photo {
   aspect-ratio: 4/3;
-  background: rgba(255, 255, 255, 0.04);
+  background: var(--bg-tertiary);
   overflow: hidden;
 }
-.barista-card-photo img { width: 100%; height: 100%; object-fit: cover; }
+.barista-card-photo img { width: 100%; height: 100%; object-fit: cover; filter: saturate(0.92); }
 .barista-card-photo-fallback {
   width: 100%; height: 100%;
   display: flex; align-items: center; justify-content: center;
-  opacity: 0.35;
+  color: var(--ink-400);
+  opacity: 0.5;
+}
+.barista-card-body { padding: 22px 22px 24px; display: flex; flex-direction: column; gap: 12px; flex: 1; }
+.barista-card-name {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.25rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  margin: 0;
+  color: var(--ink-100);
 }
-.barista-card-body { padding: 18px 20px; display: flex; flex-direction: column; gap: 10px; flex: 1; }
-.barista-card-name { font-size: 1.15rem; margin: 0; }
 .barista-card-meta {
   display: inline-flex; align-items: center; gap: 6px;
-  font-size: 0.82rem;
-  opacity: 0.7;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--ink-400);
 }
-.barista-card-stats { display: flex; gap: 10px; flex-wrap: wrap; }
+.barista-card-stats { display: flex; gap: 8px; flex-wrap: wrap; }
 .barista-stat {
   display: inline-flex; align-items: center; gap: 5px;
-  font-size: 0.78rem;
-  padding: 4px 10px;
-  background: rgba(126, 195, 255, 0.12);
-  color: #7ec3ff;
-  border-radius: 999px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 600;
+  letter-spacing: var(--ls-wide);
+  padding: 5px 12px;
+  background: rgba(201, 151, 74, 0.10);
+  color: var(--accent-soft);
+  border: 1px solid rgba(201, 151, 74, 0.25);
+  border-radius: var(--radius-pill);
 }
 .barista-section h4 {
   font-size: 0.78rem;
@@ -7214,8 +8382,9 @@ textarea:focus-visible {
 .form-field input:focus,
 .form-field textarea:focus {
   outline: none;
-  border-color: #7ec3ff;
-  background: rgba(255, 255, 255, 0.06);
+  border-color: var(--accent);
+  background: rgba(244, 236, 224, 0.05);
+  box-shadow: 0 0 0 3px var(--accent-glow);
 }
 .form-actions { display: flex; gap: 10px; margin-top: 6px; }
 .form-note { font-size: 0.78rem; opacity: 0.6; line-height: 1.5; margin: 0; }
@@ -7280,14 +8449,14 @@ textarea:focus-visible {
   border: 1px solid transparent;
 }
 .dual-tag-baristas {
-  color: #ffd27a;
-  background: rgba(255, 210, 122, 0.1);
-  border-color: rgba(255, 210, 122, 0.28);
+  color: var(--accent);
+  background: rgba(201, 151, 74, 0.10);
+  border-color: rgba(201, 151, 74, 0.30);
 }
 .dual-tag-cafes {
-  color: #7ec3ff;
-  background: rgba(126, 195, 255, 0.1);
-  border-color: rgba(126, 195, 255, 0.28);
+  color: var(--accent-soft);
+  background: rgba(232, 200, 150, 0.10);
+  border-color: rgba(232, 200, 150, 0.30);
 }
 
 /* Get Discovered card */
@@ -7295,14 +8464,24 @@ textarea:focus-visible {
   max-width: 880px;
   margin: 0 auto;
   padding: 36px;
-  background: linear-gradient(135deg, rgba(255, 210, 122, 0.08), rgba(20, 20, 50, 0.4));
-  border: 1px solid rgba(255, 210, 122, 0.22);
-  border-radius: 18px;
+  background: var(--bg-card);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-xl);
   display: grid;
   grid-template-columns: 1.2fr 1fr;
   gap: 32px;
   align-items: center;
+  position: relative;
+  overflow: hidden;
+}
+.discovered-card::before {
+  content: '';
+  position: absolute;
+  inset: 0;
+  background: radial-gradient(ellipse 600px 300px at 0% 0%, var(--accent-glow), transparent 60%);
+  pointer-events: none;
 }
+.discovered-card > * { position: relative; }
 @media (max-width: 760px) {
   .discovered-card {
     grid-template-columns: 1fr;
@@ -7324,12 +8503,13 @@ textarea:focus-visible {
   align-items: flex-start;
 }
 .discovered-perks .perk-icon {
-  width: 38px; height: 38px;
+  width: 40px; height: 40px;
   flex-shrink: 0;
   display: flex; align-items: center; justify-content: center;
-  border-radius: 10px;
-  background: rgba(255, 210, 122, 0.15);
-  color: #ffd27a;
+  border-radius: var(--radius-md);
+  background: rgba(201, 151, 74, 0.12);
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.25);
 }
 .discovered-perks li > div:last-child {
   display: flex;
@@ -7424,25 +8604,32 @@ textarea:focus-visible {
 }
 .cross-link-card:hover {
   transform: translateY(-3px);
-  border-color: rgba(126, 195, 255, 0.4);
-  background: rgba(126, 195, 255, 0.05);
+  border-color: var(--border-strong);
+  background: var(--bg-hover);
+  box-shadow: var(--shadow-3);
 }
 .cross-link-icon {
-  width: 42px; height: 42px;
+  width: 44px; height: 44px;
   display: flex; align-items: center; justify-content: center;
-  border-radius: 10px;
-  background: rgba(126, 195, 255, 0.15);
-  color: #7ec3ff;
-  margin-bottom: 4px;
+  border-radius: var(--radius-md);
+  background: rgba(201, 151, 74, 0.12);
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.25);
+  margin-bottom: 6px;
 }
 .cross-link-card h4 {
-  font-size: 1.05rem;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 1.12rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
   margin: 0;
+  color: var(--ink-100);
 }
 .cross-link-card p {
-  font-size: 0.88rem;
-  opacity: 0.75;
-  line-height: 1.5;
+  font-size: 0.92rem;
+  color: var(--ink-300);
+  line-height: 1.55;
   margin: 0;
   flex: 1;
 }
@@ -7450,10 +8637,13 @@ textarea:focus-visible {
   display: inline-flex;
   align-items: center;
   gap: 6px;
-  margin-top: 8px;
-  font-size: 0.85rem;
-  font-weight: 600;
-  color: #7ec3ff;
+  margin-top: 10px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  color: var(--accent);
 }
 
 /* ===== Barista signup form ===== */
@@ -7466,13 +8656,17 @@ textarea:focus-visible {
 .signup-trust {
   display: inline-flex;
   align-items: center;
-  gap: 6px;
-  padding: 6px 12px;
-  font-size: 0.78rem;
-  border-radius: 999px;
-  background: rgba(255, 210, 122, 0.1);
-  color: #ffd27a;
-  border: 1px solid rgba(255, 210, 122, 0.25);
+  gap: 8px;
+  padding: 7px 14px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  border-radius: var(--radius-pill);
+  background: rgba(201, 151, 74, 0.10);
+  color: var(--accent-soft);
+  border: 1px solid rgba(201, 151, 74, 0.28);
 }
 
 .barista-form .form-step {
@@ -7483,12 +8677,12 @@ textarea:focus-visible {
 .barista-form .form-step:first-child { border-top: 0; padding-top: 0; }
 .form-step-tag {
   display: inline-block;
-  font-size: 0.72rem;
+  font-family: var(--font-sans);
+  font-size: 11px;
   font-weight: 700;
-  letter-spacing: 0.14em;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  opacity: 0.7;
-  color: #ffd27a;
+  color: var(--accent);
 }
 
 .barista-form .form-field > span {
@@ -7538,16 +8732,23 @@ textarea:focus-visible {
   align-items: center;
   gap: 6px;
   padding: 8px 14px;
-  font-size: 0.85rem;
-  font-weight: 600;
-  background: rgba(255, 210, 122, 0.1);
-  color: #ffd27a;
-  border: 1px dashed rgba(255, 210, 122, 0.4);
-  border-radius: 8px;
+  font-family: var(--font-sans);
+  font-size: 11px;
+  font-weight: 700;
+  letter-spacing: var(--ls-wide);
+  text-transform: uppercase;
+  background: rgba(201, 151, 74, 0.10);
+  color: var(--accent);
+  border: 1px dashed rgba(201, 151, 74, 0.40);
+  border-radius: var(--radius-md);
   cursor: pointer;
-  transition: background 0.15s;
+  transition: background-color var(--dur-fast) var(--ease),
+              border-color var(--dur-fast) var(--ease);
+}
+.education-add:hover {
+  background: rgba(201, 151, 74, 0.20);
+  border-style: solid;
 }
-.education-add:hover { background: rgba(255, 210, 122, 0.2); }
 
 .captcha-field input {
   max-width: 200px;
@@ -7562,11 +8763,11 @@ textarea:focus-visible {
 }
 
 .barista-form .form-note strong { font-weight: 700; }
-.barista-form .form-note a { color: #7ec3ff; text-decoration: underline; }
+.barista-form .form-note a { color: var(--accent); text-decoration: underline; text-underline-offset: 3px; }
 
 /* Text-only barista card (photoless directory) */
 .barista-card-text {
-  background: linear-gradient(155deg, rgba(126, 195, 255, 0.06), rgba(255, 255, 255, 0.03));
+  background: linear-gradient(155deg, rgba(201, 151, 74, 0.05), var(--bg-card));
 }
 .barista-card-text .barista-card-body { padding: 22px 22px; gap: 12px; }
 .barista-edu-list {
@@ -7612,9 +8813,9 @@ textarea:focus-visible {
 }
 .access-banner-warn span { color: rgba(255, 255, 255, 0.78); font-size: 0.82rem; }
 .access-banner-info {
-  background: rgba(126, 195, 255, 0.08);
-  border: 1px solid rgba(126, 195, 255, 0.25);
-  color: var(--mm-blue);
+  background: rgba(201, 151, 74, 0.08);
+  border: 1px solid rgba(201, 151, 74, 0.28);
+  color: var(--accent-soft);
 }
 
 /* ===== Hire flow stepper ===== */
@@ -7641,34 +8842,36 @@ textarea:focus-visible {
 }
 .hire-steps li.active {
   opacity: 1;
-  border-color: rgba(126, 195, 255, 0.4);
-  background: rgba(126, 195, 255, 0.08);
-  box-shadow: 0 0 0 3px rgba(126, 195, 255, 0.08);
+  border-color: var(--accent);
+  background: rgba(201, 151, 74, 0.08);
+  box-shadow: 0 0 0 3px var(--accent-glow);
 }
 .hire-steps li.done {
   opacity: 0.85;
-  border-color: rgba(76, 175, 80, 0.35);
-  background: rgba(76, 175, 80, 0.06);
+  border-color: rgba(107, 147, 98, 0.40);
+  background: rgba(107, 147, 98, 0.06);
 }
 .hire-step-num {
-  width: 28px;
-  height: 28px;
+  width: 30px;
+  height: 30px;
   border-radius: 50%;
   display: inline-flex;
   align-items: center;
   justify-content: center;
-  background: rgba(255, 255, 255, 0.08);
-  font-weight: 700;
-  font-size: 0.85rem;
+  background: rgba(244, 236, 224, 0.08);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-weight: 500;
+  font-size: 14px;
   flex-shrink: 0;
 }
 .hire-steps li.active .hire-step-num {
-  background: var(--mm-blue);
-  color: #0D0D0D;
+  background: var(--accent);
+  color: #1a1208;
 }
 .hire-steps li.done .hire-step-num {
-  background: rgba(76, 175, 80, 0.85);
-  color: #0D0D0D;
+  background: var(--success);
+  color: #1a1208;
 }
 .hire-steps li > div { display: flex; flex-direction: column; line-height: 1.3; }
 .hire-steps li strong { font-size: 0.92rem; }
@@ -7676,56 +8879,75 @@ textarea:focus-visible {
 
 /* ===== Cafe brief form (Baristas page) ===== */
 .brief-form {
-  background: linear-gradient(155deg, rgba(126, 195, 255, 0.08), rgba(255, 255, 255, 0.03));
-  border: 1px solid rgba(126, 195, 255, 0.22);
-  border-radius: 16px;
-  padding: 24px 24px 22px;
+  background: var(--bg-card);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-xl);
+  padding: 28px 28px 26px;
   margin-top: 24px;
-  color: var(--white);
+  color: var(--ink-100);
+  position: relative;
+  overflow: hidden;
+}
+.brief-form::before {
+  content: '';
+  position: absolute;
+  inset: 0 0 auto 0;
+  height: 1px;
+  background: linear-gradient(90deg, transparent, var(--accent) 50%, transparent);
 }
 .brief-form-head {
   display: flex;
-  gap: 12px;
+  gap: 14px;
   align-items: flex-start;
-  margin-bottom: 20px;
+  margin-bottom: 24px;
+}
+.brief-form-head svg { color: var(--accent); margin-top: 4px; flex-shrink: 0; }
+.brief-form-head h3 {
+  margin: 0 0 6px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.25rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
 }
-.brief-form-head svg { color: var(--mm-blue); margin-top: 4px; flex-shrink: 0; }
-.brief-form-head h3 { margin: 0 0 4px; font-size: 1.1rem; color: var(--white); }
-.brief-form-head p { margin: 0; font-size: 0.88rem; opacity: 0.7; }
+.brief-form-head p { margin: 0; font-size: 0.92rem; color: var(--ink-300); line-height: 1.55; }
 .brief-form-grid {
   display: grid;
   grid-template-columns: repeat(2, 1fr);
-  gap: 14px;
+  gap: 16px;
 }
-.brief-field { display: flex; flex-direction: column; gap: 6px; }
+.brief-field { display: flex; flex-direction: column; gap: 7px; }
 .brief-field-full { grid-column: 1 / -1; }
 .brief-field > span {
-  font-size: 0.78rem;
-  font-weight: 600;
-  letter-spacing: 0.3px;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 700;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  opacity: 0.75;
+  color: var(--ink-300);
 }
-.brief-field > span em { color: var(--mm-pink); font-style: normal; margin-left: 2px; }
+.brief-field > span em { color: var(--accent); font-style: normal; margin-left: 4px; }
 .brief-field input,
 .brief-field select,
 .brief-field textarea {
-  border: 1px solid rgba(255, 255, 255, 0.12);
-  border-radius: 10px;
-  padding: 11px 13px;
+  border: 1px solid var(--border-light);
+  border-radius: var(--radius-md);
+  padding: 12px 14px;
   font: inherit;
-  background: rgba(255, 255, 255, 0.04);
-  color: var(--white);
-  transition: border-color 0.15s, background 0.15s;
+  background: var(--bg-surface);
+  color: var(--ink-100);
+  transition: border-color var(--dur-fast) var(--ease),
+              box-shadow var(--dur-fast) var(--ease);
 }
 .brief-field input::placeholder,
-.brief-field textarea::placeholder { color: rgba(255, 255, 255, 0.35); }
+.brief-field textarea::placeholder { color: var(--ink-400); }
 .brief-field input:focus,
 .brief-field select:focus,
 .brief-field textarea:focus {
   outline: none;
-  border-color: var(--mm-blue);
-  background: rgba(255, 255, 255, 0.06);
+  border-color: var(--accent);
+  box-shadow: 0 0 0 3px var(--accent-glow);
 }
 .brief-field select option { background: var(--bg-card); color: var(--white); }
 .brief-field textarea { resize: vertical; min-height: 80px; }
@@ -8397,7 +9619,8 @@ textarea:focus-visible {
   width: 9px;
   height: 9px;
   border-radius: 50%;
-  background: linear-gradient(135deg, var(--mm-blue), var(--mm-pink));
+  background: var(--accent);
+  box-shadow: 0 0 14px var(--accent-glow);
   animation: rfBounce 1.1s var(--ease-out) infinite;
   opacity: 0.8;
 }
@@ -8416,7 +9639,7 @@ textarea:focus-visible {
 .icon-btn:focus-visible,
 .checkout-btn:focus-visible,
 .add-btn:focus-visible {
-  outline: 2px solid var(--mm-blue);
+  outline: 2px solid var(--accent);
   outline-offset: 3px;
   border-radius: 4px;
 }
@@ -8820,10 +10043,13 @@ textarea:focus-visible {
   border-bottom: 1px solid var(--border);
 }
 .nav-mobile-head .brand-name {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
   font-size: 1.1rem;
-  letter-spacing: 0.18em;
-  text-transform: uppercase;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
+  text-transform: none;
 }
 .nav-mobile-links {
   display: flex;
@@ -8831,12 +10057,13 @@ textarea:focus-visible {
   gap: 4px;
 }
 .nav-mobile-links .nav-link {
-  padding: 14px 12px;
-  border-radius: 10px;
-  font-size: 0.78rem;
+  padding: 14px 14px;
+  border-radius: var(--radius-md);
+  font-size: 12px;
 }
-.nav-mobile-links .nav-link:hover { background: var(--bg-hover); }
-.nav-mobile-links .nav-link.active { background: rgba(74, 144, 217, 0.12); }
+.nav-mobile-links .nav-link::after { display: none; }
+.nav-mobile-links .nav-link:hover { background: var(--bg-hover); color: var(--ink-100); }
+.nav-mobile-links .nav-link.active { background: rgba(201, 151, 74, 0.12); color: var(--accent-soft); }
 .nav-mobile-account,
 .nav-mobile-auth {
   display: flex;
@@ -8868,20 +10095,26 @@ textarea:focus-visible {
 .recently-viewed-card {
   background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 12px;
-  padding: 10px;
+  border-radius: var(--radius-lg);
+  padding: 12px;
   cursor: pointer;
   text-align: left;
-  transition: border-color 0.25s var(--ease), transform 0.25s var(--ease);
+  transition: border-color var(--dur-base) var(--ease),
+              transform var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
   display: flex;
   flex-direction: column;
-  gap: 10px;
+  gap: 12px;
+}
+.recently-viewed-card:hover {
+  border-color: var(--border-strong);
+  transform: translateY(-3px);
+  box-shadow: var(--shadow-2);
 }
-.recently-viewed-card:hover { border-color: var(--mm-blue); transform: translateY(-3px); }
 .recently-viewed-image {
   width: 100%;
   aspect-ratio: 1.1;
-  border-radius: 8px;
+  border-radius: var(--radius-md);
   overflow: hidden;
   background: var(--bg-surface);
   display: flex;
@@ -8889,22 +10122,35 @@ textarea:focus-visible {
   justify-content: center;
 }
 .recently-viewed-image img { width: 100%; height: 100%; object-fit: cover; }
-.recently-viewed-info { display: flex; flex-direction: column; gap: 2px; padding: 0 4px 6px; }
+.recently-viewed-info { display: flex; flex-direction: column; gap: 4px; padding: 4px 6px 8px; }
 .recently-viewed-category {
-  font-size: 0.62rem;
+  font-family: var(--font-sans);
+  font-size: 10px;
+  font-weight: 700;
   text-transform: uppercase;
-  letter-spacing: 0.18em;
-  color: var(--text-muted);
+  letter-spacing: var(--ls-widest);
+  color: var(--accent);
 }
 .recently-viewed-name {
-  font-size: 0.86rem;
-  font-weight: 600;
-  color: var(--text-primary);
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 24, 'SOFT' 30;
+  font-size: 0.95rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
   overflow: hidden;
   text-overflow: ellipsis;
   white-space: nowrap;
 }
-.recently-viewed-price { font-size: 0.95rem; font-weight: 700; color: var(--mm-blue); }
+.recently-viewed-price {
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.1rem;
+  font-weight: 500;
+  letter-spacing: var(--ls-tight);
+  color: var(--ink-100);
+  margin-top: 2px;
+}
 
 /* =====================================================================
    Command palette (⌘K)
@@ -9070,10 +10316,10 @@ textarea:focus-visible {
 /* ===== WHAT WE OFFER ===== */
 .offer-section {
   position: relative;
-  padding: 120px 0 110px;
+  padding: var(--section-gap) 0;
   background:
-    radial-gradient(ellipse 70% 60% at 50% 0%, rgba(74, 144, 217, 0.05), transparent 70%),
-    radial-gradient(ellipse 60% 50% at 50% 100%, rgba(212, 100, 122, 0.05), transparent 70%),
+    radial-gradient(ellipse 70% 60% at 50% 0%, rgba(201, 151, 74, 0.06), transparent 70%),
+    radial-gradient(ellipse 60% 50% at 50% 100%, rgba(28, 24, 20, 0.4), transparent 70%),
     var(--bg-primary);
   overflow: hidden;
 }
@@ -9082,32 +10328,21 @@ textarea:focus-visible {
   position: absolute;
   inset: 0;
   background-image:
-    linear-gradient(rgba(255, 255, 255, 0.09) 1px, transparent 1px),
-    linear-gradient(90deg, rgba(255, 255, 255, 0.09) 1px, transparent 1px);
-  background-size: 64px 64px;
-  -webkit-mask-image: radial-gradient(ellipse 75% 65% at 50% 50%, #000 40%, transparent 95%);
-          mask-image: radial-gradient(ellipse 75% 65% at 50% 50%, #000 40%, transparent 95%);
-  opacity: 1;
-  pointer-events: none;
-}
-.offer-section::after {
-  content: '';
-  position: absolute;
-  inset: 0;
-  background-image:
-    linear-gradient(rgba(255, 255, 255, 0.035) 1px, transparent 1px),
-    linear-gradient(90deg, rgba(255, 255, 255, 0.035) 1px, transparent 1px);
-  background-size: 16px 16px;
-  -webkit-mask-image: radial-gradient(ellipse 60% 55% at 50% 50%, #000 30%, transparent 85%);
-          mask-image: radial-gradient(ellipse 60% 55% at 50% 50%, #000 30%, transparent 85%);
+    linear-gradient(rgba(244, 236, 224, 0.04) 1px, transparent 1px),
+    linear-gradient(90deg, rgba(244, 236, 224, 0.04) 1px, transparent 1px);
+  background-size: 72px 72px;
+  -webkit-mask-image: radial-gradient(ellipse 70% 60% at 50% 50%, #000 35%, transparent 90%);
+          mask-image: radial-gradient(ellipse 70% 60% at 50% 50%, #000 35%, transparent 90%);
+  opacity: 0.8;
   pointer-events: none;
 }
+.offer-section::after { display: none; }
 .offer-section-glow {
   position: absolute;
   width: 520px;
   height: 520px;
   border-radius: 50%;
-  background: radial-gradient(circle, rgba(74, 144, 217, 0.18), transparent 70%);
+  background: radial-gradient(circle, rgba(201, 151, 74, 0.16), transparent 70%);
   top: 10%;
   left: -180px;
   filter: blur(80px);
@@ -9119,7 +10354,7 @@ textarea:focus-visible {
   width: 480px;
   height: 480px;
   border-radius: 50%;
-  background: radial-gradient(circle, rgba(212, 100, 122, 0.18), transparent 70%);
+  background: radial-gradient(circle, rgba(143, 106, 48, 0.18), transparent 70%);
   top: 40vh;
   right: -300px;
   filter: blur(80px);
@@ -9142,34 +10377,32 @@ textarea:focus-visible {
   position: relative;
   display: flex;
   flex-direction: column;
-  background:
-    linear-gradient(180deg, rgba(36, 32, 28, 0.7), rgba(22, 20, 18, 0.55));
+  background: var(--bg-card);
   border: 1px solid var(--border);
-  border-radius: 24px;
+  border-radius: var(--radius-xl);
   overflow: hidden;
-  transition: transform 0.4s var(--ease-out), border-color 0.4s var(--ease-out), box-shadow 0.4s var(--ease-out);
+  transition: transform var(--dur-base) var(--ease),
+              border-color var(--dur-base) var(--ease),
+              box-shadow var(--dur-base) var(--ease);
   isolation: isolate;
+  backdrop-filter: blur(20px);
 }
 .offer-pillar::before {
   content: '';
   position: absolute;
-  inset: 0;
-  border-radius: 24px;
-  padding: 1px;
-  background: linear-gradient(160deg, rgba(255, 255, 255, 0.08), transparent 40%);
-  -webkit-mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
-          mask: linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
-  -webkit-mask-composite: xor;
-          mask-composite: exclude;
+  inset: 0 0 auto 0;
+  height: 1px;
+  background: linear-gradient(90deg, transparent, rgba(244, 236, 224, 0.12), transparent);
   pointer-events: none;
+  z-index: 4;
 }
 .offer-pillar:hover {
-  transform: translateY(-6px);
-  border-color: var(--border-light);
-  box-shadow: 0 30px 80px -30px rgba(0, 0, 0, 0.6);
+  transform: translateY(-5px);
+  border-color: var(--border-strong);
+  box-shadow: var(--shadow-4);
 }
-.offer-pillar-pink:hover { box-shadow: 0 30px 80px -30px rgba(212, 100, 122, 0.35); }
-.offer-pillar-blue:hover { box-shadow: 0 30px 80px -30px rgba(74, 144, 217, 0.35); }
+.offer-pillar-pink:hover { box-shadow: var(--shadow-4), 0 30px 80px -30px var(--accent-glow); }
+.offer-pillar-blue:hover { box-shadow: var(--shadow-4), 0 30px 80px -30px var(--accent-glow); }
 
 .offer-pillar-media {
   position: relative;
@@ -9180,50 +10413,55 @@ textarea:focus-visible {
   width: 100%;
   height: 100%;
   object-fit: cover;
-  transition: transform 0.8s var(--ease-out);
+  filter: saturate(0.92);
+  transition: transform var(--dur-slow) var(--ease),
+              filter var(--dur-slow) var(--ease);
 }
 .offer-pillar:hover .offer-pillar-media img {
-  transform: scale(1.05);
+  transform: scale(1.04);
+  filter: saturate(1);
 }
 .offer-pillar-media-fade {
   position: absolute;
   inset: 0;
-  background: linear-gradient(180deg, transparent 50%, rgba(10, 9, 8, 0.85) 100%);
+  background: linear-gradient(180deg, transparent 55%, rgba(12, 10, 9, 0.92) 100%);
   pointer-events: none;
 }
 .offer-pillar-index {
   position: absolute;
-  top: 20px;
-  left: 22px;
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 44px;
-  font-weight: 500;
+  top: 22px;
+  left: 26px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 144, 'SOFT' 30;
+  font-size: 56px;
+  font-weight: 400;
   line-height: 1;
-  color: var(--white);
-  letter-spacing: 0.02em;
-  text-shadow: 0 2px 18px rgba(0, 0, 0, 0.45);
+  color: var(--ink-100);
+  letter-spacing: var(--ls-tight);
+  text-shadow: 0 4px 24px rgba(0, 0, 0, 0.55);
   z-index: 2;
 }
-.offer-pillar-pink .offer-pillar-index { color: var(--mm-pink-light); }
-.offer-pillar-blue .offer-pillar-index { color: var(--mm-blue-light); }
+.offer-pillar-pink .offer-pillar-index,
+.offer-pillar-blue .offer-pillar-index { color: var(--accent-soft); }
 
 .offer-pillar-tag {
   position: absolute;
-  top: 22px;
-  right: 22px;
+  top: 24px;
+  right: 24px;
   display: inline-flex;
   align-items: center;
-  gap: 6px;
-  padding: 7px 12px;
-  background: rgba(10, 9, 8, 0.72);
-  backdrop-filter: blur(10px);
-  -webkit-backdrop-filter: blur(10px);
-  border: 1px solid rgba(255, 255, 255, 0.12);
-  border-radius: 999px;
-  color: var(--white);
-  font-size: 11px;
-  font-weight: 600;
-  letter-spacing: 0.08em;
+  gap: 7px;
+  padding: 8px 14px;
+  background: rgba(12, 10, 9, 0.75);
+  backdrop-filter: blur(12px);
+  -webkit-backdrop-filter: blur(12px);
+  border: 1px solid var(--border-strong);
+  border-radius: var(--radius-pill);
+  color: var(--accent-soft);
+  font-family: var(--font-sans);
+  font-size: 10px;
+  font-weight: 700;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
   z-index: 2;
 }
@@ -9231,73 +10469,76 @@ textarea:focus-visible {
 .offer-pillar-body {
   display: flex;
   flex-direction: column;
-  gap: 18px;
-  padding: 32px 32px 34px;
+  gap: 20px;
+  padding: 36px 36px 38px;
 }
 
 .offer-pillar-title {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: clamp(26px, 2.5vw, 34px);
-  font-weight: 500;
-  line-height: 1.1;
-  color: var(--white);
-  letter-spacing: 0.005em;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 96, 'SOFT' 30;
+  font-size: clamp(1.8rem, 2.6vw, 2.4rem);
+  font-weight: 400;
+  line-height: 1.08;
+  color: var(--ink-100);
+  letter-spacing: var(--ls-tight);
 }
 
 .offer-pillar-desc {
-  color: var(--text-secondary);
-  font-size: 14.5px;
-  line-height: 1.6;
+  color: var(--ink-300);
+  font-size: 0.98rem;
+  line-height: 1.65;
 }
 
 .offer-pillar-specs {
   display: grid;
   grid-template-columns: repeat(3, 1fr);
   gap: 10px;
-  padding: 14px;
-  background: rgba(255, 255, 255, 0.02);
+  padding: 16px;
+  background: rgba(244, 236, 224, 0.02);
   border: 1px solid var(--border);
-  border-radius: 14px;
+  border-radius: var(--radius-md);
 }
 .offer-spec {
   display: flex;
   flex-direction: column;
   align-items: center;
-  gap: 4px;
+  gap: 6px;
   text-align: center;
 }
 .offer-spec-label {
-  font-size: 10px;
-  font-weight: 600;
-  letter-spacing: 0.12em;
+  font-family: var(--font-sans);
+  font-size: 9.5px;
+  font-weight: 700;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-muted);
+  color: var(--ink-400);
 }
 .offer-spec-value {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 19px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.05rem;
   font-weight: 500;
-  color: var(--white);
-  letter-spacing: 0.01em;
+  color: var(--ink-100);
+  letter-spacing: var(--ls-tight);
 }
-.offer-pillar-pink .offer-spec-value { color: var(--mm-pink-light); }
-.offer-pillar-blue .offer-spec-value { color: var(--mm-blue-light); }
+.offer-pillar-pink .offer-spec-value,
+.offer-pillar-blue .offer-spec-value { color: var(--accent-soft); }
 
 .offer-pillar-features {
   list-style: none;
   display: flex;
   flex-direction: column;
-  gap: 9px;
+  gap: 11px;
   padding: 0;
   margin: 4px 0 0;
 }
 .offer-pillar-features li {
   display: flex;
   align-items: center;
-  gap: 10px;
-  color: var(--text-secondary);
-  font-size: 13.5px;
-  line-height: 1.45;
+  gap: 12px;
+  color: var(--ink-200);
+  font-size: 0.94rem;
+  line-height: 1.5;
 }
 .offer-pillar-features li svg {
   flex-shrink: 0;
@@ -9305,16 +10546,13 @@ textarea:focus-visible {
   width: 22px;
   height: 22px;
   border-radius: 50%;
-  background: rgba(255, 255, 255, 0.06);
-  color: var(--white);
-}
-.offer-pillar-pink .offer-pillar-features li svg {
-  background: rgba(212, 100, 122, 0.16);
-  color: var(--mm-pink-light);
+  background: rgba(201, 151, 74, 0.12);
+  color: var(--accent);
 }
+.offer-pillar-pink .offer-pillar-features li svg,
 .offer-pillar-blue .offer-pillar-features li svg {
-  background: rgba(74, 144, 217, 0.16);
-  color: var(--mm-blue-light);
+  background: rgba(201, 151, 74, 0.14);
+  color: var(--accent);
 }
 
 .offer-pillar-cta {
@@ -9348,29 +10586,31 @@ textarea:focus-visible {
   width: 40px;
   height: 40px;
   border-radius: 12px;
-  background: linear-gradient(135deg, rgba(74, 144, 217, 0.18), rgba(212, 100, 122, 0.18));
-  color: var(--white);
-  border: 1px solid var(--border-light);
+  background: rgba(201, 151, 74, 0.10);
+  color: var(--accent);
+  border: 1px solid rgba(201, 151, 74, 0.30);
 }
 .offer-trust-text { min-width: 0; }
 .offer-trust-num {
-  font-family: 'Yanone Kaffeesatz', sans-serif;
-  font-size: 20px;
+  font-family: var(--font-display);
+  font-variation-settings: 'opsz' 32, 'SOFT' 30;
+  font-size: 1.18rem;
   font-weight: 500;
-  color: var(--white);
-  letter-spacing: 0.01em;
+  color: var(--ink-100);
+  letter-spacing: var(--ls-tight);
   line-height: 1;
   white-space: nowrap;
   overflow: hidden;
   text-overflow: ellipsis;
 }
 .offer-trust-label {
-  margin-top: 3px;
-  font-size: 11px;
-  font-weight: 500;
-  letter-spacing: 0.06em;
+  margin-top: 6px;
+  font-family: var(--font-sans);
+  font-size: 10.5px;
+  font-weight: 600;
+  letter-spacing: var(--ls-widest);
   text-transform: uppercase;
-  color: var(--text-muted);
+  color: var(--ink-400);
   white-space: nowrap;
   overflow: hidden;
   text-overflow: ellipsis;
diff --git a/src/components/Footer.jsx b/src/components/Footer.jsx
index b4836c8..4e354a0 100644
--- a/src/components/Footer.jsx
+++ b/src/components/Footer.jsx
@@ -1,5 +1,7 @@
 import { Link } from 'react-router-dom'
-import { Globe, Mail, Phone, MapPin } from 'lucide-react'
+import { useState } from 'react'
+import { Globe, Mail, Phone, MapPin, Clock, ArrowRight, Send } from 'lucide-react'
+import toast from 'react-hot-toast'
 
 function InstagramIcon({ size = 16 }) {
   return (
@@ -28,15 +30,50 @@ function XIcon({ size = 16 }) {
 }
 
 export default function Footer() {
+  const [email, setEmail] = useState('')
+
+  const onSubscribe = (e) => {
+    e.preventDefault()
+    if (!email) return
+    toast.success("Thanks — we'll keep you posted.")
+    setEmail('')
+  }
+
   return (
     <footer className="footer">
+      <div className="footer-newsletter">
+        <div className="footer-newsletter-inner">
+          <div className="footer-newsletter-copy">
+            <span className="eyebrow">The Brew Letter</span>
+            <h3 className="footer-newsletter-title">Coffee notes from the bar.</h3>
+            <p>New roasts, lesson drops, and the occasional cafe story — once a month, never more.</p>
+          </div>
+          <form className="footer-newsletter-form" onSubmit={onSubscribe}>
+            <input
+              type="email"
+              value={email}
+              onChange={(e) => setEmail(e.target.value)}
+              placeholder="Your email address"
+              required
+              aria-label="Email address"
+            />
+            <button type="submit" className="footer-newsletter-submit" aria-label="Subscribe">
+              <span>Subscribe</span>
+              <ArrowRight size={14} />
+            </button>
+          </form>
+        </div>
+      </div>
+
       <div className="footer-main">
         <div className="footer-brand">
-          <div className="footer-brand-logo">
+          <Link to="/" className="footer-brand-logo">
             <img src="/logo.png" alt="Mastermind Brews" />
             <span>Mastermind Brews</span>
-          </div>
-          <p>Specialty coffee beans from Chikmagalur, Karnataka, roasted by Bean Rove. From the team behind Mastermind Bicycle Cafe &amp; Bar in Mulund, Mumbai.</p>
+          </Link>
+          <p className="footer-brand-blurb">
+            Single-origin specialty coffee from Chikmagalur, roasted in partnership with Bean Rove. By the team behind Mastermind Bicycle Cafe &amp; Bar, Mulund.
+          </p>
           <div className="footer-social">
             <a href="https://www.instagram.com/mastermindbicyclecafe/" target="_blank" rel="noopener noreferrer" aria-label="Instagram"><InstagramIcon size={16} /></a>
             <a href="https://www.facebook.com/mastermindbicyclecafe/" target="_blank" rel="noopener noreferrer" aria-label="Facebook"><FacebookIcon size={16} /></a>
@@ -45,39 +82,69 @@ export default function Footer() {
         </div>
 
         <div className="footer-col">
-          <h4>Quick Links</h4>
-          <Link to="/">Home</Link>
-          <Link to="/about">About Us</Link>
+          <h4>Shop</h4>
           <Link to="/store">Buy Coffee</Link>
           <Link to="/workshop">Learn Coffee</Link>
           <Link to="/consultancy">Our Projects</Link>
+          <Link to="/baristas">Hire Baristas</Link>
+        </div>
+
+        <div className="footer-col">
+          <h4>Company</h4>
+          <Link to="/about">About Us</Link>
+          <Link to="/blog">Journal</Link>
+          <Link to="/contact">Contact</Link>
+          <Link to="/barista-signup">Join the Network</Link>
         </div>
 
         <div className="footer-col">
           <h4>Account</h4>
-          <Link to="/login">My Account</Link>
-          <Link to="/signup">Sign Up</Link>
+          <Link to="/login">Sign In</Link>
+          <Link to="/signup">Create Account</Link>
           <Link to="/my-orders">My Orders</Link>
           <Link to="/wishlist">Wishlist</Link>
         </div>
 
-        <div className="footer-col">
-          <h4>Get In Touch</h4>
-          <a href="tel:+918591850161"><Phone size={12} /> +91 85918 50161</a>
-          <a href="mailto:hello@mastermindcafe.in"><Mail size={12} /> hello@mastermindcafe.in</a>
-          <a href="https://www.mastermindcafe.in/" target="_blank" rel="noopener noreferrer"><Globe size={12} /> mastermindcafe.in</a>
-          <a href="https://maps.google.com/?q=Mastermind+Bicycle+Cafe+Mulund" target="_blank" rel="noopener noreferrer"><MapPin size={12} /> Avior Corporate Park, Mulund West, Mumbai</a>
-        </div>
+        <aside className="footer-cafe-card">
+          <span className="eyebrow">Visit the Cafe</span>
+          <h4 className="footer-cafe-title">Mastermind<br/>Bicycle Cafe &amp; Bar</h4>
+          <div className="footer-cafe-meta">
+            <div className="footer-cafe-row">
+              <MapPin size={14} />
+              <span>Avior Corporate Park, LBS Marg, Mulund West, Mumbai</span>
+            </div>
+            <div className="footer-cafe-row">
+              <Clock size={14} />
+              <span>Open daily &middot; 11 AM &ndash; 11 PM</span>
+            </div>
+            <div className="footer-cafe-row">
+              <Phone size={14} />
+              <a href="tel:+918591850161">+91 85918 50161</a>
+            </div>
+            <div className="footer-cafe-row">
+              <Mail size={14} />
+              <a href="mailto:hello@mastermindcafe.in">hello@mastermindcafe.in</a>
+            </div>
+          </div>
+          <a
+            href="https://maps.google.com/?q=Mastermind+Bicycle+Cafe+Mulund"
+            target="_blank"
+            rel="noopener noreferrer"
+            className="footer-cafe-map"
+          >
+            Open in Maps <ArrowRight size={12} />
+          </a>
+        </aside>
       </div>
 
       <div className="footer-bottom">
-        <p>&copy; 2026 Mastermind Bicycle Cafe & Bar. All rights reserved.</p>
+        <p>&copy; 2026 Mastermind Bicycle Cafe &amp; Bar. Crafted in Mulund, Mumbai.</p>
         <div className="footer-bottom-links">
-          <Link to="/privacy-policy">Privacy Policy</Link>
-          <Link to="/terms">Terms & Conditions</Link>
-          <Link to="/refund-policy">Return & Refund</Link>
-          <Link to="/shipping">Shipping & Delivery</Link>
-          <Link to="/contact">Contact Us</Link>
+          <Link to="/privacy-policy">Privacy</Link>
+          <Link to="/terms">Terms</Link>
+          <Link to="/refund-policy">Refunds</Link>
+          <Link to="/shipping">Shipping</Link>
+          <Link to="/contact">Contact</Link>
         </div>
       </div>
     </footer>
diff --git a/src/components/Navbar.jsx b/src/components/Navbar.jsx
index 185efb0..1451cf8 100644
--- a/src/components/Navbar.jsx
+++ b/src/components/Navbar.jsx
@@ -68,7 +68,7 @@ export default function Navbar({ onOpenSearch }) {
           <img src="/logo.png" alt="Mastermind Brews" />
           <div className="nav-logo-text">
             <span className="brand-name">Mastermind Brews</span>
-            <span className="brand-sub">Coffee &middot; Academy &middot; Hire</span>
+            <span className="brand-sub">Est. Mulund, Mumbai</span>
           </div>
         </Link>
 
